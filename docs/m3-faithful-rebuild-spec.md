# M3 "Boksidan" — faithful product rebuild spec

**Goal:** make the live product *be* M3, not EDITION-with-rail-accents. Each task rebuilds a live surface to match a specific M3 reference screen, element for element, verified by visual diff before it is called done.

**The spec is the artifact.** M3 lives at `/redesign-l12?dev=1&v=3` (component `app/src/components/devbake/l12/M3.tsx`, fixtures `devbake/redesign/fixtures.ts` + `fixturesSections.ts`). When in doubt, read M3.tsx and copy its exact values.

> Generated from the grounded task-spec panel (8 agents reading M3 + live + tests). Per-task element tables are exhaustive; the task manager (#152–#158) carries the actionable digest + a pointer here.

## Locked decisions

- **Desktop drill = SINGLE column** (`.m3-frame` max-width 880px). Reverses tasks 80–87's 2-col StudyDesk.
- **Phone drill = linear + bare + floating bottom-center "Nästa fråga" pill.**
- **Desktop chrome = Minimal mast** — one quiet hairline band: brand `⌜ HP-Coach` + visible nav (Hem/Övning/Lektion/Framsteg) + quiet picker corner. No section echo, no bottom status line, no exam-tag flourish.
- **M2 reprints each distractor's struck option text** (`M3.tsx:973-974`), replacing the live letter-only block (`PedagogyPanel.tsx:600`). Load-bearing for the single-column call.
- **Focused phone drill gains a visible touch exit** (back/close) — M1. Esc/⌘K don't count on phone.
- **⌘K is a power accelerator, not primary nav.** Nav + settings visible without it; picker → future Settings surface.
- **Progress = textual eyebrow** ("FRÅGA n AV total"); no progress bar unless later requested.
- **camelCase↔snake_case trap:** M3 fixtures are camelCase; live corpus is `why_tempting`/`why_wrong`/`pregrade_tactic`, `figure` (not figureMeta), no `sectionLabel`/`total`/`contextTitle`. Copy M3's CSS/px, not its field accesses.

## Execution order
- M0 (#152) FIRST — pure CSS-layer + doc audit with zero markup change; M1/M2/M3H/M4 all consume the .hpc-m3-* GAP families and the meta-color/keyframe fixes it lands, so building any screen before M0 forces every screen task to re-add CSS and risk conflicting blocks in index.css.
- M1 (#153) — reverses desktop drill to single-column .m3-frame and stops SessionPlayer routing active drill to BoksidanDesk; this is the structural pivot the whole rebuild hangs on, and it owns the shared eyebrow/usePregradeTactic/sectionLongLabel/position+total plumbing M4 and M5 import, so it must precede them.
- M2 (#154) — graded/pedagogy rewrite builds BELOW M1's single-column options (the struck-option-text reprint is only necessary because the referent can no longer sit in an adjacent column); it depends on M1's flush/onNext wiring and the surviving call site, so it follows M1.
- M3H (#155) — Home rebuild is independent of the drill graded path and can run in parallel with M2 after M0, but is sequenced here because it shares the minimal-mast chrome decision with M1 and must reuse (not fork) the mast M1 finalizes.
- M4 (#156) — section apparatus VERIFIES + fills gaps on top of M0's chassis, M1's eyebrow/tactic/keys, and M2's distractor reprint across LÄS/NOG/XYZ/DTK; it is a hard downstream of all three so it lands after them.
- M5 (#157) — carry-over surfaces (diagnostik/auth/repetition/lektion) INHERIT the final drill/home chrome via shared SessionPlayer/Page; harmonizing before M1/M2/M3H/M4 are final would chase a moving target, so it lands after the screens it borrows from.
- M6 (#158) LAST — read-only cross-surface fidelity sweep that can only PASS once M0–M5 are merged and owner-approved; any FAIL routes back to the owning task, so it is the terminal gate, never a parallel task.

## Global Definition of Done
- Every M-series PR (M0–M6) carries the per-task anti-drift gate satisfied: M3 reference URL+state cited (/redesign-l12?dev=1&v=3 with the correct s=/q= and, for graded, the click-to-grade interaction recorded), live+M3 side-by-side screenshots at BOTH 1440 AND 402 in BOTH light AND dark, palette forced to spalt (DEFAULT_THEME.palette (tokens.ts:495)) with motion settled/reduced.
- Across the set, EVERY .m3-* selector + all 5 keyframes from M3.tsx (CSS const 33-723) is accounted for in docs/m3-class-map.md (M0) and rendered faithfully on at least one live surface; no selector silently unmapped.
- All LOCKED DECISIONS are observably true in the live build: desktop drill single-column .m3-frame 880px (no BoksidanDesk 2-col at 1440), phone drill linear+bare with floating bottom-center 'Nästa fråga' pill + visible touch exit, desktop minimal-mast (brand + Hem/Övning/Lektion/Framsteg + quiet picker corner, NO section echo / NO bottom status line / NO exam-tag), M2 reprints each distractor's struck option text, progress is textual eyebrow only (no progress bar), ⌘K not primary nav.
- Exact px/clamp/letter-spacing/color-token/Swedish-copy values copied VERBATIM from the cited M3.tsx lines (verified against computed styles/DOM, not eyeballed); NO M3 camelCase field access leaked into live code — live reads snake_case (why_tempting/why_wrong/pregrade_tactic/solution_path), 'figure' not figureMeta, and derives sectionLabel/total/contextTitle since the corpus lacks them.
- All data-testids preserved and resolvable (drill-prompt carrying ONLY the headword/stem, drill-next, drill-context on the passage body, question-figure, drill-exit (new, phone), every pedagogy-* incl. folded technique/pitfall, home-score-line relocated to stats, daily-plan-*, top-trap-link/trap-trend, auth-form-pane/auth-brand-pane); rewritten tests (HomeMobile 3 score-line cases + plan-eyebrow case, any StudyDesk/2-col responsive assertions, at-risk page-shell/running-head/status-line) updated in the SAME PR that changes their contract.
- pnpm test (vitest) AND pnpm test:e2e (playwright) green at the relevant viewports for every PR; pnpm build clean (TS, no dead imports after folds/removals); the M6 report records pass counts as evidence.
- docs/m3-class-map.md (M0) and docs/m3-fidelity-report.md (M6) both exist and are complete; M6 shows every grid row PASS before merge.
- Owner approves the visual diff before EVERY merge — NO auto-merge label on ANY M-series PR; each PR branches off main (or the M-series base) and merges promptly once green + approved.

## Anti-drift gates
- GATE 1 — Diff-against-M3 is mandatory, not optional: every PR must attach live+M3 side-by-side captures at 1440 AND 402 in light AND dark before review. Tasks 80-87 drifted precisely because they screenshotted only the live result and never the M3 reference beside it. M6 (#158) is a dedicated terminal task whose ONLY deliverable is the 36-cell (72-image) comparison grid — drift cannot survive a task that exists solely to diff.
- GATE 2 — Reverse the 2-col graft explicitly: M1 must make SessionPlayer STOP routing active drill to BoksidanDesk/StudyDesk (the tasks-80-87 artifact) and the M0 class-map must FLAG .hpc-studydesk* (index.css:940-969) as non-M3 drift scheduled for removal. The fidelity sweep FAILS any surface still showing 2-col at 1440. The graft's load-bearing structure is named and torn out, not merely re-skinned.
- GATE 3 — Copy CSS values, never field accesses: the systemic camelCase(M3)/snake_case(live) trap is documented in BOLD in docs/m3-class-map.md and repeated in every screen task's dataTraps; the rule is 'port M3's px/clamp/token VALUES, read live snake_case fields'. M6 judges RENDERED output (px/copy), not field names, so a correct snake_case mapping is a PASS and a leaked camelCase access shows up as undefined/blank (a visible FAIL).
- GATE 4 — Per-element checklist with file:line + MATCH/GAP/WRONG + acceptance: no task may claim done on a 'looks good' basis; each element row names the M3 source line, the exact value, the live status, and a one-line acceptance check, so 'matches M3 per-element' is the bar (not 'has M3 accents'). The grafting failure was accent-bolting without element-level diffing — the checklist format makes that impossible to pass off.
- GATE 5 — Owner approves the visual before merge, NO auto-merge on any M-series PR: the human who said 'looks nothing like what we agreed on' is the final gate on every PR, and the standing auto-merge workflow is explicitly disabled for this set so nothing green-but-drifted self-merges.
- GATE 6 — M3 reference must stay live and READ-ONLY at /redesign-l12?dev=1&v=3 (M3.tsx + fixtures.ts/fixturesSections.ts); editing the reference is forbidden, and if the dev route is removed the sweep loses its truth source (M6 dependency). The single source of truth is pinned on screen for every comparison.

## Cross-task risks
- SHARED PLUMBING OWNERSHIP (highest risk): M1 creates src/lib/sectionLongLabel.ts, src/lib/usePregradeTactic.ts, and threads position/total as new optional props into DrillQuestion; M4 and M5 IMPORT these. If M1's signatures drift or land late, M4's eyebrow and M5's carry-over inherit break. Mitigation is real but fragile — name/locate them in M1 exactly as M4 expects, and gate M4/M5 on M1 merged. A late prop-shape change in M1 cascades into 3 tasks.
- POSITION/TOTAL THREADING is a multi-call-site change: SessionPlayer passes position=index+1/total=plan.length on BOTH phone and (new) desktop paths; StudyDesk must forward if retained; the eyebrow + rail meta '3 / 10' both consume them. The eyebrow MUST guard against undefined (DrillVariantShell.test.tsx mounts without them) or the shell test throws. Risk: one call site forgotten → eyebrow shows 'FRÅGA — AV —' or crashes on a path nobody screenshots.
- HOME-SCORE-LINE TEST REWRITE collides with a contract change: HomeMobile.test.tsx:117-147 asserts DOT-decimal verbal/kvant ('0.65','verbal 0.81','kvant 0.49','kvant —'); M3H replaces that mono line with a COMMA-prognos stats row and relocates the testid. The 3 score-line cases (incl. the two null-gates at :103/:114) MUST be rewritten in the M3H PR, and scoring.test.ts:208-213 (formatScore dot) must stay UNTOUCHED while formatScoreComma is ADDED. Risk: mutating formatScore breaks /progress; forgetting the rewrite leaves CI red or, worse, a stale assertion passing against drifted UI.
- contextTitle / sectionLabel / total HAVE NO LIVE SOURCE: M3 reads QUESTION.sectionLabel/number/total and question.contextTitle from fixtures; the live corpus has none. M4 must DROP m3-passage-h (no title source — do not invent one) and the eyebrow must DERIVE sectionLabel from sectionLongLabel + total from session length. Risk: a copy-paste of M3's field access yields undefined headers or a fabricated passage title that looks like fidelity but is invented data.
- PRE-GRADE TACTIC SURFACE is new and async: M1 adds usePregradeTactic reading snake_case pregrade_tactic via async loadExplanation (returns null for un-backfilled qids). The aside MUST render gracefully (hidden) on null/loading, never 'undefined'. M2 then must NOT repeat the tactic post-grade (StrategiBlock removed) — the tactic lives ONLY pre-grade. Risk: double-tactic (pre + post) if M2's StrategiBlock removal is missed, or a crash if M1 doesn't null-guard the async load.
- DISTRACTOR-TEXT JOIN is load-bearing AND cross-task: M2 must thread question.options into PedagogyPanel and resolve struck text via options.find(o=>o.letter===d.letter)?.text (DistractorExplanation has NO text field — explanations.test.ts:13 constructs one without it). M4 then VERIFIES this join across XYZ (LaTeX via MathText) and LÄS (long prose). Risk: the join assumes a distractor.text field (breaks the unit test + live type) or 404s a letter with no matching option.
- MINIMAL-MAST OWNERSHIP is ambiguous across M0/M1/M3H/M5/M6: the mast is a LOCKED decision with no M3 element to copy; M1, M3H, M5 all 'reuse the mast M1/M3H finalize' but none is unambiguously the builder. Risk: a second mast fork in Page.tsx, or each surface ships a slightly different band. Needs one explicit mast home (likely Page.tsx shared shell) decided before M1 lands, not 'coordinate' hand-waving.
- EDITION TESTID REMOVAL collides with home.spec.ts: responsive.spec.ts:142-145 / home.spec.ts assert page-shell + running-head + status-line (the EDITION chrome). The minimal-mast decision removes section echo + bottom status line. Risk: these e2e assertions go red on Home when M1/M3H/M5 strip the shell, and it's unclear which task owns rewriting them — M6 flags it but a fix must land in M1/M3H, not M6.

## Gaps / missing tasks flagged
- [RESOLVED 2026-06-18 — now owned by task MC #159; see the 'MC (#159)' section at the end of this doc.] (Originally flagged:) NO TASK OWNS THE MINIMAL-MAST BUILD as a first-class deliverable. M1/M3H/M5 each 'remove the EDITION shell and reuse the mast', and M6 only verifies it, but no task is charged with BUILDING the one shared minimal-mast component (brand + nav + quiet picker corner) and deciding its home (Page.tsx vs a new component). This is the single largest structural gap — the locked chrome decision has no clear builder, exactly the kind of ambiguity that let tasks 80-87 leave doubled orientation. Recommend a small explicit M-chrome task (or assign mast-build unambiguously to M1) before M1's screen work.
- MD (#151, the layout decision bake-off) is referenced as the parent that 'locked single-column' but is still in_progress; its decision is a hard prerequisite for M1/M2 yet none of the 7 specs gate themselves on MD being CLOSED. If MD reopens the single-vs-2-col question, M1's whole reversal is at risk. Recommend M1 explicitly block on MD closeout.
- /progress (ProgressScreen) is named as a live counterpart in M6's capture list but has NO dedicated rebuild task and is NOT in M5's carry-over set. It still uses formatScore (dot) and presumably the EDITION shell. M6 will capture it 2x2 but there is no owning task to route a FAIL to — it falls through the M0-M5 net. Recommend either adding /progress to M5's carry-over surfaces or explicitly scoping it out with owner sign-off.
- The ELF section long-label (and ELF as a section in M4's apparatus) is an OPEN QUESTION in M1 ('Engelsk läsförståelse' vs keep English) and ELF is never captured in M6's section matrix (only las|nog|xyz|dtk). ELF reading apparatus + its English-content label is uncovered — M4 should confirm ELF inherits the LÄS passage chassis and M1 must resolve the label before either ships.
- CMD+K / SETTINGS SURFACE has no build task. The decisions say the palette/mode/edition picker is 'destined for a real Settings surface, not ⌘K-only and not a permanent masthead fixture', but M5 only DOCUMENTS this as a deferred consideration and the 'quiet picker corner' is an interim hack. There is no task to actually build /settings or to verify ⌘K is reachable-but-not-primary. Acceptable to defer, but the decision is only half-covered (picker is removed from being primary, but its real home is unbuilt).
- MEK section drill apparatus is never covered: M4 handles LÄS/NOG/XYZ/DTK and ORD is M1/M2, but MEK (sentence completion, 20q) appears in neither the M4 checklist nor M6's q-toggle matrix. If MEK has any section-specific apparatus beyond plain options it will inherit nothing and may drift. Confirm MEK is just options+prompt (then it's covered by the shared rows) or scope it into M4.
- NO EXPLICIT ROLLBACK/SEQUENCING SAFETY for the index.css shared file: M0, M1, M2, M3H, M4, M5 all edit src/index.css. M0 is additive, but M1-M5 each may add the same GAP classes 'if M0 hasn't landed'. Without a hard 'M0 must merge first' enforcement, two tasks can add conflicting .hpc-m3-* blocks. The specs say 'coordinate' but no task owns the merge-order enforcement — recommend a stated rule that no screen PR touches index.css class definitions once M0 is merged (consume only).

## Decision-coverage check
"All 9 LOCKED DECISIONS are reflected in at least one task: (1) Desktop drill SINGLE COLUMN .m3-frame 880px — M1 (reverses BoksidanDesk in SessionPlayer), reinforced by M0 (flags .hpc-studydesk drift, lands .hpc-m3-frame), M4/M6 verify. (2) Phone drill linear+bare + floating bottom-center 'Nästa fråga' pill — M1 (builds pill + drops running head), M2 (suppresses in-panel next-row when flush), M6 verifies. (3) Desktop minimal-mast (brand+nav+picker, NO section echo/status line/exam-tag) — M1/M3H/M5 each remove EDITION shell, M6 verifies BOTH 'not bare' and 'not full EDITION'; NOTE the build-owner ambiguity flagged in gaps. (4) M2 reprints distractor struck OPTION TEXT — M2 (PedagogyPanel.tsx:600 letter-only → struck text via threaded options), M4 verifies across sections, M0 lands .hpc-m3-dis-h s CSS. (5) Phone focused drill VISIBLE touch exit (Esc/⌘K don't count) — M1 (new drill-exit testid + onHome), M6 verifies at 402. (6) ⌘K power-user accelerator, not primary nav — M1 (removes ⌘k status hint), M3H (removes statusHints), M5 (documents picker→Settings), nav visible in mast. (7) Progress = textual eyebrow, NO progress bar — M1 (deletes DrillProgress, adds .hpc-m3-eyebrow), M3H (textual prognos), M6 FAILS any bar. (8) camelCase(M3)/snake_case(live) systemic trap — M0 (BOLD warning in class-map), every screen task's dataTraps, M6 judges rendered output. (9) Picker destined for real Settings surface not ⌘K-only/not masthead-fixture — M5 documents as deferred consideration (PARTIAL: decision honored by removing picker as primary, but the real Settings home is unbuilt — flagged in gaps). NO LOCKED DECISION IS WHOLLY UNCOVERED; the two soft spots are the minimal-mast BUILDER (decision #3, verified everywhere but built nowhere unambiguously) and the Settings surface (decision #9, documented not built)."

---

## M0 — M0 — Foundation: CSS/class parity audit + fill (.m3-* → .hpc-m3-*)

**Objective.** Make the live `.hpc-m3-*` CSS layer in app/src/index.css a faithful superset of M3.tsx's exported `CSS` const, so M1–M6 can rebuild markup against classes that already match M3 exactly. M0 changes NO screen markup. Deliverables: (1) a complete class→class mapping table at docs/m3-class-map.md (MATCH / GAP / WRONG per selector); (2) fill the GAP families M3 needs (stats/resume/plan/trap/tactic/step-tier/dis/keys/eyebrow/h/cta/tag/next-row/missing/passage-h/ped + the foundation root/frame/reset/mono) into the live layer; (3) the global color fix: `.hpc-m3-meta` color `--accent` → `--muted` (M3.tsx:80) and rewrite the stale comment at index.css:548 that claims the rail is cobalt; (4) reconcile the 3 divergent keyframes (settle, verdict-in, in-x) to M3's exact values; (5) widen reduced-motion to M3's star-wildcard form; (6) add `.hpc-m3-verdict-sub`. Every value copied verbatim from M3.tsx CSS — copy CSS/px, never M3's camelCase field accesses (those are a data trap handled in M1/M2). Live token names already match M3's token names 1:1 (index.css:29-54), so token references port unchanged.

**Reference.** /redesign-l12?dev=1&v=3 — M3 component at app/src/components/devbake/l12/M3.tsx; the audited reference is its exported `CSS` const (M3.tsx:33-723). No screen state needed for M0 (this is a CSS-const-vs-CSS-layer audit, not a rendered-screen diff). M0 visual verification renders the M3 reference at this URL across screen=home / drill=ord|las|nog|xyz|dtk to confirm no live M3-consuming surface (DrillRailSection, DailyPlanCard, TopTrapsCard, DiagnosticReport) regressed after the additive fill + meta color change.

**Files to edit.**
- /home/loucmane/dev/hpfetcher/app/src/index.css (the .hpc-m3-* layer 553-929: meta color line 568, keyframes 900/910/628, reduced-motion 920-929, add new GAP families + verdict-sub; rewrite stale comment 543-551)
- /home/loucmane/dev/hpfetcher/docs/m3-class-map.md (NEW — the full mapping table deliverable; does not exist yet)

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| .m3-root (root container) | M3.tsx:46-54 | min-height:100dvh; background:var(--bg); color:var(--ink); font-family:var(--font-ui); font-size:15px; line-height:1.55; -webkit-font-smoothing:antialiased | GAP | `.hpc-m3-root` exists in live layer with these exact values; the live `body`/root already sets bg/ink/font-ui (index.css:241-244) so the rule is harmless but must exist so M1+ can mount on `.hpc-m3-root`. |
| .m3-reset (button reset) | M3.tsx:34-44 | margin:0; padding:0; border:0; background:none; font:inherit; color:inherit; text-align:inherit; appearance:none; cursor:pointer | GAP | `.hpc-m3-reset` exists; used by CTA + option buttons in M1/M2 so they inherit type and lose UA chrome. |
| .m3-frame (page measure) | M3.tsx:56-60 + responsive M3.tsx:721 | max-width:880px; margin:0 auto; padding:56px 24px 96px. @media(max-width:900px): padding:40px 18px 72px | GAP | `.hpc-m3-frame` exists with 880px measure + both padding values. This is the LOCKED single-column desktop drill width — must be 880px, not the StudyDesk 2-col. |
| .m3-row (rail \| spine \| content grid) | M3.tsx:64-68 + M3.tsx:720 | display:grid; grid-template-columns:128px 1px 1fr; column-gap:28px. @media(max-width:900px): 96px 1px 1fr / column-gap:20px | MATCH | Live index.css:554-558 + 601-604 already match exactly (128px 1px 1fr / 28px; 96px / 20px). Live ALSO adds a 600px phone linearisation (611-626: 1fr, spine hidden, meta left-aligned) NOT in M3 — keep it (phone linear is a LOCKED decision), record as live-addition in the table. |
| .m3-spine (vertical hairline) | M3.tsx:70-73 | background:var(--hairline); align-self:stretch | MATCH | Live index.css:559-562 matches exactly. |
| .m3-meta (rail label) | M3.tsx:75-85 | font-family:var(--font-mono); font-size:11px; letter-spacing:0.08em; text-transform:uppercase; color:var(--muted); text-align:right; padding-top:5px; font-variant-numeric:tabular-nums; animation:m3-in-x 260ms cubic-bezier(0.22,1,0.36,1) both | WRONG | Live index.css:568 sets color:var(--accent) — M3.tsx:80 uses var(--muted). FIX: change live to var(--muted). M3 reserves cobalt --accent for ordinals/tactic-handle/dis-labels/CTA only (DECISION). All else in live 563-573 matches. NB this rail is consumed live by DrillRailSection, DailyPlanCard, TopTrapsCard, DiagnosticReport — the color change is global and intended. |
| .m3-meta strong (rail emphasis) | M3.tsx:87-91 | display:block; color:var(--ink-2); font-weight:500 | MATCH | Live index.css:574-578 matches exactly. |
| .m3-content (content column) | M3.tsx:93-97 | min-width:0; animation:m3-in-y 300ms cubic-bezier(0.22,1,0.36,1) both; animation-delay:60ms | MATCH | Live index.css:579-583 matches exactly. |
| .m3-section / :first-child | M3.tsx:99-100 | margin-top:64px; :first-child margin-top:0. (M3 has no phone override) Live adds @600px margin-top:44px | MATCH | Live index.css:584-589 matches 64px/0; the 44px phone value (623-625) is a live phone-linear addition — keep, record as addition. |
| .m3-rule (drawn hairline) | M3.tsx:102-109 | height:1px; background:var(--hairline); border:0; margin:0 0 20px; transform-origin:left center; animation:m3-draw 260ms cubic-bezier(0.22,1,0.36,1) both | MATCH | Live index.css:590-597 matches exactly. |
| .m3-h (section heading) | M3.tsx:111-118 | font-size:13px; font-weight:600; letter-spacing:0.04em; text-transform:uppercase; color:var(--ink-2); margin:0 0 6px | GAP | `.hpc-m3-h` added with these exact values (the `<h2 className="m3-h">` section titles 'Dagens plan'/'Så löser du den'/'Varför de andra lockar'). |
| .m3-mono (tabular mono helper) | M3.tsx:120-123 | font-family:var(--font-mono); font-variant-numeric:tabular-nums | GAP | `.hpc-m3-mono` added with these exact values (utility used for inline tabular numerals). |
| .m3-eyebrow (mono kicker) | M3.tsx:127-134 | font-family:var(--font-mono); font-size:11px; letter-spacing:0.14em; text-transform:uppercase; color:var(--muted); font-variant-numeric:tabular-nums | GAP | `.hpc-m3-eyebrow` added exactly (note letter-spacing 0.14em — wider than meta's 0.08em). This carries 'ORDFÖRSTÅELSE · FRÅGA 3 AV 10' in M1; progress is textual eyebrow, NO progress bar (DECISION). |
| .m3-display (hero serif headword) | M3.tsx:136-146 | font-family:var(--font-display); font-style:italic; font-weight:400; color:var(--ink); font-size:clamp(44px,6vw,64px); line-height:1.05; letter-spacing:-0.01em; margin:10px 0 0; animation:m3-settle 480ms cubic-bezier(0.22,1,0.36,1) both | WRONG | Live index.css:657-667 matches EXCEPT margin: live=6px 0 0, M3=10px 0 0. FIX live margin-top to 10px to match M3. |
| .m3-stats (home stats row) | M3.tsx:150-156 | display:flex; gap:48px; margin-top:28px; padding-top:20px; border-top:1px solid var(--hairline) | GAP | `.hpc-m3-stats` added exactly. Home composition family — used by M3H. |
| .m3-stat-n (stat numeral) | M3.tsx:158-164 | font-family:var(--font-mono); font-variant-numeric:tabular-nums; font-size:30px; line-height:1.1; color:var(--ink) | GAP | `.hpc-m3-stat-n` added exactly. |
| .m3-stat-l (stat label) | M3.tsx:166-170 | font-size:12px; color:var(--muted); margin-top:2px | GAP | `.hpc-m3-stat-l` added exactly. |
| .m3-stat-d (stat delta) | M3.tsx:172-176 | font-size:12px; color:var(--ok); margin-top:2px | GAP | `.hpc-m3-stat-d` added exactly (uses --ok green for the +0.1 delta). |
| .m3-resume (resume card) | M3.tsx:178-186 | display:flex; align-items:center; justify-content:space-between; gap:16px; background:var(--accent-soft); padding:16px 20px; margin-top:4px | GAP | `.hpc-m3-resume` added exactly (note background:var(--accent-soft) — the soft cobalt wash). |
| .m3-resume-t (resume title) | M3.tsx:188-192 | font-size:15px; font-weight:500; color:var(--ink) | GAP | `.hpc-m3-resume-t` added exactly. |
| .m3-resume-s (resume subtitle) | M3.tsx:194-200 | font-family:var(--font-mono); font-size:12px; color:var(--muted); margin-top:3px; font-variant-numeric:tabular-nums | GAP | `.hpc-m3-resume-s` added exactly. |
| .m3-cta + :hover + :focus-visible (primary button) | M3.tsx:202-218 | font-family:var(--font-ui); font-size:13px; font-weight:600; color:var(--accent-ink); background:var(--accent); padding:10px 20px; white-space:nowrap; transition:opacity 160ms ease. :hover opacity:0.88. :focus-visible outline:2px solid var(--accent); outline-offset:3px | GAP | `.hpc-m3-cta` (+ hover + focus-visible) added exactly. Cobalt --accent fill is a DECISION-permitted use of cobalt (CTA). |
| .m3-plan-item + :last-child (plan row grid) | M3.tsx:220-229 | display:grid; grid-template-columns:32px 1fr auto; gap:16px; align-items:baseline; padding:14px 0; border-bottom:1px solid var(--hairline-2). :last-child border-bottom:0 | GAP | `.hpc-m3-plan-item` (+ :last-child) added exactly. Numbered plan, NOT cobalt-rail kickers (M3H decision). |
| .m3-plan-n (plan ordinal) | M3.tsx:231-238 | font-family:var(--font-display); font-weight:600; font-size:19px; line-height:1.2; color:var(--accent); font-variant-numeric:tabular-nums | GAP | `.hpc-m3-plan-n` added exactly (cobalt --accent ordinal — DECISION-permitted). |
| .m3-plan-t (plan headline) | M3.tsx:240-246 | font-family:var(--font-display); font-weight:500; font-size:17px; line-height:1.35; color:var(--ink) | GAP | `.hpc-m3-plan-t` added exactly. |
| .m3-plan-r (plan rationale) | M3.tsx:248-253 | font-size:13px; color:var(--muted); margin-top:3px; max-width:56ch | GAP | `.hpc-m3-plan-r` added exactly. |
| .m3-plan-min (plan minutes) | M3.tsx:255-261 | font-family:var(--font-mono); font-size:12px; color:var(--muted-2); font-variant-numeric:tabular-nums; white-space:nowrap | GAP | `.hpc-m3-plan-min` added exactly. |
| .m3-tag (inline section chip) | M3.tsx:263-272 | font-family:var(--font-mono); font-size:11px; letter-spacing:0.08em; color:var(--muted); border:1px solid var(--hairline); padding:1px 6px; margin-right:8px; vertical-align:2px | GAP | `.hpc-m3-tag` added exactly (used in plan + trap rows). |
| .m3-trap + :last-child (trap row) | M3.tsx:274-283 | display:flex; align-items:baseline; justify-content:space-between; gap:24px; padding:13px 0; border-bottom:1px solid var(--hairline-2). :last-child border-bottom:0 | GAP | `.hpc-m3-trap` (+ :last-child) added exactly. |
| .m3-trap-t (trap text) | M3.tsx:285-288 | font-size:14.5px; color:var(--ink) | GAP | `.hpc-m3-trap-t` added exactly (note 14.5px). |
| .m3-trap-n (trap count) | M3.tsx:290-296 | font-family:var(--font-mono); font-size:12px; color:var(--muted-2); font-variant-numeric:tabular-nums; white-space:nowrap | GAP | `.hpc-m3-trap-n` added exactly. |
| .m3-tactic (pre-grade tactic aside) | M3.tsx:300-304 | margin-top:24px; padding:14px 18px; border-left:1px solid var(--hairline) | GAP | `.hpc-m3-tactic` added exactly. Used in M1 pre-answer (LOCKED: tactic prominent before answering). |
| .m3-tactic-h (tactic handle label) | M3.tsx:306-312 | font-family:var(--font-mono); font-size:11px; letter-spacing:0.1em; text-transform:uppercase; color:var(--accent) | GAP | `.hpc-m3-tactic-h` added exactly (cobalt --accent tactic handle — DECISION-permitted). |
| .m3-tactic-t (tactic move sentence) | M3.tsx:314-322 | font-family:var(--font-display); font-style:italic; font-size:16px; line-height:1.5; color:var(--ink-2); margin-top:4px; max-width:60ch | GAP | `.hpc-m3-tactic-t` added exactly. |
| .m3-opts + .is-prose variants | M3.tsx:324-327, 659-667 | margin-top:8px; border-top:1px solid var(--hairline). .is-prose .m3-opt{align-items:start} .is-prose .m3-opt-t{font-size:16px; line-height:1.55; max-width:65ch} .is-prose .m3-opt-k{padding-top:17px} | WRONG | Live index.css:735-738 base matches; live .is-prose (835-837) ONLY sets align-items:start and is MISSING the .is-prose .m3-opt-t (16px/1.55/65ch) and .is-prose .m3-opt-k (padding-top:17px) overrides. ADD both to match M3 (LÄS prose option sizing). |
| .m3-opt + states (option row) | M3.tsx:329-347 | display:grid; grid-template-columns:3px 36px 1fr auto; align-items:center; gap:14px; width:100%; border-bottom:1px solid var(--hairline-2); color:var(--ink); transition:background 160ms ease,color 160ms ease. :disabled cursor:default. :focus-visible outline:2px solid var(--accent); outline-offset:-2px | MATCH | Live index.css:739-761 matches (live adds explicit border:0, background:transparent, text-align:left, cursor:pointer to undo UA button chrome since live uses real <button> w/o .m3-reset — keep these; M3 relies on .m3-reset instead). Functionally equivalent; record reset-approach difference in table. |
| .m3-ind + hover/picked/ok/bad indicator | M3.tsx:349-362 | width:3px; align-self:stretch; background:transparent; transform:scaleY(0); transition:transform 160ms ease,background 160ms ease. :hover:not(:disabled) .m3-ind{background:var(--accent); transform:scaleY(1)} | MATCH | Live index.css:762-774 matches. Live ALSO adds `.hpc-m3-opt.is-picked .hpc-m3-ind{background:var(--ink); transform:scaleY(1)}` (775-778) — an is-picked state NOT in M3 (M3 grades immediately, no picked-pending). Keep (live single-select needs it); record as live addition. |
| .m3-opt-k (option key) | M3.tsx:364-369 | font-family:var(--font-mono); font-size:12px; color:var(--muted-2); padding:15px 0 | MATCH | Live index.css:779-786 matches + adds text-transform:lowercase, font-variant-numeric:tabular-nums (M3 lowercases the letter in JSX instead). Equivalent; keep. |
| .m3-opt-t (option text) | M3.tsx:371-376 | font-family:var(--font-display); font-size:18px; font-weight:450; padding:15px 0 | MATCH | Live index.css:787-793 matches + adds min-width:0 (grid overflow guard). Keep. |
| .m3-opt-v (option verdict tag) | M3.tsx:378-384 | font-family:var(--font-mono); font-size:11px; letter-spacing:0.1em; text-transform:uppercase; padding-right:14px | MATCH | Live index.css:794-802 matches + adds color:var(--muted-2), white-space:nowrap (M3 leaves color to state classes; idle .m3-opt-v has no text). Keep. |
| .m3-opt.is-ok / .is-bad / .is-dim (graded states) | M3.tsx:386-404 | is-ok{background:var(--ok-soft)} is-ok .m3-ind{background:var(--ok);scaleY(1)} is-ok t/k/v{color:var(--ok)}. is-bad{background:var(--bad-soft)} is-bad .m3-ind{background:var(--bad);scaleY(1)} is-bad t/k/v{color:var(--bad)} is-bad .m3-opt-t{text-decoration:line-through; text-decoration-thickness:1.5px}. is-dim t/k{color:var(--muted-2)} | MATCH | Live index.css:803-834 matches exactly (all three states + strikethrough thickness 1.5px). |
| .m3-keys (keyboard hint) | M3.tsx:406-412 | font-family:var(--font-mono); font-size:11px; color:var(--muted-2); margin-top:14px; letter-spacing:0.06em | GAP | `.hpc-m3-keys` added exactly. Note: keys hint is desktop-only; phone gets a floating 'Nästa fråga' pill instead (LOCKED, lands in M1). |
| .m3-ped (graded reveal wrapper) | M3.tsx:416-418 | animation:m3-in-y 320ms cubic-bezier(0.22,1,0.36,1) both | GAP | `.hpc-m3-ped` added exactly (wraps the whole post-grade pedagogy block). |
| .m3-verdict | M3.tsx:420-422 | padding:4px 0 0 | MATCH | Live index.css:862-864 matches exactly. |
| .m3-verdict-word + .is-ok/.is-bad | M3.tsx:424-439 | font-family:var(--font-display); font-style:italic; font-weight:600; font-size:clamp(34px,5vw,46px); line-height:1.05; letter-spacing:-0.01em; color:var(--ink); display:inline-block; animation:m3-verdict-in 420ms cubic-bezier(0.22,1,0.36,1) both. .is-ok{color:var(--ok)} .is-bad{color:var(--bad)} | MATCH | Live index.css:865-881 matches exactly. |
| .m3-verdict-sub (verdict sub-sentence) | M3.tsx:441-447 | margin-top:10px; font-size:15px; color:var(--ink-2); max-width:60ch; animation:m3-in-y 320ms cubic-bezier(0.22,1,0.36,1) 140ms both | GAP | `.hpc-m3-verdict-sub` added exactly — explicitly called out in M0 charge item (5). Note the 140ms delay on the in-y animation. |
| .m3-solution (answer lede) | M3.tsx:451-463 | margin-top:22px; padding:18px 0; border-top:1px solid var(--ink); border-bottom:1px solid var(--hairline); font-family:var(--font-display); font-weight:600; font-size:19px; line-height:1.5; color:var(--ink); max-width:75ch; animation:m3-in-y 320ms cubic-bezier(0.22,1,0.36,1) 200ms both | WRONG | Live index.css:886-898 DIVERGES: margin:4px 0 (M3 margin-top:22px), padding:16px 0 (M3 18px 0), font-size:clamp(17px,0.9rem+0.4vw,19px) (M3 fixed 19px), adds letter-spacing:-0.01em (M3 none), and MISSING the m3-in-y 200ms-delay animation. Reconcile to M3's exact fixed values. (Scope note: the brief's explicit M0 edit list is the 5 items; this WRONG row is recorded in the table for M2 if not fixed in M0 — flag to owner.) |
| .m3-step + :last-child (walkthrough step grid) | M3.tsx:465-474 | display:grid; grid-template-columns:36px 1fr; gap:16px; padding:18px 0; border-bottom:1px solid var(--hairline-2); animation:m3-in-y 300ms cubic-bezier(0.22,1,0.36,1) both. :last-child border-bottom:0 | GAP | `.hpc-m3-step` (+ :last-child) added exactly. Used in M2. |
| .m3-step-n (step ordinal) | M3.tsx:476-483 | font-family:var(--font-display); font-weight:600; font-size:21px; line-height:1.2; color:var(--accent); font-variant-numeric:tabular-nums | GAP | `.hpc-m3-step-n` added exactly (cobalt --accent ordinal — DECISION-permitted). |
| .m3-step-h (step title) | M3.tsx:485-492 | font-family:var(--font-display); font-weight:600; font-size:18px; line-height:1.3; color:var(--ink); margin:0 | GAP | `.hpc-m3-step-h` added exactly. |
| .m3-step-tier (essential/detail badge) | M3.tsx:494-503 | font-family:var(--font-mono); font-size:10px; font-weight:400; letter-spacing:0.12em; text-transform:uppercase; color:var(--muted-2); margin-left:10px; vertical-align:3px | GAP | `.hpc-m3-step-tier` added exactly. M3 prints 'kärna'/'detalj' from tier essential/detail (M3.tsx:954) — M2 must map this; live corpus tier field must be checked there. |
| .m3-step-t + .is-pre (step body) | M3.tsx:505-511, 669 | font-size:15px; line-height:1.65; color:var(--ink-2); margin:6px 0 0; max-width:75ch. .is-pre{white-space:pre-line} | GAP | `.hpc-m3-step-t` + `.hpc-m3-step-t.is-pre` added exactly. |
| .m3-dis + :last-child (distractor block) | M3.tsx:513-519 | padding:16px 0; border-bottom:1px solid var(--hairline-2); animation:m3-in-y 300ms cubic-bezier(0.22,1,0.36,1) both. :last-child border-bottom:0 | GAP | `.hpc-m3-dis` (+ :last-child) added exactly. |
| .m3-dis-h + .m3-dis-h s (distractor heading w/ struck option text) | M3.tsx:521-526, 536 | font-family:var(--font-display); font-weight:600; font-size:17px; color:var(--ink). .m3-dis-h s{text-decoration-thickness:1.5px} | GAP | `.hpc-m3-dis-h` + `.hpc-m3-dis-h s` added exactly. LOAD-BEARING (DECISION): M2 must reprint each distractor's struck option text (M3.tsx:973-974 the struck d.text) inside this heading, replacing live letter-only block (PedagogyPanel.tsx ~600) — makes single-column safe. CSS must exist now. |
| .m3-dis-k (distractor key) | M3.tsx:528-534 | font-family:var(--font-mono); font-weight:400; font-size:12px; color:var(--muted-2); margin-right:8px | GAP | `.hpc-m3-dis-k` added exactly (scoped under .m3-dis-h in M3; keep equivalent selector `.hpc-m3-dis-h .hpc-m3-dis-k`). |
| .m3-dis-l (distractor label: lockar/fel) | M3.tsx:538-545 | font-family:var(--font-mono); font-size:11px; letter-spacing:0.12em; text-transform:uppercase; color:var(--accent); margin:8px 0 2px | GAP | `.hpc-m3-dis-l` added exactly (cobalt --accent label 'Varför det lockar'/'Varför det är fel' — DECISION-permitted dis-label). |
| .m3-dis-p (distractor paragraph) | M3.tsx:547-553 | font-size:14.5px; line-height:1.6; color:var(--ink-2); margin:0; max-width:75ch | GAP | `.hpc-m3-dis-p` added exactly (note 14.5px). M2 binds to live snake_case why_tempting/why_wrong — DATA TRAP. |
| .m3-next-row (graded footer) | M3.tsx:555-560 | margin-top:36px; display:flex; align-items:center; gap:16px | GAP | `.hpc-m3-next-row` added exactly (desktop 'Nästa fråga' CTA + 'Enter går vidare' hint; phone replaces with floating pill in M1). |
| .m3-passage-h (passage title) | M3.tsx:564-571 | font-family:var(--font-display); font-weight:600; font-size:21px; line-height:1.3; color:var(--ink); margin:0 0 14px | GAP | `.hpc-m3-passage-h` added exactly (LÄS 'TEXTEN' passage title, M4). |
| .m3-passage p + :last-child (passage body) | M3.tsx:573-583 | font-family:var(--font-display); font-size:16.5px; font-weight:400; line-height:1.7; color:var(--ink-2); margin:0 0 14px; max-width:70ch. :last-child margin-bottom:0 | MATCH | Live index.css:681-693 matches + adds white-space:pre-wrap (689) NOT in M3 (live passages carry literal newlines). Keep; record addition. |
| .m3-q (section prompt) | M3.tsx:585-595 | font-family:var(--font-display); font-weight:500; font-size:clamp(22px,3vw,27px); line-height:1.35; letter-spacing:-0.005em; color:var(--ink); margin:0; max-width:60ch; animation:m3-settle 480ms cubic-bezier(0.22,1,0.36,1) both | MATCH | Live index.css:668-678 matches exactly. |
| .m3-stmt + :last-child (NOG statement grid) | M3.tsx:597-605 | display:grid; grid-template-columns:44px 1fr; gap:14px; padding:15px 0; border-bottom:1px solid var(--hairline-2). :last-child border-bottom:0 | WRONG | Live index.css:696-708 matches base BUT adds `.hpc-m3-stmt:first-child{border-top:1px solid var(--hairline)}` (703-705) NOT in M3 (M3 has no top border on first statement). Decide in table: either drop to match M3, or keep + record as intentional live affordance. Flag to owner; default = drop to match M3. |
| .m3-stmt-n (statement number) | M3.tsx:607-613 | font-family:var(--font-mono); font-size:12px; color:var(--muted-2); font-variant-numeric:tabular-nums; padding-top:4px | MATCH | Live index.css:709-715 matches exactly. |
| .m3-stmt-t (statement text) | M3.tsx:615-622 | font-family:var(--font-display); font-size:17.5px; line-height:1.55; color:var(--ink); margin:0; max-width:65ch | MATCH | Live index.css:716-723 matches exactly. |
| .m3-coda (NOG coda line) | M3.tsx:624-632 | font-family:var(--font-display); font-style:italic; font-size:16px; line-height:1.5; color:var(--ink-2); margin:0 0 10px; max-width:65ch | WRONG | Live index.css:724-732 matches EXCEPT margin: live=0 0 12px, M3=0 0 10px. FIX live to 0 0 10px. |
| .m3-fig + figure-frame strip + img | M3.tsx:634-657 | margin:0; padding:0; background:none; border:none. [data-testid='question-figure']{border/border-radius/background/padding none !important}. img{display:block; width:100%; height:100%; object-fit:contain} | MATCH | Live index.css:840-857 matches exactly (incl. the !important card-frame strip). |
| .m3-missing (missing-explanation note) | M3.tsx:671-680 | font-family:var(--font-display); font-style:italic; font-size:16px; line-height:1.5; color:var(--muted); margin:22px 0 0; max-width:60ch; animation:m3-in-y 320ms cubic-bezier(0.22,1,0.36,1) 200ms both | GAP | `.hpc-m3-missing` added exactly ('Förklaring saknas ännu...' graceful state, M4/section drills). |
| @keyframes m3-in-y | M3.tsx:684-687 | from{opacity:0; transform:translateY(10px)} to{opacity:1; transform:none} | MATCH | Live index.css:638-647 matches (translateY(10px)→translateY(0); 'none' vs 'translateY(0)' equivalent). |
| @keyframes m3-in-x | M3.tsx:689-692 | from{opacity:0; transform:translateX(-10px)} to{opacity:1; transform:none} | WRONG | Live index.css:628-637 uses translateX(-8px); M3 uses translateX(-10px). FIX live `from` to translateX(-10px). |
| @keyframes m3-draw | M3.tsx:694-697 | from{transform:scaleX(0)} to{transform:scaleX(1)} | MATCH | Live index.css:648-655 matches exactly. |
| @keyframes m3-settle | M3.tsx:699-702 | from{opacity:0; letter-spacing:0.01em} to{opacity:1; letter-spacing:-0.01em} | WRONG | Live index.css:900-909 animates transform:translateY(6px)→0; M3 animates LETTER-SPACING 0.01em→-0.01em (no Y). FIX live `m3-settle` to M3's letter-spacing keyframe exactly. |
| @keyframes m3-verdict-in | M3.tsx:704-707 | from{opacity:0; transform:translateX(-8px)} to{opacity:1; transform:none} | WRONG | Live index.css:910-919 slides from translateY(8px); M3 slides from translateX(-8px) (enters from the LEFT where the pick landed). FIX live `m3-verdict-in` to translateX(-8px). |
| @media (prefers-reduced-motion: reduce) | M3.tsx:709-717 | .m3-root *, .m3-root *::before, .m3-root *::after { animation-duration:0.01ms !important; animation-delay:0ms !important; transition-duration:0.01ms !important } | WRONG | Live index.css:920-929 lists only 6 explicit selectors with animation:none (misses opt/ind/cta transitions + future GAP-family animations). REPLACE with M3's star-wildcard scoped under `.hpc-m3-root` (incl. ::before/::after, the three !important duration/delay overrides). Requires `.hpc-m3-root` to exist (see root row). |
| Stale comment block (rail-is-cobalt claim) | n/a (live-only; contradicts M3.tsx:80) | index.css:548 reads 'label is STRUCTURE → cobalt --accent mono' | WRONG | Rewrite the comment (index.css 543-551) to state the rail label is var(--muted) mono per M3; cobalt --accent is reserved for ordinals/tactic-handle/dis-labels/CTA only. Comment must not contradict the code after the meta fix. |
| StudyDesk seam (.hpc-studydesk*) | no M3 equivalent (task-84 2-col drift; LOCKED-reversed) | index.css:931-969 (grid 2-col @1200px container, sticky question col, marginalia border) | WRONG | Record in the table as NON-M3 drift to be REMOVED when M1 lands single-column drill (LOCKED: desktop drill = single column, .m3-frame 880px). M0 does NOT delete it (no markup change), but the table must flag it as scheduled-for-removal so it is not mistaken for M3. |

**Data traps.**
- camelCase (M3/fixtures) vs snake_case (live corpus) is SYSTEMIC: M3.tsx reads whyTempting/whyWrong/pregradeTactic (M3.tsx:977/979/887); live corpus + live PedagogyPanel.tsx use why_tempting/why_wrong/pregrade_tactic (PedagogyPanel.tsx:309/611/622). M0 copies M3's CSS px/clamp/token VALUES only — it touches NO field accesses, so this trap does not bite M0, but the class-map doc must warn M1/M2 in bold.
- figureMeta is fixture-only: fixturesSections.ts:48 uses `figureMeta:{src,aspect_ratio,kind}`; M3.tsx:1052-1055 reads question.figureMeta; the LIVE corpus field is `figure` (not figureMeta), per the brief and the M0 charge. M4 must map figure→figureMeta. Irrelevant to M0 CSS but documented in the map.
- M3 fixtures provide sectionLabel/total/contextTitle (fixtures.ts:13/15, fixturesSections.ts:34/36/41); the LIVE corpus has NO sectionLabel/total/contextTitle. The eyebrow ('ORDFÖRSTÅELSE · FRÅGA n AV total') in M1/M4 must derive these live (e.g. via sectionRailLabel + session length) — flagged in the map for M1.
- M3 step tier values are essential/detail (M3.tsx:954) rendered as kärna/detalj. Live corpus tier field name/values must be confirmed in M2 before binding .hpc-m3-step-tier.
- Token names already match 1:1 between M3 and live (index.css:29-54 defines --bg/--ink/--ink-2/--muted/--muted-2/--hairline/--hairline-2/--accent/--accent-ink/--accent-soft/--ok/--ok-soft/--bad/--bad-soft/--font-ui/--font-display/--font-mono). So porting M3 CSS needs NO token renaming — copy var() references verbatim. This is the one place NOT to over-think.

**Test contracts.**
- M0 changes no markup, so no data-testid is added/removed. Existing testids on M3-consuming surfaces must remain green: DrillRailSection (data-testid passthrough, DrillRailSection.tsx:39), DailyPlanCard, TopTrapsCard, DiagnosticReport. Run `pnpm test` + `pnpm test:e2e` to confirm the meta color change + additive CSS broke nothing.
- The `.hpc-m3-meta` color change from --accent to --muted is a VISUAL change on EVERY live surface currently using .hpc-m3-meta (drill rail, DailyPlanCard:107, TopTrapsCard:85, DiagnosticReport:295). Any e2e/visual-regression snapshot that asserts the rail label color (cobalt) will need re-baselining. Grep tests for hardcoded accent/oklch rail assertions before merge.
- No new keyframe NAMES are introduced (all 5 already exist live); only 3 keyframe VALUES change (in-x, settle, verdict-in). Any test asserting transform direction/letter-spacing on .hpc-m3-display/.hpc-m3-verdict-word animations would need updating — unlikely but grep for it.
- Adding GAP-family classes is purely additive (new selectors, no overrides of existing ones except the documented WRONG fixes). It cannot break existing component styling unless a live component already (mis)uses a name like .hpc-m3-cta — confirmed none do (only the 22 live classes are consumed). Verify with a grep for each new class name in app/src before merge.
- docs/m3-class-map.md is a doc artifact, not under test; but its existence + completeness IS the M0 gate. CI lint (markdown) if present must pass.

**Decisions applied.**
- Desktop drill = SINGLE COLUMN, .m3-frame max-width 880px (M3.tsx:57) — M0 adds .hpc-m3-frame at 880px and flags the .hpc-studydesk 2-col seam (index.css:931-969) as non-M3 drift scheduled for removal in M1. REVERSES tasks 80–87.
- M3 reserves cobalt --accent for ordinals (plan-n M3.tsx:236, step-n M3.tsx:481), tactic-handle (tactic-h M3.tsx:311), dis-labels (dis-l M3.tsx:543), and CTA (cta M3.tsx:207) ONLY. Everything else, including the rail meta, is --muted (M3.tsx:80). M0 fixes the rail meta color + the stale comment to enforce this.
- Progress = textual eyebrow ('FRÅGA n AV total'), NO visual progress bar — .hpc-m3-eyebrow is added as the only progress affordance (M3.tsx:127-134, 882).
- M2's struck distractor option text (DECISION: load-bearing for single-column) needs .hpc-m3-dis-h + .hpc-m3-dis-h s (strikethrough 1.5px, M3.tsx:521-536) — M0 lands the CSS so M2 has it ready.
- Phone drill = linear + bare + floating bottom-center 'Nästa fråga' pill (M1 territory). M0 keeps the live 600px linearisation (index.css:611-626) since it serves the phone-linear decision; the floating pill + visible touch exit are M1 markup, not M0 CSS.
- ⌘K is power-user only; nav/settings visible without it (M5/chrome territory). No M0 CSS impact, noted so the map doesn't add masthead-fixture styles.

**Definition of done.**
- docs/m3-class-map.md exists and lists EVERY one of the 64 .m3-* selectors + 5 keyframes from M3.tsx (M3.tsx:33-723) with its live .hpc-m3-* status (MATCH / GAP / WRONG) and the exact M3 value — table complete, zero selectors unaccounted.
- Every GAP family is filled into the .hpc-m3-* layer (index.css 553-929) with M3's EXACT values copied verbatim (px/clamp/letter-spacing/token/animation): root, reset, frame, h, mono, eyebrow, stats, stat-n/l/d, resume(+t/s), cta(+hover+focus), plan-item(+last)/n/t/r/min, tag, trap(+last)/t/n, tactic(+h+t), keys, ped, step(+last)/n/h/tier/t(+is-pre), dis(+last)/h(+s)/k/l/p, next-row, passage-h, missing, plus .is-prose opt-t/opt-k overrides.
- The 4 documented WRONG fixes are applied: .hpc-m3-meta color --accent→--muted (index.css:568); .hpc-m3-display margin-top 6px→10px (665); the stale comment (543-551) rewritten; the 3 keyframes (in-x -8→-10px:628-637, settle Y→letter-spacing:900-909, verdict-in Y→X -8px:910-919). Solution + coda + stmt:first-child + is-prose divergences either fixed or explicitly flagged-to-owner in the table.
- .hpc-m3-verdict-sub added exactly (M3.tsx:441-447) including the 140ms in-y delay.
- Reduced-motion block (index.css:920-929) replaced with M3's star-wildcard scoped under .hpc-m3-root (M3.tsx:709-717) incl. ::before/::after + the three !important duration/delay overrides; .hpc-m3-root exists so the wildcard has a scope.
- ANTI-DRIFT GATE: M3 reference URL+state cited (/redesign-l12?dev=1&v=3); live + M3 screenshots captured at 1440 AND 402, light AND dark, confirming no M3-consuming surface (DrillRailSection/DailyPlanCard/TopTrapsCard/DiagnosticReport) regressed from the meta-color + additive fill; every selector matched in the table; exact values copied (not approximated); all data-testids intact; `pnpm test` AND `pnpm test:e2e` green; owner approves the visual before merge — NO auto-merge.
- Grep confirms none of the new .hpc-m3-* class names collided with a pre-existing live selector (additive-only safety check).

**Dependencies.**
- Hard prerequisite for M1 (drill pre-answer: needs .hpc-m3-eyebrow/.hpc-m3-tactic/.hpc-m3-keys/.hpc-m3-frame), M2 (graded: needs .hpc-m3-ped/.hpc-m3-verdict-sub/.hpc-m3-step*/.hpc-m3-dis*/.hpc-m3-next-row), M3H (home: needs .hpc-m3-stats/.hpc-m3-stat-*/.hpc-m3-resume*/.hpc-m3-plan-*/.hpc-m3-trap*/.hpc-m3-cta/.hpc-m3-tag/.hpc-m3-h), M4 (sections: needs .hpc-m3-passage-h/.hpc-m3-missing + the .is-prose opt overrides). M0 must land first — 'No screen work until this is done' (docs/m3-faithful-rebuild-spec.md M0).
- Reference is READ-ONLY: app/src/components/devbake/l12/M3.tsx (CSS const 33-723) and devbake/redesign/fixtures.ts + fixturesSections.ts. Do not edit these.
- Depends on the live token layer being intact (index.css:29-54) — all ported var() references resolve against it. No token changes in M0.
- Blocks nothing in the data/corpus pipeline; purely a frontend CSS-layer + doc task. No worker/D1/migration involvement.

## M1 — M1 — Drill pre-answer (idle) faithful rebuild to M3 DrillScreen idle

**Objective.** Make the live pre-answer (idle) drill — the state before the user picks an answer — render pixel-faithfully against M3.tsx DrillScreen idle (/redesign-l12?dev=1&v=3, ORD). Concretely: (1) reverse the desktop layout from the 2-col BoksidanDesk/StudyDesk back to SINGLE-COLUMN DrillQuestion in an .m3-frame (max-width 880px); (2) add the textual EYEBROW ({LONG_LABEL} · FRÅGA n AV total) as a sibling of the prompt, killing the DrillProgress visual bar; (3) add the pre-grade TACTIC ASIDE (cobalt "Taktik · {handle}" + serif-italic move) reading explanation.pregrade_tactic (snake_case) via a new memoized usePregradeTactic(qid) hook threaded as a prop; (4) add the keys hint ("Tangenter a–{lastKey} väljer · klick fungerar också"); (5) thread position/total as NEW props from SessionPlayer + StudyDesk into DrillQuestion to feed the eyebrow; (6) add a NEW section→long-label map (ORD→"Ordförståelse"); (7) give the focused PHONE drill a VISIBLE touch exit (back/close to Home) since tabs are hidden; (8) replace the phone full-width sticky button with a FLOATING bottom-center "Nästa fråga" pill. Copy M3's CSS px/clamp/letter-spacing/tokens and Swedish copy verbatim; never copy M3's camelCase field accesses (live data is snake_case).

**Reference.** /redesign-l12?dev=1&v=3 — DrillScreen idle (ORD), the pre-answer state with picked === null (M3.tsx DrillScreen, picked state initialized null at M3.tsx:849; idle branch graded===false renders the .m3-tactic aside at M3.tsx:885-890 and the .m3-keys hint at M3.tsx:924-926). Reference content from fixtures.ts: QUESTION (qid var-2026-verb1-ORD-003, section ORD, sectionLabel "Ordförståelse", number 3, total 10, prompt "eftertrakta", 5 options A–E) at fixtures.ts:10-25; EXPLANATION.pregradeTactic { handle "Stamordskartan", move "När ordet börjar på efter- — …" } at fixtures.ts:28-31.

**Files to edit.**
- /home/loucmane/dev/hpfetcher/app/src/components/drill/DrillQuestion.tsx — add position/total props + eyebrow sibling + pre-grade tactic aside + keys hint; consume a NEW usePregradeTactic result passed as prop
- /home/loucmane/dev/hpfetcher/app/src/components/session/SessionPlayer.tsx — reverse desktop to single-column DrillQuestion in .m3-frame (was BoksidanDesk ~622-639); thread position={index+1} total={plan.length} (~633-635/662) into DrillQuestion; drop DrillProgress visual bar (~662); add VISIBLE phone touch exit (close→Home, onHome already exists ~379); replace full-width sticky Btn with floating bottom-center 'Nästa fråga' pill on phone (~704-733)
- /home/loucmane/dev/hpfetcher/app/src/lib/sectionLongLabel.ts — NEW file: section→long-label map (ORD→'Ordförståelse', …). NOT sectionRailLabel.ts (that is rail chrome).
- /home/loucmane/dev/hpfetcher/app/src/lib/usePregradeTactic.ts — NEW file: memoized hook wrapping loadExplanation(qid), returns { handle, move } | null from explanation.pregrade_tactic (snake_case)
- /home/loucmane/dev/hpfetcher/app/src/components/StudyDesk.tsx — if retained on any path, thread position/total through to DrillQuestion; per LOCKED DECISION desktop no longer uses StudyDesk for drill so the SessionPlayer dispatch must stop routing here for active drill
- /home/loucmane/dev/hpfetcher/app/src/index.css — consume .hpc-m3-eyebrow / .hpc-m3-tactic* / .hpc-m3-keys (added in M0); if M0 has not landed, add them here mirroring M3.tsx:127-134, 300-322, 406-412
- /home/loucmane/dev/hpfetcher/app/src/components/drill-variants/DrillVariantShell.test.tsx — guard does not need editing but the eyebrow guard against missing position/total must keep its render test green

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| Frame / single-column container (.m3-frame) | M3.tsx:872 (DrillScreen returns <div className="m3-frame">); CSS M3.tsx:56-60 | max-width: 880px; margin: 0 auto; padding: 56px 24px 96px. Responsive @media(max-width:900px): padding 40px 18px 72px (M3.tsx:721). Live equivalent class is .hpc-m3-frame or wrap DrillQuestion in a 880px max-width centered column. | WRONG | At 1440px the active drill is a SINGLE centered column ≤880px wide (NOT the 2-col BoksidanDesk); SessionPlayer no longer routes active drill to BoksidanDesk/StudyDesk. Screenshot at 1440 shows one column. |
| Margin-rail meta (.m3-meta: ORD bold over '3 / 10') | M3.tsx:875-878 (<div className="m3-meta"><strong>{QUESTION.section}</strong>{QUESTION.number} / {QUESTION.total}</div>); CSS M3.tsx:75-91 | font-family var(--font-mono); font-size 11px; letter-spacing 0.08em; text-transform uppercase; color var(--muted); text-align right; padding-top 5px; font-variant-numeric tabular-nums. strong: display block; color var(--ink-2); font-weight 500. Content: bold section over 'number / total'. NOTE M3 reads QUESTION.number/QUESTION.total — LIVE has NO number/total field; feed from NEW position/total props (index+1 / plan.length). | GAP | Rail shows ORD in --ink-2 bold over '3 / 10' (or current/total) in --muted mono, right-aligned. DrillRailSection meta already exists; ensure it shows position/total, not a static label. |
| Vertical hairline spine (.m3-spine) | M3.tsx:879 (<div className="m3-spine" />); CSS M3.tsx:70-73 | background var(--hairline); align-self stretch; grid column width 1px (M3.tsx:66 grid-template-columns 128px 1px 1fr, column-gap 28px; @900px 96px 1px 1fr gap 20px M3.tsx:720). | MATCH | A 1px hairline divides rail from content at 128px (96px ≤900px). DrillRailSection already renders the spine; verify column widths match. |
| EYEBROW (.m3-eyebrow: '{LONG_LABEL} · FRÅGA n AV total') | M3.tsx:881-883 (<div className="m3-eyebrow">{QUESTION.sectionLabel.toUpperCase()} · FRÅGA {QUESTION.number} AV {QUESTION.total}</div>); CSS M3.tsx:127-134 | font-family var(--font-mono); font-size 11px; letter-spacing 0.14em; text-transform uppercase; color var(--muted); font-variant-numeric tabular-nums. Text: 'ORDFÖRSTÅELSE · FRÅGA 3 AV 10'. TRAP: M3 reads QUESTION.sectionLabel (camelCase, 'Ordförståelse') — LIVE has NO sectionLabel field; build long label from a NEW sectionLongLabel map (ORD→'Ordförståelse'). FRÅGA n AV total uses NEW position/total props. Word is 'FRÅGA' + 'AV' (uppercase via CSS, source title-case 'Ordförståelse'). | GAP | Eyebrow renders above the headword as '{LONG-LABEL} · FRÅGA n AV total' in 11px mono 0.14em --muted. It is a SIBLING of drill-prompt (NOT inside it). Live currently has no eyebrow — only DrillProgress bar. |
| Display headword (.m3-display: 'eftertrakta') | M3.tsx:884 (<h1 className="m3-display">{QUESTION.prompt}</h1>); CSS M3.tsx:136-146 | font-family var(--font-display); font-style italic; font-weight 400; color var(--ink); font-size clamp(44px, 6vw, 64px); line-height 1.05; letter-spacing -0.01em; margin 10px 0 0 (live .hpc-m3-display uses margin 6px 0 0 — M0 reconciles; M3 truth is 10px). Live class .hpc-m3-display matches except margin. | MATCH | Headword renders in 44–64px italic display serif; data-testid="drill-prompt" stays on THIS element (DrillQuestion.tsx:156). Margin matches M3 (10px top) after M0. |
| PRE-GRADE TACTIC ASIDE (.m3-tactic: cobalt 'Taktik · {handle}' + serif-italic move) | M3.tsx:885-890 (idle-only: {!graded ? <aside className="m3-tactic"><p className="m3-tactic-h">Taktik · {EXPLANATION.pregradeTactic.handle}</p><p className="m3-tactic-t">{EXPLANATION.pregradeTactic.move}</p></aside> : null}); CSS M3.tsx:300-322 | .m3-tactic: margin-top 24px; padding 14px 18px; border-left 1px solid var(--hairline). .m3-tactic-h: font-mono; 11px; letter-spacing 0.1em; uppercase; color var(--accent) [cobalt]. .m3-tactic-t: font-display; italic; 16px; line-height 1.5; color var(--ink-2); margin-top 4px; max-width 60ch. Header text literal: 'Taktik · ' + handle. TRAP: M3 reads EXPLANATION.pregradeTactic.handle/.move (camelCase) — LIVE corpus is explanation.pregrade_tactic.handle/.move (snake_case, explanations.ts:101-103). Read via NEW usePregradeTactic(qid). Shown ONLY when !graded. | GAP | Before grading, an aside shows 'Taktik · Stamordskartan' in cobalt mono + the serif-italic move below. Reads pregrade_tactic (snake_case). Hidden once graded. PreGradeFill (StyleA.tsx:478, off live path) is NOT reused. This is a NEW pre-grade surface in DrillQuestion. |
| Options block (.m3-opts a–e) | M3.tsx:895-923 (<div className="m3-opts"> mapping QUESTION.options to .m3-opt buttons with .m3-ind / .m3-opt-k / .m3-opt-t / .m3-opt-v); CSS M3.tsx:324-404 | .m3-opts: margin-top 8px; border-top 1px solid var(--hairline). .m3-opt: grid 3px 36px 1fr auto; gap 14px; border-bottom 1px solid var(--hairline-2). .m3-ind 3px hover→var(--accent). .m3-opt-k mono 12px var(--muted-2) padding 15px 0 (lowercase letter). .m3-opt-t font-display 18px weight 450 padding 15px 0. .m3-opt-v mono 11px 0.1em uppercase. Idle: NO is-ok/is-bad/is-dim, NO verdict caption. Live .hpc-m3-opt + OptionRow already match (DrillQuestion.tsx:238-269, index.css:739+). | MATCH | Five option rows a–e, hover shows cobalt left indicator, no grading colors pre-pick. option-{letter} testids present. Matches existing OptionRow. |
| Keys hint (.m3-keys: 'Tangenter a–{lastKey} väljer · klick fungerar också') | M3.tsx:924-926 (idle-only: {!graded ? <div className="m3-keys">Tangenter a–e väljer · klick fungerar också</div> : null}). SectionDrillScreen uses dynamic lastKey: M3.tsx:1118 'Tangenter a–{lastKey} väljer · klick fungerar också'. CSS M3.tsx:406-412 | font-mono; 11px; color var(--muted-2); margin-top 14px; letter-spacing 0.06em. Copy literal: 'Tangenter a–{lastKey} väljer · klick fungerar också' where lastKey = last option letter lowercased (a–e for ORD's 5 opts; dynamic per question.options.length). En-dash '–' (U+2013), middle dot '·' (U+00B7). Shown ONLY when !graded. | GAP | Below the options when not graded: 'Tangenter a–e väljer · klick fungerar också' in 11px mono. lastKey derived from question.options length. Hidden once graded. |
| Section meta label 'Välj synonym' / 'Välj svar' rail label for options row | M3.tsx:895 (<Section meta={<>Välj synonym</>} …>). Live uses RAIL_CHOOSE = 'Välj svar' (sectionRailLabel.ts:25, DrillQuestion.tsx:183) | M3 ORD says 'Välj synonym'; live uses generic 'Välj svar'. M3 SectionDrillScreen also uses 'Välj svar' (M3.tsx generic). Keep live RAIL_CHOOSE 'Välj svar' — it is the cross-section generic and M3's section screens agree; the ORD-only 'Välj synonym' is M3 baseline flourish. DECISION: out of scope cosmetic; flag but do not change unless owner wants ORD-specific label. | MATCH | Options row carries a cobalt mono rail label. Note divergence (ORD baseline 'Välj synonym' vs live 'Välj svar') in PR for owner; default keep 'Välj svar'. |
| Keyboard a–e select handler (idle) | M3.tsx:853-869 (onKey: if !graded and KEY_TO_LETTER[key] → setPicked; KEY_TO_LETTER M3.tsx:23-29). Live: a/b/c/d/e auto-advance from task 98 (#98) | Pressing a–e picks the matching option when not graded; modifier keys (meta/ctrl/alt) ignored. Live already wires letter keys (task 98). No new px values. | MATCH | Pressing 'a'..'e' selects option pre-grade; verify it still works after single-column reversal (handler may live in SessionPlayer or DispatchedVariant — confirm it survives the BoksidanDesk removal). |
| Desktop minimal-mast chrome (one hairline top band: brand + nav + picker corner) | LOCKED DECISION (no M3 mast in DrillScreen itself; M3 frame is bare). M3.tsx DrillScreen has NO masthead — chrome is owner-specified minimal mast. Live mast pieces: BrandMark/DesktopNav (#83), Page runningHead (SessionPlayer.tsx:742) | One quiet hairline top band: brand '⌜ HP-Coach' + visible nav (Hem/Övning/Lektion/Framsteg) + a quiet picker corner. NO section echo (no 'ORD · 3/10' in mast — the rail already carries it), NO bottom status line, NO exam-tag. Remove Page's runningHead ['HP · Coach', q.section] echo + status line for active drill (SessionPlayer.tsx:742-749). | WRONG | Desktop active drill shows ONE hairline mast with brand + 4 nav items + picker corner; NO section echo, NO bottom status line, NO progress bar. (Full chrome implementation may span M0/M6 — M1 at minimum removes the doubled runningHead/status for single-column drill.) |
| Phone: NO running head (bare linear) | LOCKED DECISION (phone drill = linear + bare). M3 frame responsive M3.tsx:719-722. Live phone renders DrillQuestion in <MobileFrame tabs={false}> + Page runningHead (SessionPlayer.tsx:740-752) | Phone drops the running head entirely; content is linear DrillQuestion. Keep .m3-frame responsive padding 40px 18px 72px ≤900px (M3.tsx:721). | WRONG | At 402px the active drill has NO running head band; just eyebrow → headword → tactic → options → keys, linear. |
| Phone: VISIBLE touch exit (back/close to Home) | No M3 equivalent (M3 is desktop mock); LOCKED DECISION — focused phone drill must gain a visible touch exit (back/close); Esc/⌘K don't count. onHome exists SessionPlayer.tsx:379 | A visible tappable close/back control (e.g. top-left ‹ or ✕, aria-label 'Tillbaka' or 'Avsluta övning') that calls onHome (navigate to '/'). Min 44×44px touch target. Reachable without scrolling. Today tabs={false} hides BottomTabs and there is NO exit. | GAP | At 402px a visible back/close control is present and tappable; clicking returns to Home. data-testid e.g. 'drill-exit'. Esc still works but is NOT the only exit. |
| Phone: FLOATING bottom-center 'Nästa fråga' pill | M3.tsx:986-988 (m3-cta 'Nästa fråga') is desktop graded-only; LOCKED DECISION — phone drill gets a FLOATING bottom-center 'Nästa fråga' pill reachable without scrolling. Live phone CTA is a full-width sticky Btn 'Nästa →' (SessionPlayer.tsx:704-733) | Floating, bottom-CENTER pill (not full-width bar), reachable without scrolling. Copy 'Nästa fråga' (M3.tsx:987) — replaces live 'Nästa →' / 'Avsluta →'. Pre-answer (idle) the Next is disabled (phase !== 'graded'); pill appears/enables after grading. Keep data-testid='drill-next'. | WRONG | At 402px the advance control is a floating bottom-center pill labeled 'Nästa fråga' (last question 'Avsluta'); disabled pre-grade; reachable without scroll. Replaces the full-width sticky bar. |
| NEW prop threading: position + total into DrillQuestion | M3 reads QUESTION.number/QUESTION.total (fixtures.ts:15-16); LIVE Question type has neither. SessionPlayer computes index+1 / plan.length (SessionPlayer.tsx:633-635, 662, 743, 747) | Add props position?: number; total?: number to DrillQuestion. SessionPlayer passes position={index+1} total={plan.length} on BOTH phone and (new single-column) desktop paths. StudyDesk.tsx must forward them if still used anywhere. Eyebrow consumes them; rail meta '3 / 10' consumes them. | GAP | DrillQuestion receives position/total; eyebrow + rail meta render the live count (e.g. '3 / 10', 'FRÅGA 3 AV 10'). Props are optional with a guard. |
| NEW section→long-label map (ORD→'Ordförståelse') | M3 reads QUESTION.sectionLabel 'Ordförståelse' (fixtures.ts:13); LIVE has no sectionLabel. sectionRailLabel.ts (rail chrome) is explicitly NOT this map. | NEW file src/lib/sectionLongLabel.ts: Record<Section,string> with ORD→'Ordförståelse'; provide full-name labels for ORD/LÄS/MEK/ELF/XYZ/KVA/NOG/DTK (ORD 'Ordförståelse', LÄS 'Läsförståelse', MEK 'Meningskomplettering', ELF 'English Reading Comprehension' or 'Engelsk läsförståelse' — confirm w/ owner since ELF content is English, XYZ 'Matematisk problemlösning', KVA 'Kvantitativa jämförelser', NOG 'Kvantitativa resonemang' / 'Tillräcklig information', DTK 'Diagram, tabeller och kartor'). Eyebrow uppercases via CSS. | GAP | Eyebrow long label resolves from the NEW map; ORD shows 'ORDFÖRSTÅELSE'. Map lives in its own file, NOT in sectionRailLabel.ts. Final Swedish wording confirmed with owner (open question). |
| NEW memoized usePregradeTactic(qid) hook | M3 reads EXPLANATION.pregradeTactic synchronously from fixtures (fixtures.ts:28-31); LIVE must async-load + memoize via loadExplanation(qid) (explanations.ts:163, already per-exam cached). | NEW src/lib/usePregradeTactic.ts: hook that calls loadExplanation(qid), returns { handle, move } \| null from explanation.pregrade_tactic (snake_case, explanations.ts:101-103). Memoized on qid; tolerant of null (no tactic → aside hidden). Passed into DrillQuestion as a prop (per charge) OR called inside; charge says 'passed as a prop' — DrillQuestion takes pregradeTactic?: {handle,move}\|null prop, the hook lives at the SessionPlayer/parent boundary. | GAP | Tactic aside content comes from the live corpus pregrade_tactic (snake_case) via the memoized hook; renders 'Stamordskartan' for var-2026-verb1-ORD-003; absent gracefully when the corpus has no tactic. |
| DrillProgress visual bar — REMOVE | No M3 equivalent (M3 idle has NO progress bar; LOCKED DECISION — progress is textual eyebrow only, NO visual bar). Live DrillProgress is a 2px --accent ruler bar (DrillProgress.tsx:46-62), rendered at SessionPlayer.tsx:662 | Delete the <DrillProgress …> render for active drill (SessionPlayer.tsx:661-663). Its 'ORD · Övning' + '03 / 10' + 2px ruler bar are all replaced: section/count by the eyebrow + rail meta, the bar by nothing. | WRONG | No visual progress bar anywhere in the active drill at 1440 or 402; progress is conveyed only by the textual eyebrow + rail meta. |
| data-testid integrity (drill-prompt on PROMPT only; eyebrow a sibling) | Live: drill-prompt on the headword h1 (DrillQuestion.tsx:156); answer-resolver reads its textContent (mistakes.spec.ts:62-72, drill.spec.ts:65) | Eyebrow text ('ORDFÖRSTÅELSE · FRÅGA 3 AV 10') must NOT be inside the drill-prompt element — it would pollute the textContent the resolver matches against window.__HPC_BANK__ prompts. Keep eyebrow + tactic + keys as SIBLINGS of drill-prompt. | MATCH | page.getByTestId('drill-prompt').textContent() returns ONLY the headword/stem (e.g. 'eftertrakta'); the answer resolver still matches the bank. Verified against mistakes.spec.ts + drill.spec.ts. |

**Data traps.**
- camelCase vs snake_case (SYSTEMIC): M3 reads EXPLANATION.pregradeTactic.handle/.move (M3.tsx:887-888); LIVE corpus is explanation.pregrade_tactic.handle/.move (explanations.ts:101-103). Copy M3's CSS, NOT its field path. Reading pregradeTactic on live data = undefined = silent missing tactic.
- Missing field sectionLabel: M3 reads QUESTION.sectionLabel ('Ordförståelse', fixtures.ts:13); LIVE Question has NO sectionLabel. Must build a NEW sectionLongLabel map — do NOT add sectionLabel to the corpus and do NOT reuse sectionRailLabel.ts (that map is rail chrome promptLabel/contextLabel only).
- Missing fields number/total: M3 reads QUESTION.number/QUESTION.total (fixtures.ts:15-16); LIVE Question has neither. Eyebrow + rail '3 / 10' must come from NEW position/total props (index+1 / plan.length), which are NOT currently threaded into DrillQuestion.
- figure vs figureMeta: live Question uses question.figure (DrillQuestion.tsx:138), NOT figureMeta. Not directly in M1's ORD path but applies to any section apparatus copied later — do not introduce figureMeta.
- DrillScreen uses fixtures' synchronous QUESTION/EXPLANATION; live must async-load the explanation (loadExplanation is async, returns null for un-backfilled qids). The tactic aside MUST render gracefully (hidden) when the tactic is null/loading — never crash or show 'undefined'.
- M3 'Välj synonym' (M3.tsx:895) is ORD-specific baseline flourish; live RAIL_CHOOSE is generic 'Välj svar'. Don't blindly copy 'Välj synonym' across sections — flag for owner.
- M3 .m3-display margin is 10px 0 0 (M3.tsx:144) but live .hpc-m3-display is 6px 0 0 (index.css:665). M0 owns reconciliation; if M0 hasn't landed, do not silently keep 6px — match M3's 10px or coordinate with M0.

**Test contracts.**
- DrillVariantShell.test.tsx (2 cases, lines 17-33): renders missing-qid recovery + resolvable variant. Eyebrow MUST guard against missing position/total so DrillQuestion renders without crashing when position/total are undefined (the shell test path may not thread them). Acceptance: both cases stay green; eyebrow renders 'FRÅGA — AV —' or hides cleanly rather than throwing on undefined.
- mistakes.spec.ts:62-72 readPromptCorrectLetter: reads getByTestId('drill-prompt').textContent() and matches it against window.__HPC_BANK__ prompt strings. Eyebrow/tactic/keys MUST be siblings of drill-prompt — if eyebrow text leaks into drill-prompt, the bank lookup fails. Acceptance: prompt textContent === bare headword.
- drill.spec.ts:65 — same drill-prompt textContent answer-resolution pattern; same sibling guarantee. Acceptance: e2e drill flow resolves answers.
- mistakes.spec.ts startSessionAndAwaitQ1 (lines 93-125) waits on getByTestId('drill-start') then drill-prompt then drill-next. Renaming/moving drill-next (phone pill) MUST keep data-testid='drill-next'. Acceptance: e2e still finds drill-next.
- Phone exit affordance needs a stable testid (e.g. data-testid='drill-exit') so a future e2e can assert the visible touch exit exists; add it now even if no test references it yet.
- Any responsive Playwright spec (3-viewport, tasks 65/75/106) that asserted the 2-col StudyDesk at desktop MUST be updated to expect single-column at 1440 — search tests-e2e for StudyDesk/2-col assertions before merge.
- pnpm test (vitest) + pnpm test:e2e (playwright) both green at 1440 and 402.

**Decisions applied.**
- Desktop drill = SINGLE COLUMN in .m3-frame (max-width 880px) — REVERSES tasks 80–87's BoksidanDesk 2-col. SessionPlayer.tsx:622-639 must stop routing active drill to BoksidanDesk.
- Phone drill = linear + bare (no running head) + FLOATING bottom-center 'Nästa fråga' pill reachable without scrolling — replaces full-width sticky Btn (SessionPlayer.tsx:704-733) and drops Page runningHead (SessionPlayer.tsx:742).
- Desktop chrome = MINIMAL MAST: one hairline band with brand (⌜ HP-Coach) + visible nav (Hem/Övning/Lektion/Framsteg) + quiet picker corner; NO section echo, NO bottom status line, NO exam-tag. Remove the doubled runningHead/status for active drill.
- M2's distractor reprint is NOT this task, but M1 must NOT regress it — out of scope here.
- Focused PHONE drill MUST gain a VISIBLE touch exit (back/close to Home); Esc/⌘K do not count. New in M1 (SessionPlayer phone path, tabs={false} hides BottomTabs).
- ⌘K is a power-user accelerator, NOT primary nav; navigation + settings visible without it. Remove the ⌘K status hint from the active-drill chrome (SessionPlayer.tsx:748 hints ['⌘k palett']) per minimal-mast.
- Progress = textual eyebrow ('FRÅGA n AV total'); NO visual progress bar — delete DrillProgress for active drill (SessionPlayer.tsx:661-663).
- camelCase vs snake_case is SYSTEMIC: copy M3's CSS/px values, read live snake_case fields (pregrade_tactic, why_tempting, why_wrong) — never M3's field accesses.

**Definition of done.**
- ANTI-DRIFT GATE: M3 reference URL+state cited (/redesign-l12?dev=1&v=3, DrillScreen idle) and a live screenshot diffed against an M3 screenshot at BOTH 1440px AND 402px in BOTH light AND dark — four live + four M3 captures attached to the PR.
- Every elementChecklist row reconciled: GAP/WRONG rows implemented to MATCH; MATCH rows verified unchanged. Exact px/clamp/letter-spacing/token/Swedish copy copied verbatim from the cited M3 source.
- Desktop active drill is single-column ≤880px (BoksidanDesk no longer in the active-drill path); phone drill is bare-linear with a visible touch exit + a floating bottom-center 'Nästa fråga' pill.
- Eyebrow ('{LONG-LABEL} · FRÅGA n AV total'), pre-grade tactic aside (cobalt 'Taktik · {handle}' + serif-italic move from snake_case pregrade_tactic via usePregradeTactic), and keys hint ('Tangenter a–{lastKey} väljer · klick fungerar också') all render pre-grade and hide on grade.
- NEW files created: src/lib/sectionLongLabel.ts and src/lib/usePregradeTactic.ts; position/total threaded as new optional props into DrillQuestion from SessionPlayer (and StudyDesk if retained).
- No visual progress bar anywhere; DrillProgress removed from active drill.
- data-testid='drill-prompt' carries ONLY the headword/stem (eyebrow/tactic/keys are siblings); data-testid='drill-next' preserved on the phone pill; new data-testid='drill-exit' on the phone touch exit.
- Eyebrow guarded against missing position/total (no crash); DrillVariantShell.test.tsx stays green.
- All data-testids present; pnpm test green; pnpm test:e2e green at 1440 and 402 (responsive specs updated to expect single-column desktop).
- Owner approves the visual before merge — NO auto-merge label on this PR.
- Branch off main (or the M-series base), merge promptly once CI is green and owner approves.

**Dependencies.**
- M0 (#152) — Foundation: adds/repairs .hpc-m3-eyebrow / .hpc-m3-tactic* / .hpc-m3-keys CSS (currently absent from index.css) and the .m3-display margin reconciliation. M1 consumes these classes; if M0 has not merged, M1 must add the eyebrow/tactic/keys CSS itself mirroring M3.tsx:127-134/300-322/406-412 and coordinate to avoid a later conflict.
- MD (#151) — Layout decision bake-off is the parent decision that locked single-column desktop; M1 implements that reversal so it depends on MD's decision being final.
- Hands off to M2 (#154) — graded/pedagogy rewrite (distractor reprint, verdict). M1 must leave the graded path intact (PedagogyPanel still renders) so M2 can build on it.
- Shared with M3H/M4/M5: the NEW sectionLongLabel.ts and usePregradeTactic.ts and the position/total prop threading are reused by other section screens (M4) — name and locate them so M4 can import without refactor.

**Open questions for owner.**
- Final Swedish wording for the section→long-label map (sectionLongLabel.ts): confirm ELF (English content — 'Engelsk läsförståelse' vs keep English 'English Reading Comprehension'?), NOG ('Kvantitativa resonemang' vs 'Tillräcklig information'), DTK ('Diagram, tabeller och kartor'), XYZ ('Matematisk problemlösning'). Only ORD→'Ordförståelse' is fixed by fixtures.ts:13.
- M3 ORD baseline uses the rail label 'Välj synonym' (M3.tsx:895) but live uses generic 'Välj svar' (sectionRailLabel.ts:25). Keep generic, or restore the ORD-specific 'Välj synonym'?
- How much of the minimal-mast desktop chrome (brand + nav + picker corner, removing runningHead/status) belongs in M1 vs M0/M6? M1 needs at minimum to stop the doubled runningHead/status for single-column drill; full mast may be a shared chrome task.
- Exact form of the phone touch exit: top-left back chevron ‹ vs top-right close ✕, and label ('Tillbaka' to Home vs 'Avsluta övning' with confirm)? Affects whether mid-session progress is discarded.

## M2 — M2 — Drill graded / pedagogy rewrite → M3 DrillScreen graded

**Objective.** Rewrite PedagogyPanel's PostGradeBody so the graded drill reveal is a faithful single-column rebuild of M3 DrillScreen's graded section (M3.tsx ~929-991, the .m3-ped block), NOT the current EDITION re-skin. Render: the italic-serif UTFALL verdict word (Rätt./Fel., is-ok/is-bad) + a verdict-sub sentence + a solution lede; a "Så löser du den" step grid using hpc-m3-step with "1." ordinals (NOT zero-padded "01") and a kärna/detalj tier badge; a "Varför de andra lockar" distractor block where each distractor REPRINTS its struck-through OPTION TEXT (the load-bearing reprint that makes single-column safe — today PedagogyPanel.tsx:600 shows only the bare letter), with body text from snake_case why_tempting/why_wrong; and a hpc-m3-next-row with a hpc-m3-cta "Nästa fråga" (suppressed when flush, i.e. phone, since the floating pill from M1 owns that affordance there). Fold the five live-only blocks (CoachLine, FrameworkChip, StrategiBlock, MetaBlock, QABar) per the decisions below. Preserve every pedagogy-* data-testid.

**Reference.** /redesign-l12?dev=1&v=3 — ORD drill (var-2026-verb1-ORD-003, headword "eftertrakta"), AFTER picking an answer (graded state). M3 renders the DrillScreen component (M3.tsx:848-995). Capture BOTH a correct pick (answer D) → "Rätt." is-ok, and a wrong pick (e.g. A) → "Fel." is-bad. The graded apparatus is the .m3-ped block, M3.tsx:929-991: Utfall (verdict word + verdict-sub + m3-solution), "3 steg / Så löser du den" (m3-step grid), "4 fällor / Varför de andra lockar" (m3-dis blocks), m3-next-row (m3-cta + m3-keys "Enter går vidare").

**Files to edit.**
- /home/loucmane/dev/hpfetcher/app/src/components/drill/PedagogyPanel.tsx (PostGradeBody rewrite: verdict-sub, solution lede gating, step grid, distractor reprint, next-row CTA; thread options through props; fold CoachLine into verdict-sub, drop StrategiBlock+MetaBlock, keep FrameworkChip+QABar)
- /home/loucmane/dev/hpfetcher/app/src/components/StudyDesk.tsx (line 60-66: thread question.options + question.answer + question.section into <PedagogyPanel>)
- /home/loucmane/dev/hpfetcher/app/src/components/drill/DrillQuestion.tsx (line 200: thread question.options + question.answer into <PedagogyPanel>)
- /home/loucmane/dev/hpfetcher/app/src/index.css (M0 should add hpc-m3-verdict-sub, hpc-m3-step + -n/-h/-tier/-t, hpc-m3-dis + -h/-k/-l/-p, hpc-m3-next-row, hpc-m3-cta, hpc-m3-keys; if M0 has not landed them, M2 adds them copying the M3 CSS const values cited below)

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| Outcome section wrapper (.m3-ped) — animated container for the whole graded reveal | M3.tsx:930 (className m3-ped) + CSS M3.tsx:416-418 | animation: m3-in-y 320ms cubic-bezier(0.22,1,0.36,1) both. Live class name hpc-m3-ped (or reuse existing PedagogyPanel <aside> wrapper). Need only the entrance; do NOT introduce card chrome (border/radius/background). Live PostGradeBody has no single animated wrapper today. | GAP | Graded body fades/translates in as one unit at 1440 and 402; no border/radius/background box around it. |
| Utfall section label (margin-rail meta word "Utfall") | M3.tsx:931 (<Section meta={<>Utfall</>} delay={0}>) | Swedish copy "Utfall" in the margin rail meta slot. Single-column live PedagogyPanel has no margin rail, so DROP the standalone "Utfall" label — the verdict word is the section opener; keep current behavior of no eyebrow before verdict. | GAP | No redundant "Utfall" eyebrow above the verdict; verdict word reads as the section opener. |
| Verdict wrapper .hpc-m3-verdict (role=status, aria-live) | M3.tsx:932 (.m3-verdict) + CSS M3.tsx:420-422 + live index.css:862-864 | padding: 4px 0 0. Keep live's role="status" aria-live="polite" aria-atomic="true" aria-label (PedagogyPanel.tsx:289-295) — an a11y addition beyond M3, KEEP. | MATCH | hpc-m3-verdict exists in index.css with padding 4px 0 0; a11y attrs preserved. |
| Verdict word "Rätt." / "Fel." (italic serif, is-ok/is-bad) | M3.tsx:933-935 (.m3-verdict-word + is-ok/is-bad) + CSS M3.tsx:424-439 + live index.css:865-881 | font-family var(--font-display); font-style italic; font-weight 600; font-size clamp(34px,5vw,46px); line-height 1.05; letter-spacing -0.01em; display inline-block; animation m3-verdict-in 420ms cubic-bezier(0.22,1,0.36,1) both. is-ok → color var(--ok); is-bad → color var(--bad). EXACT strings "Rätt." / "Fel." (with trailing period). NOTE live keyframe hpc-m3-verdict-in (index.css:910) uses translateY(8px) while M3 uses translateX(-8px) (M3.tsx:705); prefer matching M3's translateX(-8px) for fidelity. | MATCH | Verdict word renders italic serif at clamp(34-46px), green on correct / red on wrong, trailing period present, at 1440 and 402 in light+dark. |
| Verdict-sub sentence (coached one-liner under the verdict word) | M3.tsx:936-940 (ORD) + M3.tsx:1129-1139 (SectionDrill) + CSS M3.tsx:441-447 | CSS margin-top 10px; font-size 15px; color var(--ink-2); max-width 60ch; animation m3-in-y 320ms cubic-bezier(0.22,1,0.36,1) 140ms both. Class hpc-m3-verdict-sub DOES NOT EXIST in index.css yet (M0/M2 must add). The 4 EXACT Swedish strings: (1) ORD correct "Snyggt — taktiken höll hela vägen." (M3.tsx:938); (2) ORD wrong `Rätt svar är ${answer.toLowerCase()}) vilja ha. Häng med i varför.` (M3.tsx:939; literal "vilja ha" is the ORD fixture's correct option — live MUST substitute the real correct option text); (3) Section correct "Snyggt — rätt tänkt hela vägen." (M3.tsx:1131); (4) Section wrong `Rätt svar är ${answer.toLowerCase()}) {correctOption.text}. Häng med i varför.` (M3.tsx:1134-1136; trailing "Häng med i varför." only when an explanation exists). LIVE GENERALIZATION: correct → "Snyggt — rätt tänkt hela vägen." (universal); wrong → `Rätt svar är ${answer.toLowerCase()}) ${correctOptionText}. Häng med i varför.` with correctOptionText = options.find(o=>o.letter===answer).text via <MathText>. | GAP | Under the verdict word a 15px ink-2 sentence at max-width 60ch: correct shows "Snyggt — rätt tänkt hela vägen."; wrong shows "Rätt svar är d) vilja ha. Häng med i varför." with the REAL correct option text and lowercase letter. |
| Solution lede .hpc-m3-solution (bold serif restatement between rules) | M3.tsx:942 (.m3-solution {EXPLANATION.solution}) + CSS M3.tsx:451-463 + live index.css:886-898 | margin 4px 0 (M3 uses margin-top 22px; live already 4px 0 — keep live); padding 16px 0 (M3 18px 0); border-top 1px solid var(--ink); border-bottom 1px solid var(--hairline); font-family var(--font-display); font-weight 600; font-size clamp(17px,0.9rem+0.4vw,19px); line-height 1.5; letter-spacing -0.01em; color var(--ink); max-width 75ch. DATA: M3 reads EXPLANATION.solution; LIVE field is solution_path (snake_case) via <MathText>. GATING: keep the existing hasStructuredSteps gate (PedagogyPanel.tsx:282,303) — print the lede when structured steps[] exist (ORD fixture has both steps and solution) so a single-step prose fallback never double-prints. | MATCH | hpc-m3-solution renders solution_path via MathText, bold serif ~19px between a 1px ink top rule and a hairline bottom rule, max-width 75ch; does not duplicate the single-step prose. |
| Steps section label + count + title ("3 steg" + "Så löser du den") | M3.tsx:945 (<Section meta={<>3 steg</>} title="Så löser du den">); section variant {explanation.steps.length} steg (M3.tsx:1153) | Title (h2.m3-h, CSS M3.tsx:111-118): font-size 13px; font-weight 600; letter-spacing 0.04em; text-transform uppercase; color var(--ink-2); margin 0 0 6px. Swedish title "Så löser du den". Count meta `${steps.length} steg` rendered inline as an Eyebrow/Mono (no margin rail in single column). REPLACE live Eyebrow "Post-mortem"/"Så här löses uppgiften" (PedagogyPanel.tsx:277,315). | WRONG | Step section is headed "Så löser du den" (not "Post-mortem"/"Så här löses uppgiften"); a step count (N STEG) shows as eyebrow/mono. |
| Step row .hpc-m3-step (grid: ordinal + body) | M3.tsx:946-960 (.m3-step, animationDelay 220+i*80) + CSS M3.tsx:465-474 | display grid; grid-template-columns 36px 1fr; gap 16px; padding 18px 0; border-bottom 1px solid var(--hairline-2); :last-child border-bottom 0; animation m3-in-y 300ms cubic-bezier(0.22,1,0.36,1) both with inline animationDelay = `${220 + i*80}ms`. Class hpc-m3-step DOES NOT EXIST in index.css (M0/M2 add). REPLACES live StepList/StepCard <ol>/<li> (PedagogyPanel.tsx:452-562) which uses paddingLeft 42 + absolute hanging numeral. KEEP data-testid pedagogy-steps on the grid container and pedagogy-step-{n} on each row. | WRONG | Each step is a 36px/1fr grid row with 18px vertical padding and hairline-2 separators (last none); rows enter staggered at 220+i*80ms; pedagogy-steps + pedagogy-step-{n} testids present. |
| Step ordinal .hpc-m3-step-n ("1." NOT "01") | M3.tsx:949 ({s.n}.) + CSS M3.tsx:476-483 | font-family var(--font-display); font-weight 600; font-size 21px; line-height 1.2; color var(--accent); font-variant-numeric tabular-nums. Rendered text `${s.n}.` (e.g. "1.", "2.", "3."). CRITICAL: live StepCard (PedagogyPanel.tsx:531) renders String(step.n).padStart(2,'0') → "01". M3 uses "1." with trailing dot and NO zero pad. Remove the padStart, add the dot. | WRONG | Step ordinals read "1." "2." "3." in cobalt accent serif 21px tabular-nums; NO "01"/"02" zero-padding anywhere. |
| Step heading .hpc-m3-step-h (step title) | M3.tsx:951-952 ({s.title}) + CSS M3.tsx:485-492 | font-family var(--font-display); font-weight 600; font-size 18px; line-height 1.3; color var(--ink); margin 0. DATA: live step.title is OPTIONAL (explanations.ts:44) — render the h3 only when title present (ORD fixture steps all have titles). Render via <MathText> (section variant does, M3.tsx:1167). | GAP | When a step has a title it shows as serif 18px ink heading on the same row as its tier badge; titleless steps omit the heading gracefully. |
| Step tier badge .hpc-m3-step-tier (kärna / detalj) | M3.tsx:953-955 (tier==='essential'?'kärna':'detalj') + CSS M3.tsx:494-503 | font-family var(--font-mono); font-size 10px; font-weight 400; letter-spacing 0.12em; text-transform uppercase; color var(--muted-2); margin-left 10px; vertical-align 3px. Mapping 'essential' → "kärna", else → "detalj". DATA: live step.tier OPTIONAL 'essential'\|'detail' (explanations.ts:53); missing → default 'essential' → "kärna" (per explanations.ts:52-53). Render inside the step heading. | GAP | Each titled step shows an uppercase mono badge "KÄRNA" or "DETALJ" in muted-2, 10px, tracked 0.12em, beside the title; steps lacking tier default to kärna. |
| Step body .hpc-m3-step-t (worked-step prose) | M3.tsx:957 ({s.text}) + CSS M3.tsx:505-511; section variant adds .is-pre (M3.tsx:1172, CSS M3.tsx:669) | font-size 15px; line-height 1.65; color var(--ink-2); margin 6px 0 0; max-width 75ch. Charge says "serif body ~75ch" — MATCH M3's frame reading face (var(--font-display)) for step body; live StepCard currently uses var(--font-ui) sans (PedagogyPanel.tsx:551) — switch to the M3 face. Render via <MathText>. For section drills add .is-pre (white-space: pre-line) — out of M2's ORD scope but keep the class available. | WRONG | Step body is ~15px reading face at line-height 1.65, ink-2, max-width 75ch, via MathText; matches M3 face (not the old sans body). |
| Distractors section label + count + title ("4 fällor" + "Varför de andra lockar") | M3.tsx:964 (<Section meta={<>4 fällor</>} title="Varför de andra lockar">); section variant {explanation.distractors.length} fällor (M3.tsx:1182) | Title via .m3-h (CSS M3.tsx:111-118): 13px/600/0.04em/uppercase/ink-2. Swedish title "Varför de andra lockar". Count meta `${distractors.length} fällor`. REPLACES live Mono label "Varför inte de andra" (PedagogyPanel.tsx:569). | WRONG | Distractor section headed "Varför de andra lockar" with an "N FÄLLOR" count eyebrow; old "Varför inte de andra" label gone. |
| Distractor row .hpc-m3-dis | M3.tsx:966-981 (.m3-dis, animationDelay 360+i*80) + CSS M3.tsx:513-519 | padding 16px 0; border-bottom 1px solid var(--hairline-2); :last-child border-bottom 0; animation m3-in-y 300ms cubic-bezier(0.22,1,0.36,1) both with inline animationDelay = `${360 + i*80}ms`. Class hpc-m3-dis DOES NOT EXIST (M0/M2 add). REPLACES live DistractorBlock rows (PedagogyPanel.tsx:571-625) which use paddingLeft 28 + absolute letter. KEEP data-testid pedagogy-distractor-{letter}. | WRONG | Each distractor is a 16px-padded block with hairline-2 separators (last none), entering staggered at 360+i*80ms; pedagogy-distractor-{letter} testid present. |
| Distractor head .hpc-m3-dis-h + struck OPTION TEXT (LOAD-BEARING REPRINT) | M3.tsx:972-975 (<span m3-dis-k>{letter})</span><s>{d.text}</s>) + CSS M3.tsx:521-526 + 536 | hpc-m3-dis-h font-family var(--font-display); font-weight 600; font-size 17px; color var(--ink). <s> strike text-decoration-thickness 1.5px (M3.tsx:536). Render `${letter.toLowerCase()})` in dis-k THEN <s><MathText>{optionText}</MathText></s>. DATA TRAP: M3 fixture distractor has d.text (fixtures.ts:57,65,73,80); LIVE DistractorExplanation has NO text field (explanations.ts:26-30 = {letter, why_tempting, why_wrong}). RESOLVE optionText by threading question.options into PedagogyPanel and options.find(o=>o.letter===d.letter)?.text. REPLACES PedagogyPanel.tsx:600 which prints only the bare `${d.letter.toLowerCase()}.`. | WRONG | Each distractor head shows e.g. "a) ta reda på" with the option text struck through (1.5px), serif 17px ink; letter key mono muted; struck text is the REAL option text from the threaded options array, matching the option rows above. |
| Distractor letter key .hpc-m3-dis-k | M3.tsx:973 (.m3-dis-k) + CSS M3.tsx:528-534 | font-family var(--font-mono); font-weight 400; font-size 12px; color var(--muted-2); margin-right 8px. Text `${letter.toLowerCase()})` (lowercase letter + close paren). | WRONG | Letter key renders "a)" "b)" … in mono 12px muted-2 immediately before the struck option text. |
| Distractor labels .hpc-m3-dis-l ("Varför det lockar" / "Varför det är fel") | M3.tsx:976,978 (.m3-dis-l) + CSS M3.tsx:538-545 | font-family var(--font-mono); font-size 11px; letter-spacing 0.12em; text-transform uppercase; color var(--accent); margin 8px 0 2px. EXACT Swedish "Varför det lockar" then "Varför det är fel". REPLACES live inline "Lockar för att " / "Men fel för att " prefixes (PedagogyPanel.tsx:610,621). | WRONG | Two cobalt uppercase mono labels per distractor: "VARFÖR DET LOCKAR" above why_tempting, "VARFÖR DET ÄR FEL" above why_wrong; old inline prefixes removed. |
| Distractor body .hpc-m3-dis-p (why_tempting / why_wrong) | M3.tsx:977,979 ({d.whyTempting}/{d.whyWrong}) + CSS M3.tsx:547-553 | font-size 14.5px; line-height 1.6; color var(--ink-2); margin 0; max-width 75ch. DATA TRAP: M3 fixture reads d.whyTempting/d.whyWrong (camelCase, fixtures.ts:58,60); LIVE fields are why_tempting/why_wrong (snake_case, explanations.ts:28-29). Copy M3's px, read LIVE snake_case. Render both via <MathText>. | WRONG | why_tempting then why_wrong each render as ~14.5px ink-2 paragraphs at line-height 1.6, max-width 75ch, via MathText; reads snake_case fields. |
| Next row .hpc-m3-next-row + CTA "Nästa fråga" + keys hint | M3.tsx:985-990 (.m3-next-row > .m3-cta "Nästa fråga" + .m3-keys "Enter går vidare") + CSS M3.tsx:555-560 / 202-218 / 406-414 | next-row margin-top 36px; display flex; align-items center; gap 16px. cta font-family var(--font-ui); font-size 13px; font-weight 600; color var(--accent-ink); background var(--accent); padding 10px 20px; white-space nowrap; transition opacity 160ms; :hover opacity 0.88; :focus-visible outline 2px solid var(--accent) outline-offset 3px. keys (CSS M3.tsx:406-414) mono small muted hint "Enter går vidare". DECISION: on PHONE (flush=true) SUPPRESS this row — M1's floating bottom-center pill owns advance. On DESKTOP (flush=false) render hpc-m3-next-row + hpc-m3-cta wired to an optional onNext prop (thread from parent); render the CTA only when onNext provided AND !flush. hpc-m3-next-row/hpc-m3-cta/hpc-m3-keys DO NOT EXIST in index.css (M0/M2 add). | GAP | Desktop graded panel ends with a cobalt "Nästa fråga" CTA (accent bg, accent-ink, 10/20 padding) + mono "Enter går vidare" hint, 36px above; on phone (flush) the row is suppressed. |
| FOLD: CoachLine (coach voice) → verdict-sub | no M3 equivalent (M3 has only the static verdict-sub) | Live PedagogyPanel.tsx:300-302 renders <CoachLine> with VOICE[coach].feedbackRight/feedbackWrong. DECISION (charge): coach-voice folds INTO verdict-sub. M3 has no separate coach line. DROP the standalone CoachLine; hpc-m3-verdict-sub carries the M3 strings as the single outcome sentence. | WRONG | No standalone CoachLine block between verdict and solution; the verdict-sub carries the outcome sentence; visual matches M3's single-sentence sub. |
| KEEP: FrameworkChip (Layer-1 ↔ Layer-2 deep-link) | no M3 equivalent (live-only, useful) | Live PedagogyPanel.tsx:308 + FrameworkChip 339-355: <Mono>Ramverk</Mono> + <L1Chip> navigating to /lektion?section=SEC#FRAMEWORK_ID. DECISION (charge): KEEP. Place as a quiet mono row (after solution lede or near the step block), not card chrome. Reads explanation.framework_id (snake_case, explanations.ts:94). | MATCH | Framework deep-link chip still renders when framework_id present and navigates to the matching lektion anchor; quiet mono "Ramverk" + chip row, no card box. |
| FOLD: StrategiBlock (post-grade tactic restatement) → drop | M3 shows tactic only PRE-grade (.m3-tactic, M3.tsx:886-889); graded view has NO Strategi block | Live PedagogyPanel.tsx:309-314 + StrategiBlock 363-392 reprints pregrade_tactic.handle/move on the graded view. DECISION (charge): "Strategi/meta blocks fold into the tactic." M3 graded view does NOT repeat the tactic. REMOVE StrategiBlock from PostGradeBody — the tactic lives pre-grade (M1's hpc-m3-tactic). | WRONG | Graded panel does NOT contain a "Strategi" handle/move block; apparatus matches M3's verdict→solution→steps→distractors→next sequence. |
| FOLD: MetaBlock (Teknik + Fällan här chips) → drop visual, keep testids | no M3 equivalent (M3 graded view has no technique/pitfall chips) | Live PedagogyPanel.tsx:320-322 + MetaBlock 633-678 render accent-soft/bad-soft chips with testids pedagogy-technique + pedagogy-pitfall. DECISION (charge): fold into the tactic / remove the chips (M3 has none). REMOVE the colored chip boxes BUT PRESERVE pedagogy-technique + pedagogy-pitfall testids (test contract: preserve ALL pedagogy-* testids). Carry them on sr-only/folded nodes. (grep shows no test currently references them, but the contract still requires keeping them.) | WRONG | No colored Teknik/Fällan-här chip boxes in the graded view; pedagogy-technique + pedagogy-pitfall testids still resolvable in the DOM (sr-only or folded), no testid regression. |
| KEEP: QABar (Hjälpte förklaringen? 👍/👎) | no M3 equivalent (live-only, useful) | Live PedagogyPanel.tsx:323 + QABar 682-730: testid pedagogy-qa, borderTop 1px solid var(--hairline-2), Mono "Hjälpte förklaringen?"/"Tack för din feedback", two QAButtons. DECISION (charge): KEEP. Place at the very bottom, after next-row; quiet hairline-top, no card. | MATCH | QA bar still renders at panel bottom with pedagogy-qa testid and working 👍/👎 submit; quiet hairline-top, no card chrome. |
| KEEP: MissingExplanation state (pedagogy-flag-missing) | M3 section variant has .m3-missing "Förklaring saknas ännu för den här frågan." (M3.tsx:1146) for null explanation | Live MissingExplanation (PedagogyPanel.tsx:140-193) renders when loadExplanation returns null; testid pedagogy-flag-missing MUST be preserved (explicit contract). M3's parallel is the m3-missing line. Keep the live missing state + flag CTA unchanged; ensure the verdict word still renders above it (M3 shows verdict THEN m3-missing). | MATCH | When explanation is missing, the verdict word still shows, a missing-explanation message + pedagogy-flag-missing flag button render, and flagging writes a rejected feedback entry as today. |
| KEEP: pedagogy-solution testid (single-step prose path) | no direct M3 equivalent (single-step ORD collapses) | Live StepList single-step path (PedagogyPanel.tsx:457-469) carries data-testid="pedagogy-solution". When steps resolve to exactly one, keep emitting pedagogy-solution on whichever node renders the lone step. PRESERVE the testid. | MATCH | Single-step explanations still expose pedagogy-solution testid; no testid regression in vitest. |

**Data traps.**
- DISTRACTOR HAS NO TEXT (the load-bearing one): M3 fixture distractor exposes d.text (fixtures.ts:57,65,73,80) but the LIVE DistractorExplanation type is {letter, why_tempting, why_wrong} with NO text field (explanations.ts:26-30). The struck option text MUST be resolved by threading question.options into PedagogyPanel and doing options.find(o=>o.letter===d.letter)?.text. Without threading, the struck reprint is impossible. This is the central plumbing change of M2.
- camelCase vs snake_case: M3 reads d.whyTempting/d.whyWrong (fixtures.ts:58,60,…); LIVE reads d.why_tempting/d.why_wrong (explanations.ts:28-29). M3 reads EXPLANATION.solution; LIVE reads explanation.solution_path (explanations.ts:73). M3 reads EXPLANATION.pregradeTactic; LIVE reads explanation.pregrade_tactic (explanations.ts:101). Copy M3's px/clamp/color values, NOT its field names.
- Optional step fields: live step.title and step.tier are OPTIONAL (explanations.ts:44,53). M3 fixture always has title + tier. Render the heading only when title present; default missing tier to 'essential' → "kärna" (per explanations.ts:52-53). Render via <MathText> (steps may carry U+E000/U+E001 math markers).
- ORD verdict-sub hardcoded fixture text: M3.tsx:939 literally writes "vilja ha" (the ORD fixture's correct option). Live MUST substitute the real correct option text from options.find(o=>o.letter===answer).text — do NOT hardcode "vilja ha".
- Zero-pad trap: live StepCard renders String(step.n).padStart(2,'0') → "01" (PedagogyPanel.tsx:531). M3 uses `${s.n}.` → "1.". Drop the padStart and add the trailing period.
- Solution double-print: M3 always prints the solution lede; for single-step ORD the live solution_path could overlap the lone step. Keep the existing hasStructuredSteps gate (PedagogyPanel.tsx:282,303) so the lede prints when structured steps[] exist (ORD fixture has both) and isn't duplicated by a single-step prose fallback.
- Missing hpc-m3 CSS classes: hpc-m3-verdict-sub, hpc-m3-step (+ -n/-h/-tier/-t), hpc-m3-dis (+ -h/-k/-l/-p), hpc-m3-next-row, hpc-m3-cta, hpc-m3-keys are NOT in index.css yet (verified: only hpc-m3-verdict, -verdict-word, -solution, -display, -coda exist at index.css:657,724,862-898). These are M0's foundation charge; if M0 hasn't landed them, M2 must add them copying the exact px/clamp/color values from the M3 CSS const (M3.tsx:202-560).
- flush=phone semantics: PedagogyPanel gets flush=true on phone (DrillQuestion.tsx:200) and inside StudyDesk (StudyDesk.tsx:64). Per locked decision desktop drill is now SINGLE COLUMN via DrillQuestion, NOT StudyDesk — confirm with MD which call site survives before wiring onNext; the next-row CTA must be suppressed when flush (phone) because M1's floating pill owns advance there.

**Test contracts.**
- No e2e test asserts any pedagogy-* testid (grep of tests-e2e/ returns zero hits) — but the contract still requires PRESERVING all pedagogy-* testids: pedagogy-panel (PedagogyPanel.tsx:92), pedagogy-flag-missing (174), pedagogy-solution (459), pedagogy-steps (473), pedagogy-step-{n} (502), pedagogy-distractor-{letter} (577), pedagogy-technique (638), pedagogy-pitfall (658), pedagogy-qa (702). Every one must still resolve in the DOM after the rewrite, even where the visual block is folded (technique/pitfall may become sr-only/folded but the testid stays).
- Vitest: the __test export (splitProseIntoSteps/resolveSteps, PedagogyPanel.tsx:781) plus resolveSteps/StepList/StepCard are exported and consumed by the phone ExplanationPanel (PedagogyPanel.tsx:448-451). Do NOT remove these exports. If M2 rewrites StepList into the hpc-m3-step grid, the phone ExplanationPanel inherits it — verify the phone path still renders correctly OR keep StepList's public shape stable.
- pnpm test (vitest) must stay green; pnpm test:e2e (drill.spec.ts, responsive.spec.ts, mistakes.spec.ts) must stay green at all three viewports.
- Props signature change: adding options/answer (and optional onNext) to PedagogyPanel is breaking — BOTH call sites (StudyDesk.tsx:60, DrillQuestion.tsx:200) and any *.test.tsx that mounts PedagogyPanel directly must be updated in the same change. Grep for direct PedagogyPanel mounts before landing.
- drill.spec.ts: verify the verdict word "Rätt."/"Fel." and hpc-m3-verdict-word is-ok/is-bad still render so any verdict assertion holds.

**Decisions applied.**
- Desktop drill = SINGLE COLUMN (.m3-frame max-width 880px), reversing tasks 80-87's 2-col StudyDesk. The pedagogy reveal flows BELOW the options in one column — which is exactly why the struck-option-text reprint (M2's load-bearing change) is required: the referent must travel with the explanation since there is no adjacent option column to glance at.
- M2's struck distractor reprint is explicitly load-bearing: M3.tsx:973-974 (the struck d.text) replaces today's letter-only block at PedagogyPanel.tsx:600. This makes single-column safe.
- Progress = textual eyebrow only; NO progress bar — applies to the step/distractor counts ("3 steg", "4 fällor") which are textual eyebrows, not bars.
- ⌘K is a power-user accelerator, not primary nav — the "Enter går vidare" keys hint is a desktop-only accelerator, mirrored on phone by M1's floating pill (touch).
- Phone drill gets a floating bottom-center "Nästa fråga" pill (M1) reachable without scrolling — therefore M2 SUPPRESSES the in-panel hpc-m3-next-row CTA when flush=true (phone) to avoid two competing Nästa affordances.
- Fold the 5 live-only blocks per charge: CoachLine → verdict-sub (drop standalone); FrameworkChip → KEEP (useful deep-link); QABar → KEEP (useful); StrategiBlock → REMOVE (tactic lives pre-grade); MetaBlock → REMOVE visual chips but preserve pedagogy-technique/pedagogy-pitfall testids.
- camelCase(M3 fixture) vs snake_case(live) is a systemic trap — copy M3's CSS/px, read live snake_case (why_tempting/why_wrong/solution_path); resolve distractor text + correct-answer text from the threaded options array, not from the explanation.

**Definition of done.**
- ANTI-DRIFT GATE: M3 reference URL+state cited (/redesign-l12?dev=1&v=3, ORD, after-pick) and a side-by-side live vs M3 screenshot captured at 1440px AND 402px, in BOTH light AND dark, for BOTH a correct pick (Rätt./is-ok) and a wrong pick (Fel./is-bad).
- Every elementChecklist row visually matched against M3 and exact px/clamp/letter-spacing/color-token/Swedish-copy values copied (verified against computed styles / rendered DOM, not eyeball alone).
- Struck option text reprint works: each distractor head shows "<letter>) <option text>" with the real option text (from threaded options) struck through 1.5px — confirmed against the option rows above.
- Step ordinals are "1." "2." "3." (NOT "01"); tier badges show kärna/detalj; section headings read "Så löser du den" and "Varför de andra lockar"; distractor labels read "Varför det lockar"/"Varför det är fel".
- Desktop shows the hpc-m3-next-row "Nästa fråga" CTA + "Enter går vidare" hint; phone (flush) suppresses it (floating pill owns advance).
- StrategiBlock + MetaBlock visual chips removed; CoachLine folded; FrameworkChip + QABar retained and functional.
- ALL pedagogy-* testids still resolve in the DOM (pedagogy-panel, -flag-missing, -solution, -steps, -step-{n}, -distractor-{letter}, -technique, -pitfall, -qa) — verified by a DOM query.
- Both PedagogyPanel call sites (StudyDesk.tsx, DrillQuestion.tsx) updated to thread options/answer; any direct-mount tests updated.
- pnpm test green; pnpm test:e2e green at all 3 viewports; pnpm build clean (TS, no unused imports after removing StrategiBlock/MetaBlock/CoachLine if they become dead).
- Owner approves the visual diff before merge — NO auto-merge label on this PR.

**Dependencies.**
- M0 — Foundation: must land the missing hpc-m3 CSS classes (hpc-m3-verdict-sub, hpc-m3-step + -n/-h/-tier/-t, hpc-m3-dis + -h/-k/-l/-p, hpc-m3-next-row, hpc-m3-cta, hpc-m3-keys) and the --muted-2/--ok/--bad/--hairline-2/--accent-ink color tokens these reference. If M0 has not yet added them, M2 carries the CSS itself.
- M1 — Drill pre-answer: owns the phone floating "Nästa fråga" pill and the pre-grade hpc-m3-tactic; M2's decision to suppress the in-panel next-row on phone and to NOT repeat the tactic post-grade assumes M1's pre-grade tactic is the single tactic home. Coordinate the flush/onNext wiring with M1.
- MD — Layout decision bake-off (#151): confirms desktop drill = single column via DrillQuestion (not StudyDesk). M2's single-column-safe reprint + which PedagogyPanel call site survives depends on MD's outcome.
- Shared MathText component (src/components/MathText) — required to render solution_path/step.text/why_tempting/why_wrong/option text that may carry U+E000/U+E001 math markers; already imported in PedagogyPanel.tsx:33.

## M3H — M3H — Home → M3 HomeScreen (faithful rebuild)

**Objective.** Rebuild the live Home surface (HomeMobile + DailyPlanCard + TopTrapsCard + ResumptionPanel/useResumptionCandidate + scoring.ts) so it is structurally and typographically identical to M3.tsx HomeScreen (M3.tsx:758-846). This REVERSES the tasks 80-87 graft: those kept the EDITION Page shell (running head, status line, score-line, 58/42 studio grid, ResumptionPanel aside) and bolted hpc-m3 rail rows onto plan/trap rows. M3H instead reproduces the .m3-frame single-column page: a date-rail hero with a serif-italic greeting + a m3-stats row (prognos COMMA-decimal + honest week-over-week delta + streak + min idag), a m3-resume accent-soft band, a numbered m3-plan-item list, and a flat m3-trap list — each as a Section with margin-rail meta. Minimal-mast chrome (no running head / no status line / no exam-tag). Add a comma-decimal score formatter and a NEW computeProjectedDelta to scoring.ts, and rewrite the 3 home-score-line test cases + move the home-score-line testid to the stats block. Do NOT touch generateDailyPlan.

**Reference.** /redesign-l12?dev=1&v=3&s=home — the M3 "Boksidan" variant with the "Hem" toggle active. Renders M3.tsx HomeScreen (M3.tsx:758-846) inside the .m3-frame (max-width 880px, single column). Verify in light AND dark via the fixed switcher (palette default "spalt"; toggle "mörk"). Data is HOME fixture (fixtures.ts:89-150). The mast/nav called for in the LOCKED minimal-mast decision is NOT present in the M3 reference itself (M3 HomeScreen has no top band) — render the minimal mast as the agreed chrome wrapper, NOT a copy of an M3 element; the M3 reference governs the PAGE BODY only.

**Files to edit.**
- /home/loucmane/dev/hpfetcher/app/src/screens/HomeMobile.tsx
- /home/loucmane/dev/hpfetcher/app/src/components/home/DailyPlanCard.tsx
- /home/loucmane/dev/hpfetcher/app/src/components/home/TopTrapsCard.tsx
- /home/loucmane/dev/hpfetcher/app/src/components/home/ResumptionPanel.tsx
- /home/loucmane/dev/hpfetcher/app/src/components/home/useResumptionCandidate.ts
- /home/loucmane/dev/hpfetcher/app/src/lib/scoring.ts
- /home/loucmane/dev/hpfetcher/app/src/index.css (port the home-specific .hpc-m3-* classes that M0 did not land: stats/stat-n/l/d, resume*, plan-item/n/t/r/min, trap/t/n, tag, frame, h, cta — verify M0 ownership first to avoid duplication)
- /home/loucmane/dev/hpfetcher/app/src/screens/HomeMobile.test.tsx (rewrite the 3 score-line cases + move testid)
- /home/loucmane/dev/hpfetcher/app/src/lib/scoring.test.ts (ADD tests for new comma formatter + computeProjectedDelta; do NOT mutate formatScore dot-decimal cases at :208-213)
- /home/loucmane/dev/hpfetcher/app/src/routes/index.tsx (wire computeProjectedDelta from stats.data.bySection + thread date/scoreDelta props; presentational HomeMobile stays prop-driven)

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| Page frame / single-column container (.m3-frame) | M3.tsx:760 + CSS M3.tsx:56-60 | max-width:880px; margin:0 auto; padding:56px 24px 96px. Responsive @media(max-width:900px): padding:40px 18px 72px (M3.tsx:721). Port as .hpc-m3-frame. | WRONG | Home body is ONE centered column ≤880px; live currently uses Page shell + 58/42 studio grid (HomeMobile.tsx:191-194) and full-bleed padding — that grid/aside must be deleted. |
| Hero section row (.m3-section > .m3-row, first child, no top rule) | M3.tsx:761-786 + CSS :64-68,:99-100 | grid-template-columns:128px 1px 1fr; column-gap:28px (@≤900px: 96px 1px 1fr / 20px). .m3-section margin-top:64px, :first-child margin-top:0. Hero has NO <hr class=m3-rule> (only Section() helper rows do). | GAP | Hero renders as rail\|spine\|content grid; first section has no top hairline; matches M3 at 1440 and reflows to 96px rail by 900px. |
| Rail meta = date label (.m3-meta > strong) | M3.tsx:763-765 + CSS :75-91; data fixtures.ts:90 dateLabel 'Torsdag 11 juni' | font-family:var(--font-mono); font-size:11px; letter-spacing:0.08em; text-transform:uppercase; color:var(--muted); text-align:right; padding-top:5px; tabular-nums. strong: display:block; color:var(--ink-2); font-weight:500. Copy LIVE date via formatSwedishHeader(today) but NOTE that returns 'Torsdag · 11 jun' (dates.ts:43-47, with '· ' and 3-letter month), NOT 'Torsdag 11 juni'. Recommend a new full-month, capitalized-weekday formatter to match the reference verbatim. | WRONG | Rail shows capitalized Swedish weekday + day + full lowercase month, right-aligned mono uppercase --muted, in the 128px rail. Live emits 'HP · Coach'/'Hem' running head + 'X dagar till ...' status (HomeMobile.tsx:138-143) which is DELETED. |
| Greeting display (.m3-display) | M3.tsx:768 + CSS :136-146; data fixtures.ts:91 'God eftermiddag' | font-family:var(--font-display); font-style:ITALIC; font-weight:400; color:var(--ink); font-size:clamp(44px,6vw,64px); line-height:1.05; letter-spacing:-0.01em; margin:10px 0 0; animation m3-settle 480ms. Copy live greeting via hourGreeting(today) (HomeMobile.tsx:317-325; values match 'God eftermiddag'). M3 has NO trailing period and NO firstName; live appends '.' and optional ', {firstName}'. Keep live firstName behavior but render with .hpc-m3-display (italic serif). | WRONG | Greeting is serif-ITALIC clamp(44,6vw,64); live uses Display level 2 SANS (HomeMobile.tsx:206-224) at lineHeight 1.02 maxWidth 24ch — replaced by .hpc-m3-display. |
| Stats row container (.m3-stats) | M3.tsx:769-783 + CSS :150-156 | display:flex; gap:48px; margin-top:28px; padding-top:20px; border-top:1px solid var(--hairline). Port as .hpc-m3-stats. data-testid='home-score-line' MOVES HERE from the old greeting score-line (HomeMobile.tsx:228). | WRONG | Three stat cells in a flex row under a hairline; testid home-score-line is on this container; the old mono score-line block (HomeMobile.tsx:226-255) is deleted. |
| Stat 1 — prognos number (.m3-stat-n) | M3.tsx:770-774 + CSS :158-164; svDecimal M3.tsx:725-727; data fixtures.ts:92 '1.4' | font-family:var(--font-mono); tabular-nums; font-size:30px; line-height:1.1; color:var(--ink). Value = NEW comma formatter on computeProjected(...).total → '1,4'. M3 renders svDecimal('1.4')='1,4' (one decimal). Live formatScore gives DOT 2-decimal '1.40' (WRONG here). Author formatScoreComma(n): n==null?'—':n.toFixed(1).replace('.',','). | WRONG | Prognos shows COMMA one-decimal (e.g. '1,4'); '—' when total null. Mutually exclusive with the old DOT score-line — that case is rewritten. |
| Stat 1 — prognos label (.m3-stat-l) | M3.tsx:772 + CSS :166-170 | font-size:12px; color:var(--muted); margin-top:2px. Copy: 'prognos av 2,0' (COMMA, M3.tsx:772). | GAP | Label reads exactly 'prognos av 2,0' with comma in --muted 12px. |
| Stat 1 — week-over-week delta (.m3-stat-d) | M3.tsx:773 + CSS :172-176; data fixtures.ts:93 '+0.1 sedan förra veckan' | font-size:12px; color:var(--ok); margin-top:2px. Value = NEW computeProjectedDelta over SectionStats.attempts7to14d/correct7to14d (scoring.ts:22-31) vs current-week 7d, NOT SectionScore.trend. Render only when BOTH weeks have ≥N attempts in the SAME sections (recommend N=10 per confidenceFor scoring.ts:59-63) else render NOTHING (omit node). Format: signed comma decimal + ' sedan förra veckan' → '+0,1 sedan förra veckan'. Keep --ok color (no red) per progress-is-textual decision. | GAP | Delta is an HONEST week-over-week from 7d vs 7to14d summed across sections with data in both; hidden when either week below threshold; comma decimal + 'sedan förra veckan'. NOT SectionScore.trend, NOT a fabricated constant. |
| Stat 2 — streak (.m3-stat-n + .m3-stat-l) | M3.tsx:775-778; data fixtures.ts:94 streakDays 12 | n: mono 30px tabular ink; l: 12px --muted 'dagar i rad'. Value = live streakDays prop (HomeMobile.tsx:68, routes/index.tsx:38 stats.data.streakDays). | GAP | Streak shows '{streakDays}' over 'dagar i rad'; uses the existing streakDays prop (currently only forwarded to MobileFrame chrome, HomeMobile.tsx:133). |
| Stat 3 — min idag (.m3-stat-n + .m3-stat-l) | M3.tsx:779-782; data fixtures.ts:95 estimatedMinutes 16 | n: mono 30px tabular ink; l: 12px --muted 'min idag'. Value = plan.estimatedMinutes (DailyPlan). M3 reuses estimatedMinutes (16) for BOTH this stat and the plan header. Show '—' or omit when plan null (skeleton). | GAP | Third stat shows estimated minutes over 'min idag'; tracks the same number as the plan header. |
| Resume Section wrapper (meta='Påbörjad', delay 120, NO title) | M3.tsx:788-803 via Section() M3.tsx:729-756 | Section() emits <hr class=m3-rule> (CSS :102-109: 1px var(--hairline), margin 0 0 20px, m3-draw anim) + m3-row(rail\|spine\|content). meta mono uppercase 'Påbörjad' in 128px rail. delay=120ms. Reuse .hpc-m3-rule/row/meta/spine/content. | WRONG | Resume block is a Section with a drawn top hairline, 'PÅBÖRJAD' rail label, content holds the resume band. Live ResumptionPanel is a right-column aside (ResumptionPanel.tsx:29-95) — that aside is deleted; resumption moves into the single column. |
| Resume band (.m3-resume) | M3.tsx:789-802 + CSS :178-200 | display:flex; align-items:center; justify-content:space-between; gap:16px; background:var(--accent-soft); padding:16px 20px; margin-top:4px. .m3-resume-t: 15px; weight 500; --ink = '{kind} · {section}' → 'Övning · ORD' (fixtures.ts:97-98). .m3-resume-s: mono 12px --muted tabular margin-top:3px = 'fråga {position} av {total} · {device} · {when}' → 'fråga 4 av 10 · telefon · 11:07' (fixtures.ts:99-102). | WRONG | Accent-soft band: title 'Övning · ORD' over mono 'fråga 4 av 10 · telefon · 11:07' + right CTA. Live composes 'ORD-övning · pausad' + italic marginalia (useResumptionCandidate.ts:108-114) — reshape the hook to expose discrete {kind,section,position,total,device,when} (or composed M3 strings) keeping null→render-nothing + stale gating. |
| Resume CTA (.m3-reset.m3-cta — 'Fortsätt här') | M3.tsx:799-801 + CSS :202-218 | font-family:var(--font-ui); 13px; weight 600; color:var(--accent-ink); background:var(--accent); padding:10px 20px; nowrap; transition opacity 160ms; :hover .88; :focus-visible outline 2px var(--accent) offset 3px. M3 copy 'Fortsätt här' (no arrow). Live link 'Fortsätt här →' (ResumptionPanel.tsx:92). Keep a real Link/anchor to c.href styled as .hpc-m3-cta; default match M3 (drop arrow). | WRONG | Filled accent CTA 'Fortsätt här' navigating to the resume href; data-testid='home-resumption-link' preserved for the cross-device drive. |
| Plan Section wrapper (meta='16 min · 3 moment', title='Dagens plan', delay 220) | M3.tsx:805-829 + Section() :729-756 | meta fragment: <strong>{estimatedMinutes} min</strong> then '3 moment' (M3.tsx:807-810) — strong is the --ink-2 block line, '3 moment' the mono --muted line below. title 'Dagens plan' as .m3-h (CSS :111-118: 13px; weight 600; ls 0.04em; uppercase; --ink-2; margin 0 0 6px). delay 220. count = plan.items.length, minutes = plan.estimatedMinutes. | WRONG | Plan Section: rail '{N} MIN' bold over '{count} MOMENT', content 'DAGENS PLAN' h2 + numbered list. Live DailyPlanCard uses Eyebrow 'Idag · ~N min · uppskattat' (DailyPlanCard.tsx:63) + per-item rails — replaced by ONE section rail + numbered items. |
| Plan item row (.m3-plan-item grid) | M3.tsx:815-827 + CSS :220-229 | grid-template-columns:32px 1fr auto; gap:16px; align-items:baseline; padding:14px 0; border-bottom:1px solid var(--hairline-2); :last-child border-bottom:0. Cells: number \| (title+rationale) \| minutes. | WRONG | Each item is a 3-col grid (number/body/minutes) with hairline-2 separators; NOT a per-item margin-rail row (DailyPlanCard.tsx:106-149 removed). |
| Plan number (.m3-plan-n) | M3.tsx:817 + CSS :231-238 | font-family:var(--font-display); weight 600; 19px; line-height 1.2; color:var(--accent) (COBALT); tabular-nums. Content '{i+1}.' (M3.tsx:817). | WRONG | Cobalt serif '1.'/'2.'/'3.' lead each row. Live has NO numbers (uses a mono kicker 'Repetition · NOG', DailyPlanCard.tsx:205-215) — replace kicker with this numeral. |
| Plan section tag (.m3-tag) | M3.tsx:820 + CSS :263-272 | font-family:var(--font-mono); 11px; ls 0.08em; --muted; border:1px solid var(--hairline); padding:1px 6px; margin-right:8px; vertical-align:2px. Rendered inline before headline only when item.section non-null (M3.tsx:820). | GAP | Bordered mono section chip (e.g. 'XYZ','ORD') sits inline before the headline; omitted for the repetition item (section null). Maps PlanItem.section. |
| Plan title (.m3-plan-t) + headline contiguity | M3.tsx:819-822 + CSS :240-246 | font-family:var(--font-display); weight 500; 17px; line-height 1.35; --ink. Tag + headline live in ONE .m3-plan-t node so headline stays CONTIGUOUS. Use live PlanItem.headline verbatim. | WRONG | Headline is serif 17px 500; tag inline within the same line; 'Repetition · 3 missar' resolves as ONE text node (HomeMobile.test.tsx:80). Live splits kicker/headline(h3 22px) — collapse to m3-plan-t. |
| Plan rationale (.m3-plan-r) | M3.tsx:823 + CSS :248-253 | font-size:13px; color:var(--muted); margin-top:3px; max-width:56ch. Content = PlanItem.rationale verbatim. | WRONG | Rationale 13px --muted ≤56ch under the title. Live renders serif 14-15px --ink-2 (DailyPlanCard.tsx:137-147) — restyle to 13px --muted. |
| Plan minutes (.m3-plan-min) | M3.tsx:825 + CSS :255-261 | font-family:var(--font-mono); 12px; color:var(--muted-2); tabular-nums; nowrap. Content '{minutes} min' from PlanItem.estimatedMinutes. M3 shows '8 min' (no tilde). | WRONG | Trailing mono '8 min' in --muted-2 right cell. Live puts minutes in the rail strong as '~N min' (DailyPlanCard.tsx:110) — move to trailing cell, drop '~' (owner sign-off on losing the heuristic tilde). |
| Plan kind→label mapping | fixtures.ts:104-129 vs DailyPlanCard.tsx:205-215 | Live PlanItem.kind ∈ {'repetition','lesson','drill'} (English), NOT fixture Swedish {'repetition','lektion','övning'}. M3 plan items carry NO kind kicker (number+tag+headline only), so kind is not surfaced; if any label needed map lesson→'Lektion', drill→'Övning', repetition→'Repetition'. DO NOT switch on Swedish kind values. | GAP | No kind kicker rendered (M3 drops it); any internal mapping uses the live English kind enum. |
| Plan completed state (line-through / opacity) | no M3 equivalent | Live shows completed at opacity .5 + line-through (DailyPlanCard.tsx:99,131). M3 has none. PRESERVE the signals-derived completion (product behavior, not drift) but reskin within m3-plan-item: opacity .5 on row, line-through on .m3-plan-t. Keep data-completed (DailyPlanCard.tsx:95). | GAP | Completed items dim + strike within m3-plan-item; data-completed + daily-plan-item-{id}/daily-plan-link-{id} testids preserved. |
| Plan complete panel ('Klart för idag' / 'Perfect game.') | no M3 equivalent | Live CompletePanel (DailyPlanCard.tsx:154-203) renders when allComplete. M3 has none. PRESERVE behavior + testid daily-plan-complete; reskin headline to .hpc-m3-display (italic serif), eyebrow to .hpc-m3-h. Keep copy 'Klart för idag'/'Perfect game.' (HomeMobile.test.tsx:94-96). | GAP | allComplete still renders daily-plan-complete with 'Perfect game.'; restyled to Boksidan type; HomeMobile.test.tsx:92-97 green. |
| Plan skeleton (null plan) | no M3 equivalent | Live PlanSkeleton (HomeMobile.tsx:299-315) 'Laddar dagens plan …' mono. M3 has none. PRESERVE testid daily-plan-skeleton (HomeMobile.test.tsx:68); keep mono --muted; stats degrade ('—') when plan/stats null. | GAP | Null plan still renders daily-plan-skeleton and hides daily-plan-card (HomeMobile.test.tsx:66-70). |
| Traps Section wrapper (meta='Mönster', title='Dina fällor just nu', delay 320) | M3.tsx:831-843 + Section() | meta 'Mönster' (rail mono uppercase). title 'Dina fällor just nu' as .m3-h. delay 320. Section emits its own m3-rule top hairline. | WRONG | Traps render as a Section: 'MÖNSTER' rail + 'DINA FÄLLOR JUST NU' h2 + flat list. Live uses Eyebrow 'Återkommande fällor' (TopTrapsCard.tsx:32) + a 240px ink-2 divider (HomeMobile.tsx:279-286) — both replaced by Section chrome. |
| Trap row (.m3-trap) | M3.tsx:834-841 + CSS :274-296 | display:flex; align-items:baseline; justify-content:space-between; gap:24px; padding:13px 0; border-bottom:1px solid var(--hairline-2); :last-child 0. LEFT .m3-trap-t (14.5px; --ink) = <span class=m3-tag>{section}</span>{headline}. RIGHT .m3-trap-n (mono 12px --muted-2 tabular nowrap) = '{count} ggr'. | WRONG | Flat ink trap rows: tag+headline left, '{count} ggr' right — NOT a margin-rail row. Live uses hpc-m3-row with framework_id in cobalt rail (TopTrapsCard.tsx:74-105) — flatten to m3-trap two-column. CRITICAL: keep the whole row as <a href=`/drill?framework=${trap.framework_id}`> with data-testid='top-trap-link' (TopTrapsCard.tsx:68,76). |
| Trap framework_id surfacing | M3.tsx:836 (shows t.section in tag); fixtures.ts:132 id 'XYZ-TRAP-016' | M3 trap tag shows the SECTION, not framework_id. But live tests require framework_id VISIBLE (HomeMobile.test.tsx:187 'NOG-TRAP-007') AND href uses framework_id (:290). Reconcile: render trap.framework_id as the mono tag text, trap.headline as .m3-trap-t body, '{count} ggr' + TrendChip as .m3-trap-n. | GAP | Each trap row visibly shows framework_id (mono), headline, '{count} ggr'; href /drill?framework={framework_id}; HomeMobile.test.tsx:187-191,290 pass. |
| Trap trend chip (TrendChip) | no M3 equivalent (M3 trap shows only '{count} ggr') | Live TrendChip (TopTrapsCard.tsx:112-154) renders data-testid='trap-trend' with data-trend new/flat/up/down and ↑/↓/↔/ny, font-size 10-11, --muted/--ink-2. PRESERVE — append inside .m3-trap-n (mono register). Tests HomeMobile.test.tsx:213-271 pin data-trend + glyphs. | GAP | TrendChip renders inside the count cell with correct data-trend + glyph; trap-trend tests green; null-history hides it (TopTrapsCard.tsx:113-115). |
| Trap headline fallback | no M3 equivalent | Live falls back to 'Öva detta mönster →' when headline null (TopTrapsCard.tsx:103). Keep fallback copy (HomeMobile.test.tsx:194-211); render in .m3-trap-t. | GAP | Null headline shows 'Öva detta mönster' in the trap-t body. |
| Traps empty state (no traps → render nothing) | live TopTrapsCard.tsx:28 returns null when empty | PRESERVE: topTraps.length===0 → render NOTHING (no Section/eyebrow). Gate the entire trap Section on topTraps.length>0 in HomeMobile. HomeMobile.test.tsx:158-161 expects home-top-traps absent. | GAP | Empty topTraps hides the whole trap Section incl. its rail/rule; home-top-traps absent. |
| Section margin-rail meta color (.m3-meta) for all 4 sections | CSS M3.tsx:75-91 (color var(--muted)) vs live .hpc-m3-meta index.css:563-573 (color var(--accent)) | M3 home meta is mono 11px / ls 0.08em / uppercase / color var(--muted) / right / pad-top 5px / tabular; strong block --ink-2 weight 500. The EXISTING live .hpc-m3-meta is COBALT (--accent) because task 81 made the drill rail structural. Home wants --muted. Add a home-scoped --muted override or coordinate with M0's meta-color scope. | WRONG | All four rail labels (date, PÅBÖRJAD, plan minutes/moment, MÖNSTER) are --muted mono uppercase per M3 home, NOT cobalt; resolve the divergence with M0 before shipping. |
| Entrance choreography (m3-in-x / m3-in-y / m3-settle / m3-draw + delays) | CSS M3.tsx:84,95-96,108,145; keyframes :684-702; reduced-motion :709-716; delays M3.tsx:740,749 + 120/220/320 | meta m3-in-x 260ms; content m3-in-y 300ms delay+60ms; rule m3-draw 260ms; display m3-settle 480ms; Section delays 120/220/320ms. Live hpc-m3-in-x/in-y/draw exist (index.css:628-655); reduced-motion guard must cover .hpc-m3. | GAP | Sections animate with the staggered rail/content/rule choreography; respects prefers-reduced-motion. |
| Minimal-mast chrome (brand + visible nav + quiet picker corner) | no M3 equivalent (LOCKED decision; M3 HomeScreen has no top band) | ONE quiet hairline top band carrying ONLY: brand '⌜ HP-Coach', visible nav Hem/Övning/Lektion/Framsteg, a quiet settings/picker corner. NO section echo, NO bottom status line, NO exam-tag. Replace live Page runningHead+status (HomeMobile.tsx:137-143) and the MobileFrame desktop nav. Phone keeps the bottom tab bar (MobileFrame tabs) but NOT a running head. ⌘k NOT in the mast; delete 'statusHints=[⌘k palett]' (HomeMobile.tsx:123). | WRONG | Desktop shows one hairline mast: brand + 4 nav links + quiet picker corner only; no running head, status line, '⌘k palett' hint, or /2.0 echo; nav reachable without ⌘k. |
| Phone linearisation of the chassis (402px) | live @media(max-width:600px) index.css:611-626 | At ≤600px m3-row collapses to 1fr, spine hidden, meta becomes left-aligned eyebrow, section margin-top 44px. Ensure NEW home classes reflow: m3-stats gap 48px may overflow 402px — allow wrap; plan-item 32px\|1fr\|auto holds; resume band stacks CTA. No running head on phone. | GAP | At 402px the page is a clean single column; stats wrap if needed; rail labels become left eyebrows; resume CTA reachable; no horizontal scroll. |

**Data traps.**
- camelCase (M3 fixtures) vs live prop shapes: copy M3's CSS/px values, NOT its field accesses. M3 reads HOME.projectedScore (string '1.4'), HOME.scoreDelta (string), HOME.streakDays, HOME.estimatedMinutes, HOME.resume.{kind,section,position,total,device,when}, HOME.plan[].{kind('lektion'/'övning'),section,headline,rationale,minutes}, HOME.traps[].{id,section,count,headline}. Live equivalents DIFFER: projected:ProjectedTotal{total,verbal,quant:number|null}; streakDays:number; plan:DailyPlan{estimatedMinutes,items[]}; PlanItem.{kind('repetition'/'lesson'/'drill'),section,headline,rationale,estimatedMinutes,href,completed}; topTraps:TopTrap[]{framework_id,section,count,headline(nullable),trend}.
- PlanItem.kind is the ENGLISH enum {'repetition','lesson','drill'} (DailyPlanCard.tsx:207-214), NOT the fixture Swedish {'repetition','lektion','övning'} (fixtures.ts:107,116,123). Do not switch on Swedish kind values.
- TopTrap is snake_case framework_id (TopTrapsCard.tsx:44,68), not the fixture's 'id'. The drill deep-link /drill?framework=${framework_id} is pinned by HomeMobile.test.tsx:290.
- M3 DRILL fixtures use figureMeta/sectionLabel/total/contextTitle — irrelevant to Home but a reminder the fixture shape diverges from corpus; Home has no such fields. The week-over-week delta source (attempts7to14d/correct7to14d) lives on SectionStats (scoring.ts:22-31), only via stats.data.bySection in routes/index.tsx; it is NOT on SectionScore (scoring.ts:36-54) and NOT currently threaded into HomeMobile — new prop wiring required.
- Prognos decimal MISMATCH: M3 stats use COMMA one-decimal via svDecimal (M3.tsx:725-727, '1,4'); live formatScore is DOT two-decimal (scoring.ts:175-177, '1.40'). Mutually exclusive. home-score-line tests currently assert DOT decimals (HomeMobile.test.tsx:129-147) — MUST be rewritten this PR. formatScore (dot) MUST stay for /progress + scoring.test.ts:208-213; add formatScoreComma as NEW, do not mutate formatScore.
- Live formatSwedishHeader (dates.ts:43-47) emits 'Torsdag · 11 jun' (middot + 3-letter month), NOT the fixture 'Torsdag 11 juni' (full month, no middot). Author a full-month formatter or accept divergence — decide explicitly; do not assume formatSwedishHeader matches M3.
- Existing live .hpc-m3-meta is COBALT (--accent, index.css:568) because task 81 made the drill rail label structural; M3 HOME meta is --muted (M3.tsx:80). Reusing .hpc-m3-meta renders cobalt rail labels on Home (WRONG vs M3) — needs a home-scoped --muted override (coordinate with M0 meta-color scope).
- Resume 'when' is bare HH:MM in M3 (fixtures.ts:102 '11:07'); live relativeLabel prepends 'Idag · ' (useResumptionCandidate.ts:131). The M3 sub-line is 'fråga 4 av 10 · telefon · 11:07' (no 'Idag ·'). Reshaping the hook must surface the bare time or a recomposed sub-line, not relativeLabel verbatim.
- Resume device can be null (useResumptionCandidate.ts:112); the composed sub-line must drop the device segment gracefully (filter(Boolean).join(' · ')) as PhoneResumptionLine does (PhoneResumptionLine.tsx:24-26).
- The NEW home classes (.hpc-m3-stats/stat-n/l/d/resume*/plan-*/trap*/tag/frame/h/cta) are NOT yet in live index.css — only the chassis row/spine/meta/content/section/rule/display from task 81 are present (index.css:553-660). Port here unless M0 lands them first; confirm M0 ownership to avoid duplicate/conflicting CSS.

**Test contracts.**
- HomeMobile.test.tsx:128-135 (score line numeric + verbal + kvant, DOT '0.65'/'/ 2.0'/'verbal 0.81'/'kvant 0.49', no 'just nu') — REWRITE this PR: new stats row uses COMMA prognos ('1,4','prognos av 2,0') and no longer surfaces verbal/kvant as a mono line. New assertions: home-score-line holds comma prognos + 'prognos av 2,0' + (conditionally) delta. Mutually exclusive with the old dot line.
- HomeMobile.test.tsx:101-104 + 106-115 (null-gate: home-score-line ABSENT when projected null or both halves null) — KEEP null-gate but adapt to the stats block: when projected.total null the prognos shows '—'. Decide whether the stats ROW still renders (recommend: row always renders streak+min-idag, prognos '—', delta hidden) — if so these two cases change from 'absent' to 'shows —'. REWRITE all 3 score-line cases here preserving null-safety; document the new contract.
- HomeMobile.test.tsx:137-148 (em-dash missing halves, verbal 0.81 / kvant —) — tied to the OLD verbal/kvant mono line; since the stats row drops verbal/kvant, REWRITE or RETIRE. If verbal/kvant move to /progress only, delete here and ensure /progress covers it. State the decision in the PR.
- HomeMobile.test.tsx:79-82 (within(daily-plan-card) getByText('Repetition · 3 missar')/'NOG-lektion'/'KVA-drill · 10 frågor' as single nodes) — PRESERVE: m3-plan-t keeps item.headline contiguous so each resolves as ONE text node even with the inline m3-tag.
- HomeMobile.test.tsx:85-90 (eyebrow 'idag · ~18 min · uppskattat') — matches the OLD Eyebrow (DailyPlanCard.tsx:63). M3 plan header is '{N} min · 3 moment' (no 'idag', no '~', no 'uppskattat'). REWRITE to the new copy; recommend matching M3 and dropping the 'uppskattat' qualifier (owner sign-off).
- HomeMobile.test.tsx:163-192 (trap rows show framework_id 'NOG-TRAP-007','4 ggr', headline, second trap) — PRESERVE: m3-trap keeps framework_id + '{count} ggr' + headline visible.
- HomeMobile.test.tsx:194-211 ('Öva detta mönster' fallback) — PRESERVE in m3-trap-t.
- HomeMobile.test.tsx:213-271 (trap-trend data-trend down/new/unknown-hidden + glyphs) — PRESERVE TrendChip inside m3-trap-n.
- HomeMobile.test.tsx:273-291 (top-trap-link href '/drill?framework=NOG-TRAP-007') — PRESERVE the anchor + href.
- HomeMobile.test.tsx:66-70 (skeleton) / :92-97 (Klart för idag + 'Perfect game.') / :294-305 greeting / :307-317 no inline streak pill / :319-338 no manual Klar + no regenerate — PRESERVE all (reskin only).
- scoring.test.ts:208-213 (formatScore dot '1.62'/'2.00'/'—') — MUST STAY UNCHANGED; do not mutate formatScore. ADD: (a) formatScoreComma null→'—', 1.4→'1,4', 2→'2,0'; (b) computeProjectedDelta returns null when either week below threshold or sections mismatch, returns the signed comma week-over-week (or numeric + formatter) when both weeks ≥N attempts in shared sections. computeProjectedDelta is NEW, not a mutation of computeProjected/computeSectionScore.
- e2e (pnpm test:e2e, tests-e2e/): the Boksidan home was screenshotted in task 85 (85-home-*.png); the cross-device resumption Playwright drive reads home-resumption-link / home-resumption-line-phone testids — PRESERVE those through the ResumptionPanel reshape. Re-baseline home screenshots after the rebuild; confirm e2e green.

**Decisions applied.**
- Desktop SINGLE COLUMN .m3-frame (max-width 880px) — the 58/42 studio grid + right-column ResumptionPanel aside (HomeMobile.tsx:185-292) is DELETED; resumption moves into the single column as the 'Påbörjad' Section. Reverses the tasks 80-87 graft.
- Minimal-mast chrome: one quiet hairline top band with brand '⌜ HP-Coach' + visible nav Hem/Övning/Lektion/Framsteg + a quiet picker corner ONLY. No running head, status line, exam-tag, /2.0 echo, or '⌘k palett' hint. Replaces HomeMobile.tsx:137-143.
- ⌘K is a power-user accelerator, not nav. Navigation lives in the visible mast; the palette/edition/mode picker is destined for a real Settings surface, not the mast and not ⌘K-only. Remove 'statusHints=[⌘k palett]' (HomeMobile.tsx:123).
- Progress is textual: prognos number+label ('1,4' / 'prognos av 2,0'), delta textual ('sedan förra veckan'). NO visual progress bar.
- Score formatting split: COMMA one-decimal for the M3 stats row (new formatScoreComma); DOT two-decimal formatScore stays for /progress. The 3 home-score-line tests are rewritten this PR; scoring.test.ts dot cases untouched.
- Honest delta: computeProjectedDelta derives week-over-week from SectionStats.attempts7to14d/correct7to14d (NOT SectionScore.trend, NOT a hardcoded '+0.1'), rendered only when both weeks have ≥N attempts in shared sections, else BLANK. New function, not a mutation.
- Phone keeps the bottom tab bar for navigation and drops the running head; the focused-drill touch-exit decision belongs to M1, not M3H.
- Do NOT touch generateDailyPlan (scheduler) — M3H only restyles plan rendering and reads existing DailyPlan fields.
- Anti-drift: copy M3's CSS/px/clamp/letter-spacing/Swedish-copy verbatim from the cited M3.tsx lines; do not infer values from the old EDITION components.
- M3 reference governs the PAGE BODY only; the minimal mast is the agreed chrome wrapper (M3 HomeScreen has no top band) and is built to the LOCKED spec, not copied from M3.

**Definition of done.**
- ANTI-DRIFT GATE: M3 reference URL+state cited (/redesign-l12?dev=1&v=3&s=home) and the live Home diffed against it side-by-side at 1440px AND 402px in BOTH light and dark (4 screenshot pairs). Every elementChecklist row matched; every exact value (px/clamp/letter-spacing/color token/Swedish copy) copied verbatim from the cited M3.tsx lines.
- Desktop Home is a single ≤880px column (no 58/42 grid, no right-column aside); resumption renders inside the column as the 'Påbörjad' Section.
- Minimal mast renders: brand + Hem/Övning/Lektion/Framsteg nav + quiet picker corner; NO running head, status line, exam-tag, /2.0 echo, or ⌘k hint.
- Stats row: comma prognos '1,4' + 'prognos av 2,0' + honest computeProjectedDelta (blank below threshold) + streak 'dagar i rad' + 'min idag'; home-score-line testid lives on this block.
- Numbered cobalt plan items (m3-plan-item grid), inline section tags, contiguous headlines, --muted rationale, trailing mono minutes; completed/skeleton/complete states preserved + reskinned.
- Flat m3-trap rows with framework_id + headline + '{count} ggr' + TrendChip, whole row anchored to /drill?framework={framework_id}; empty-state hides the section.
- scoring.ts: formatScoreComma + computeProjectedDelta added (formatScore + computeProjected/computeSectionScore unchanged); scoring.test.ts adds coverage for both new functions; scoring.test.ts:208-213 unchanged.
- HomeMobile.test.tsx: the 3 score-line cases rewritten (comma stats + null-gate preserved); the plan eyebrow case rewritten to the M3 plan-header copy; all other testids/cases preserved (plan single-node headlines, trap rows, trap-trend, top-trap-link href, skeleton, complete, greeting, no-streak-pill, no-regenerate).
- All referenced data-testids present + green: daily-plan-card, daily-plan-item-{id}, daily-plan-link-{id}, daily-plan-complete, daily-plan-skeleton, home-top-traps, top-trap-row-{i}, top-trap-link, trap-trend, home-greeting, home-score-line, home-resumption-link, home-resumption-line-phone.
- pnpm test green; pnpm test:e2e green (home screenshots re-baselined; cross-device resumption drive passes via preserved testids).
- Required home-specific .hpc-m3-* CSS present (ported here or confirmed landed by M0, no duplication): frame, stats, stat-n/l/d, resume/resume-t/resume-s, cta, plan-item/n/t/r/min, tag, trap/trap-t/trap-n, h; rail meta resolves to --muted on Home (not cobalt).
- Owner approves the visual diff (light+dark, 1440+402) BEFORE merge. NO auto-merge label on this PR.
- No regression to /progress (formatScore dot path) or the scheduler (generateDailyPlan untouched).

**Dependencies.**
- M0 — Foundation: CSS gap fill + meta color (task 152). M3H needs the home-specific .hpc-m3-* classes and a resolution of the rail-meta color (live .hpc-m3-meta is cobalt; M3 home wants --muted). Confirm whether M0 lands the home classes; if not, port them in M3H. Sequence M0 before M3H or coordinate the CSS additions to avoid duplicate/conflicting blocks in index.css.
- M3 reference (READ-ONLY): /home/loucmane/dev/hpfetcher/app/src/components/devbake/l12/M3.tsx (HomeScreen 758-846, CSS const 33-727) and fixtures.ts (HOME 89-150) — source of truth for every value.
- routes/index.tsx wiring: must thread the date label + scoreDelta (computeProjectedDelta over stats.data.bySection) into HomeMobile, since the week-over-week source (SectionStats) is only available at the route. HomeMobile stays presentational/prop-driven (its tests have no QueryClient).
- useResumptionCandidate reshape is shared by ResumptionPanel AND PhoneResumptionLine — changing its output shape touches both; verify PhoneResumptionLine (and the cross-device e2e drive) still composes correctly. Independent of M1/M2 (drill) but must not break the shared hook.
- No dependency on M1/M2 (drill) or M4 (sections); M3H is the Home-only slice. M6 (cross-surface fidelity sweep) consumes M3H's output.

## M4 — M4 — Section apparatus (LÄS/NOG/XYZ/DTK) → M3 SectionDrillScreen fidelity

**Objective.** Bring the per-section drill apparatus (LÄS passage, DTK figure, NOG statements, XYZ math prompt, plus the shared VÄLJ SVAR + UTFALL rows) to byte-faithful parity with M3's SectionDrillScreen (M3.tsx:997-1224), running inside the M0 single-column .m3-frame chassis (LOCKED: desktop drill is SINGLE COLUMN, NOT StudyDesk). M0 lands most section CSS; M4 verifies each rule MATCHes M3, fills the rules M0 did not land (.hpc-m3-passage-h, .hpc-m3-missing, .is-pre, the is-prose opt-t/opt-k refinements), reconciles the one WRONG drift (white-space:pre-wrap on passage), wires the live data into each rail, fixes the ORD options-rail label (Välj synonym vs Välj svar), removes DrillQuestion's promptMeta override so section rails carry the static M3 label, and DROPs the m3-passage-h title because no live title source exists. M4 is NOT free to invent: copy M3's px/clamp/letter-spacing/color tokens, never M3's camelCase field accesses (the live corpus is snake_case and lacks several M3 fixture fields).

**Reference.** /redesign-l12?dev=1&v=3 with the q toggle set to each of las|nog|xyz|dtk in turn (renders M3 SectionDrillScreen, M3.tsx:997, dispatched from M3() M3.tsx:1226-1245 when screen!=='home' && drill!=='ord'). Capture each section in BOTH pre-answer (idle, picked===null) and graded (picked!==null) states. DTK is the deliberate missing-explanation case (fixturesSections.ts:505 explanation:null) — it must show the .m3-missing line, NOT crash. Reference data shape: fixturesSections.ts SECTION_DRILLS (fixturesSections.ts:53-507).

**Files to edit.**
- /home/loucmane/dev/hpfetcher/app/src/components/drill/DrillQuestion.tsx
- /home/loucmane/dev/hpfetcher/app/src/lib/nogPrompt.ts
- /home/loucmane/dev/hpfetcher/app/src/components/drill/QuestionFigure.tsx
- /home/loucmane/dev/hpfetcher/app/src/components/drill/PedagogyPanel.tsx
- /home/loucmane/dev/hpfetcher/app/src/lib/sectionRailLabel.ts (RAIL_CHOOSE / per-section options-rail label — needed to fix ORD 'Välj synonym')
- /home/loucmane/dev/hpfetcher/app/src/index.css (.hpc-m3-* section rules: add passage-h DROP-or-derive decision, .hpc-m3-missing, .is-pre, is-prose opt-t/opt-k refinements, reconcile passage white-space) — only if M0 left these gaps

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| Frame chassis = SINGLE COLUMN .m3-frame (max-width 880px, padding 64px 24px 120px per M0) | M3.tsx:1025 (.m3-frame wraps SectionDrillScreen); CSS .m3-frame M3.tsx:56-63 | max-width:880px; margin:0 auto; padding:64px 24px 120px (mobile @900px: padding:40px 18px 72px, M3.tsx:721). Live equiv landed by M0 as the drill container. | GAP | Section drill renders in one 880px-capped column at 1440; SessionPlayer routes section drills to DrillQuestion (single col), NOT StudyDesk 2-col. No right-hand pedagogy column. |
| Top meta row: m3-row (margin rail + spine + content), m3-meta strong=section + number/total, m3-eyebrow 'SECTIONLABEL · FRÅGA n AV total' | M3.tsx:1026-1039 (.m3-row/.m3-meta/.m3-spine/.m3-content/.m3-eyebrow); eyebrow uses question.sectionLabel.toUpperCase() + question.number + question.total (M3.tsx:1035) | eyebrow: font-mono 11px, letter-spacing 0.14em, uppercase, color var(--muted) (M3.tsx:127-134). DATA TRAP: M3 reads question.sectionLabel/number/total — LIVE Question has NONE of these (no sectionLabel, no total). Eyebrow ownership belongs to M1; M4 must NOT regress it. If M1 derives sectionLabel from section code + total from session length, reuse that; do not duplicate. | GAP | Eyebrow reads e.g. 'KVANTITATIVA RESONEMANG · FRÅGA 1 AV 12' for NOG; value source documented (derived, since live data lacks sectionLabel/total). Owned by M1 — M4 only confirms it survives in section drills. |
| LÄS/ELF passage rail: meta='Texten' (→ uppercase TEXTEN), m3-passage wrapper, m3-passage-h TITLE, m3-passage p paragraphs split on \n\n | M3.tsx:1041-1050 (Section meta=Texten, m3-passage, h2.m3-passage-h={question.contextTitle}, context.split('\n\n')); CSS .m3-passage-h M3.tsx:564-571, .m3-passage p M3.tsx:573-583 | passage-h: font-display, weight 600, 21px, line-height 1.3, color var(--ink), margin 0 0 14px. passage p: font-display, 16.5px, weight 400, line-height 1.7, color var(--ink-2), margin 0 0 14px, max-width 70ch; last-child margin-bottom 0. DECISION: DROP m3-passage-h — live Question has NO contextTitle field and no title source (data trap below). Render passage body only. Rail label 'Texten' already correct in sectionRailLabel.ts:41-42. | GAP | LÄS renders TEXTEN rail + body paragraphs at 16.5px/1.7/70ch matching M3; NO h2 title (dropped). drill-context testid present on body div only (DrillQuestion.tsx:129). Split uses /\n{2,}/ (live) — equivalent to M3's '\n\n'. |
| Passage paragraph white-space — DRIFT to reconcile | M3.tsx:573-583 (.m3-passage p has NO white-space property) | Live index.css:689 adds white-space:pre-wrap to .hpc-m3-passage p — NOT in M3. This is a drift carried from EDITION. Remove it so wrapping matches M3 (paragraphs already pre-split on blank lines; pre-wrap re-introduces intra-paragraph hard breaks the corpus does not want). | WRONG | index.css .hpc-m3-passage p has no white-space:pre-wrap; LÄS passage wraps identically to M3 at 1440 and 402. (Confirm no single-newline-bearing passage relies on pre-wrap before removing; if one does, normalise at split instead.) |
| DTK figure rail: meta='Underlaget' (→ UNDERLAGET), figure.m3-fig wrapping QuestionFigure | M3.tsx:1052-1058 (Section meta=Underlaget; figure.m3-fig; <QuestionFigure figure={question.figureMeta}/>); CSS .m3-fig M3.tsx:634-657 | m3-fig: margin 0; padding 0; background none; border none. Inner [data-testid='question-figure']: border/border-radius/background/padding all none !important (M3.tsx:645-650). m3-fig img: display block, width 100%, height 100%, object-fit contain (M3.tsx:652-657). DATA TRAP: M3 fixture field is figureMeta; LIVE Question field is `figure` (questions.ts:69, DrillQuestion.tsx:138 already reads question.figure). Rail label 'Underlaget' correct (sectionRailLabel.ts:48; DrillQuestion.tsx:139 hardcodes 'Underlaget'). | MATCH | DTK renders UNDERLAGET rail + figure with NO inner card frame (border/radius/bg/padding stripped); reads as the page. DrillQuestion.tsx:138-144 already uses question.figure + .hpc-m3-fig — index.css:840-857 matches M3:634-657. Confirm parity, no edit unless M0 missed a rule. |
| DTK / any section with explanation null: m3-missing graceful line in UTFALL | M3.tsx:1145-1147 (explanation null → <p className='m3-missing'>Förklaring saknas ännu för den här frågan.</p>); CSS .m3-missing M3.tsx:671-680 | m3-missing: font-display, italic, 16px, line-height 1.5, color var(--muted), margin 22px 0 0, max-width 60ch, animation hpc-m3-in-y 320ms ...200ms both. Copy as .hpc-m3-missing. Swedish copy verbatim: 'Förklaring saknas ännu för den här frågan.' DATA TRAP: M3 reads SECTION_DRILLS[drill].explanation (can be null, fixturesSections.ts:505); LIVE path is PedagogyPanel(qid) → loadExplanation returns null. PedagogyPanel ALREADY owns a missing-explanation state (task 102) — verify it renders this exact copy + .hpc-m3-missing styling, or reconcile it. | GAP | DTK graded shows italic muted 'Förklaring saknas ännu för den här frågan.' (or PedagogyPanel's task-102 equivalent re-styled to .hpc-m3-missing), does NOT crash, NO empty step/distractor blocks. .hpc-m3-missing exists in index.css matching M3:671-680. |
| NOG prompt rail: meta='Uppgiften' (→ UPPGIFTEN), m3-q prompt (the stem), pre-grade m3-tactic aside | M3.tsx:1060-1070 (Section meta = drill==='nog'?'Uppgiften':'Frågan'; p.m3-q MathText prompt; pre-grade aside.m3-tactic with pregradeTactic.handle/move); CSS .m3-q M3.tsx:585-595 | m3-q: font-display, weight 500, font-size clamp(22px,3vw,27px), line-height 1.35, letter-spacing -0.005em, color var(--ink), margin 0, max-width 60ch, animation m3-settle. Rail 'Uppgiften' correct (sectionRailLabel.ts:45 NOG.promptLabel='Uppgiften'). DATA TRAP: M3 reads explanation.pregradeTactic (camelCase); LIVE is pregrade_tactic (explanations.ts:101, PedagogyPanel.tsx:309-312). The pre-grade tactic aside is M1's element — M4 must not re-implement, only ensure NOG's stem (not the whole flat prompt) feeds m3-q. | GAP | NOG shows UPPGIFTEN rail + stem 'Alvar, Benjamin... Vem av dem är äldst?' at m3-q scale; the flat (1)/(2) text is NOT in the prompt (split out below). promptStem = parseNogPrompt(prompt).question (DrillQuestion.tsx:94-95). |
| NOG statements rail: meta='Påståenden' (→ PÅSTÅENDEN), m3-stmt rows ((n) marker + statement text), is-pre multi-line support | M3.tsx:1072-1083 (Section meta=Påståenden; per s: div.m3-stmt > span.m3-stmt-n '(n)' + p.m3-stmt-t s.text); CSS .m3-stmt M3.tsx:597-605, .m3-stmt-n M3.tsx:607-613, .m3-stmt-t M3.tsx:615-622 | m3-stmt: grid 44px 1fr, gap 14px, padding 15px 0, border-bottom 1px var(--hairline-2); last-child border-bottom 0. m3-stmt-n: font-mono 12px, color var(--muted-2), tabular-nums, padding-top 4px. m3-stmt-t: font-display 17.5px, line-height 1.55, color var(--ink), margin 0, max-width 65ch. Rail 'Påståenden' = RAIL_STATEMENTS (sectionRailLabel.ts:27, DrillQuestion.tsx:167). DATA TRAP: M3 fixture has question.statements:[{n,text}] PRE-SPLIT (fixturesSections.ts:196-205); LIVE has only a flat prompt string parsed at runtime by parseNogPrompt (nogPrompt.ts:29-43, DrillQuestion.tsx:94). NOTE M3 uses '(n)' but live renders fixed '(1)'/'(2)' (DrillQuestion.tsx:169,175) — matches the 2-statement corpus. | GAP | NOG shows PÅSTÅENDEN rail with two grid rows '(1)'/'(2)' at 17.5px/1.55/65ch; bottom hairlines per row; numbers in mono 12px muted-2. Live index.css:696-723 matches M3:597-622 (verify M0 landed; NOTE live adds .hpc-m3-stmt:first-child border-top:1px hairline at index.css:703-705 which M3 lacks — flag as minor add, keep or drop per owner). |
| NOG coda: m3-coda above options ('Tillräcklig information för lösningen erhålls') | M3.tsx:1086 (question.coda!==null → p.m3-coda); CSS .m3-coda M3.tsx:624-632 | m3-coda: font-display, italic, 16px, line-height 1.5, color var(--ink-2), margin 0 0 10px (LIVE index.css:730 uses 0 0 12px — reconcile to M3's 10px), max-width 65ch. DATA TRAP: M3 reads question.coda (fixturesSections.ts:206); LIVE derives it as nog.tail via NOG_TAIL regex (nogPrompt.ts:20,36-39, rendered DrillQuestion.tsx:184). Swedish copy: 'Tillräcklig information för lösningen erhålls'. | GAP | NOG coda renders italic above options; margin-bottom reconciled to M3's 10px (index.css:730 currently 12px). Lives inside the VÄLJ SVAR rail above the options (DrillQuestion.tsx:183-184), matching M3:1085-1086. |
| XYZ prompt rail: meta='Frågan' (→ FRÅGAN), m3-q with MathText LaTeX prompt | M3.tsx:1060-1063 (drill!=='nog' → meta='Frågan'; p.m3-q > MathText prompt) | Same m3-q values as NOG stem (clamp(22px,3vw,27px)/1.35/-0.005em/60ch). XYZ prompt '(x + 3)(x - 5)' contains LaTeX-marked text → MUST go through MathText (DrillQuestion.tsx:157,161). Rail 'Frågan' = XYZ.promptLabel (sectionRailLabel.ts:43). DATA TRAP: M3 fixture options carry raw LaTeX 'x^{2} - 2x - 15' (fixturesSections.ts:343); live options likewise need MathText in BOTH the option rows AND the struck distractor text (M2 dependency). | GAP | XYZ shows FRÅGAN rail + prompt with KaTeX-rendered (x+3)(x-5); options render x²−2x−15 via MathText, not raw braces. drill-prompt testid on the .hpc-m3-q element. |
| Prompt rail label override REMOVAL — promptMeta drift | M3.tsx:1060 (section rails ALWAYS show static 'Frågan'/'Uppgiften'); no <strong>{section}</strong> form on section drills | DrillQuestion.tsx:105-112 currently overrides meta.promptLabel with <strong>{section}</strong>(+'synonymer' for ORD) when promptIsShort (len<=18). For sections this produces e.g. '<strong>XYZ</strong>' on the prompt rail instead of M3's 'FRÅGAN'. Restrict the <strong>section</strong> form to ORD only (the headword specimen, M1's domain); all other sections use meta.promptLabel verbatim. | WRONG | XYZ prompt rail reads 'FRÅGAN' (not 'XYZ'); NOG reads 'UPPGIFTEN'; LÄS/DTK read 'FRÅGAN'. Only ORD keeps the section+synonymer rail form. promptIsShort no longer leaks the strong-section label into section drills. |
| Options rail: meta — ORD='Välj synonym', sections='Välj svar' | M3.tsx:895 (ORD DrillScreen: Section meta='Välj synonym') vs M3.tsx:1085 (SectionDrillScreen: meta='Välj svar') | Swedish copy verbatim: ORD → 'Välj synonym'; all sections → 'Välj svar'. LIVE uses RAIL_CHOOSE='Välj svar' for EVERY section incl ORD (sectionRailLabel.ts:25, DrillQuestion.tsx:183) — ORD is WRONG. Add a per-section choose label (e.g. SectionRailMeta.chooseLabel, ORD='Välj synonym' else 'Välj svar') or branch in DrillQuestion on section==='ORD'. | WRONG | ORD options rail reads 'VÄLJ SYNONYM'; XYZ/NOG/LÄS/DTK read 'VÄLJ SVAR'. Matches M3:895 vs M3:1085 exactly. |
| Options block: m3-opts, is-prose modifier for LÄS prose answers, option rows (ind/opt-k/opt-t/opt-v) | M3.tsx:1087-1116 (div.m3-opts + ' is-prose' when drill==='las'; per option button m3-opt with span.m3-ind/m3-opt-k(lowercase letter)/m3-opt-t(MathText)/m3-opt-v); CSS m3-opts M3.tsx:324-404, is-prose M3.tsx:659-667 | is-prose: .m3-opt align-items:start (M3.tsx:659); .m3-opt-t font-size 16px, line-height 1.55, max-width 65ch (M3.tsx:661-665); .m3-opt-k padding-top 17px (M3.tsx:667). LIVE landed only .hpc-m3-opts.is-prose .hpc-m3-opt{align-items:start} (index.css:835-837) — MISSING the opt-t (16/1.55/65ch) + opt-k (padding-top:17px) refinements. is-prose toggled by meta.optionsProse (sectionRailLabel.ts: LÄS/ELF true:41-42, else false). Option keys lowercased (DrillQuestion.tsx:262). M3 applies is-prose only to LÄS; live applies to LÄS+ELF (acceptable — both prose). | GAP | LÄS options wrap as prose at 16px/1.55/65ch with key top-aligned (padding-top 17px); add the two missing is-prose rules to index.css matching M3:661-667. XYZ/NOG options use compact 18px display (non-prose). Grading state colours (is-ok/is-bad/is-dim) unchanged from M0. |
| Option verdict caption: m3-opt-v 'Rätt svar' / 'Ditt svar' | M3.tsx:1110-1112 (graded&&isAnswer→'Rätt svar'; graded&&isPick→'Ditt svar'); CSS m3-opt-v M3.tsx:378-384 | m3-opt-v: font-mono 11px, letter-spacing 0.1em, uppercase, color var(--muted-2). Swedish verbatim 'Rätt svar'/'Ditt svar'. LIVE OptionRow already does this (DrillQuestion.tsx:251,266) + index.css:794-802 matches. | MATCH | Graded section drill: correct row shows 'RÄTT SVAR', wrong pick shows 'DITT SVAR'. Confirm parity, no edit. |
| Keys hint: m3-keys 'Tangenter a–{lastKey} väljer · klick fungerar också' (pre-grade only) | M3.tsx:1117-1119 (!graded → div.m3-keys, lastKey from options.length M3.tsx:1003); CSS m3-keys M3.tsx:406-414 | Swedish: 'Tangenter a–e väljer · klick fungerar också' (e for 5-option NOG; d for 4-option LÄS/XYZ/DTK — lastKey is dynamic per option count). m3-keys: font-mono 12px etc. This is M1's element (keys hint) — M4 only confirms it renders per-section with the correct lastKey (a–d for 4-opt, a–e for 5-opt NOG). | GAP | NOG (5 opts) hint reads 'a–e'; XYZ/LÄS/DTK (4 opts) read 'a–d'. Owned by M1; M4 confirms dynamic lastKey across sections. Hint disappears once graded. |
| UTFALL rail (graded): m3-ped wrapper, m3-verdict word + sub, m3-solution OR m3-missing | M3.tsx:1122-1148 (graded → div.m3-ped; Section meta='Utfall'; m3-verdict-word 'Rätt.'/'Fel.'; m3-verdict-sub; explanation? m3-solution : m3-missing) | verdict-sub correct: 'Snyggt — rätt tänkt hela vägen.' (M3.tsx:1131, NOTE differs from ORD's 'taktiken höll hela vägen' M3.tsx:938). verdict-sub wrong: 'Rätt svar är {answer.toLowerCase()}) {correctOption.text}. {explanation?'Häng med i varför.':''}' (M3.tsx:1133-1137). m3-solution: MathText(explanation.solution). DATA TRAP: M3 reads explanation.solution; LIVE is solution_path (explanations.ts:73) rendered by PedagogyPanel. UTFALL content is M2's rewrite domain (verdict + solution + steps + distractors); M4 confirms it renders correctly for SECTION questions (LaTeX in XYZ solution, prose in LÄS, missing in DTK). | GAP | Each section graded shows UTFALL rail = RAIL_OUTCOME (sectionRailLabel.ts:26, DrillQuestion.tsx:199); verdict word + correct-answer restatement reprinting the correct option's text via MathText; solution from solution_path. DTK falls to m3-missing. Bulk of styling owned by M2 — M4 verifies section data feeds it. |
| Graded steps walkthrough: m3-step rows with is-pre multi-line text (NOG steps contain embedded \n) | M3.tsx:1152-1179 (Section title='Så löser du den'; per step m3-step-n + m3-step-h(title + tier 'kärna'/'detalj') + p.m3-step-t.is-pre MathText); CSS .m3-step-t.is-pre M3.tsx:669 (white-space:pre-line) | .m3-step-t.is-pre{white-space:pre-line} — REQUIRED because NOG step 9 text embeds newlines 'B. \n  Från (1): C > A > B.\n  Från (2): B > D > E.' (fixturesSections.ts:271). LIVE index.css has NO .is-pre rule (grep confirms absent) and PedagogyPanel step text (PedagogyPanel.tsx:547-559) has no white-space:pre-line — multi-line NOG steps collapse. Add white-space:pre-line to the live step-text element (PedagogyPanel inline style or a .hpc-m3-step-t.is-pre class). DATA TRAP: M3 reads explanation.steps; LIVE is steps?:ExplanationStep[] (explanations.ts:81) with optional title/tier — guard for absent steps. Step rendering is M2's domain; M4 ensures NOG's newline-bearing steps render as separate lines. | GAP | NOG graded: step 9's 'Från (1)…' / 'Från (2)…' render on separate lines (white-space:pre-line applied). XYZ step LaTeX renders via MathText. tier badge shows 'kärna'/'detalj'. Owned jointly with M2; M4 owns the is-pre/pre-line fix. |
| Graded distractors: m3-dis with STRUCK option text reprinted (m3-dis-h s + whyTempting/whyWrong) | M3.tsx:1181-1210 (Section title='Varför de andra lockar'; per d: p.m3-dis-h > span.m3-dis-k '{letter})' + <s>{d.text}</s>; 'Varför det lockar'/p m3-dis-p whyTempting; 'Varför det är fel'/p whyWrong); CSS m3-dis M3.tsx:513-553 | m3-dis-h: font-display 600 17px; m3-dis-k: font-mono 12px muted-2 mr 8px; <s> text-decoration-thickness 1.5px; m3-dis-l: font-mono 11px 0.12em uppercase color var(--accent); m3-dis-p: 14.5px/1.6 var(--ink-2) max-width 75ch. Swedish labels verbatim 'Varför det lockar' / 'Varför det är fel'. CRITICAL DATA TRAP (load-bearing): M3 reprints <s>{d.text}</s> (the distractor's OPTION TEXT, M3.tsx:1196) — but LIVE DistractorExplanation has ONLY {letter, why_tempting, why_wrong} (explanations.ts:26-30), NO text field. The struck text MUST be joined: question.options.find(o=>o.letter===d.letter).text, rendered via MathText (so XYZ x²−15 / LÄS full-sentence distractors strike correctly). This replaces PedagogyPanel's current letter-only block (PedagogyPanel.tsx:600 renders only 'a.' + Lockar/Men fel prose). This is the M2 rewrite; M4 verifies it across sections (LaTeX strike on XYZ, long-prose strike on LÄS). | WRONG | XYZ graded distractor A reprints struck 'x²−15' (via MathText) before the lockar/fel prose; LÄS distractor B reprints the full struck sentence from options. Letter prefix mono. This is M2's deliverable — M4 confirms the option-text join works for every section's distractor letters and that no distractor 404s when a letter has no matching option. |
| Nästa fråga CTA + Enter hint (graded): m3-next-row m3-cta + m3-keys 'Enter går vidare' | M3.tsx:1214-1219 (div.m3-next-row > button.m3-cta 'Nästa fråga' + span.m3-keys 'Enter går vidare'); CSS m3-next-row M3.tsx:555-560, m3-cta M3.tsx:202-218 | m3-next-row: margin-top 36px, flex, gap 16px. m3-cta: font-ui 13px 600, color var(--accent-ink), bg var(--accent), padding 10px 20px. Swedish 'Nästa fråga' + 'Enter går vidare'. DECISION: phone gets a FLOATING bottom-center 'Nästa fråga' pill (LOCKED phone drill decision) — owned by M1/M2 advance control; M4 confirms section graded state surfaces the advance affordance and does not double-render it. | GAP | Section graded shows 'Nästa fråga' advance (desktop inline m3-next-row; phone floating pill per LOCKED decision). Advance owned upstream (M1/M2) — M4 verifies it appears in every section's graded state, incl DTK (missing-explanation still advances). |
| Phone (402px) section behavior: linear, bare (no running head), floating Nästa pill, visible touch exit | M3.tsx:719-722 (@media max-width:900px: .m3-row grid 96px 1px 1fr, .m3-frame padding 40px 18px 72px) | Mobile rail compresses to 96px margin. LOCKED: phone drill = linear + bare (no running head) + floating bottom-center 'Nästa fråga' pill reachable without scroll + a VISIBLE touch exit (back/close — Esc/⌘K do not count). The touch-exit + floating pill are M1 deliverables; M4 confirms LÄS long passage + DTK figure + NOG statements all linearise correctly at 402 and the exit/pill remain reachable. | GAP | At 402px: LÄS passage, NOG statements, DTK figure each stack rail-label-above-content; floating Nästa pill present when graded; visible touch exit present in focused drill. M4 verifies section content does not break these M1-owned affordances. |

**Data traps.**
- figure field name: M3 fixture uses question.figureMeta (fixturesSections.ts:48); LIVE Question uses `figure` (questions.ts:69). DrillQuestion.tsx:138 already reads question.figure — do NOT introduce figureMeta. Copy M3's .m3-fig CSS values, not its field name.
- contextTitle has NO live source: M3 renders h2.m3-passage-h={question.contextTitle} (M3.tsx:1044) from a fixture-only field (fixturesSections.ts:83 'Kopplingen till Medelhavet'). LIVE Question has no contextTitle and no derivable title. DECISION: DROP the m3-passage-h title entirely (do not invent one, do not reuse section name). Render passage body only.
- sectionLabel/total absent: eyebrow needs question.sectionLabel + question.total (M3.tsx:1035); LIVE Question has neither (questions.ts:55-69). Section drills must derive sectionLabel from the section code and total from session length — coordinate with M1 (which owns the eyebrow); do not hardcode '12'.
- NOG statements are runtime-parsed, not pre-split: M3 fixture has question.statements:[{n,text}] + question.coda already separated (fixturesSections.ts:196-206). LIVE has only a flat prompt string; parseNogPrompt (nogPrompt.ts:29-43) splits it via U+E000/U+E001 PUA markers and the canonical tail regex. The PUA markers MUST stay as \uXXXX escapes (nogPrompt.ts:14 comment — the editor harness strips literal PUA). parseNogPrompt returns null when split fails → fall back to inline prompt (no statements rail).
- distractor has NO text field (LOAD-BEARING): M3 reprints <s>{d.text}</s> (M3.tsx:1196). LIVE DistractorExplanation = {letter, why_tempting, why_wrong} only (explanations.ts:26-30). The struck option text must be JOINED from question.options.find(o=>o.letter===d.letter).text and run through MathText. This is M2's job; M4 depends on it landing for section drills (XYZ LaTeX, LÄS prose).
- explanation field names are snake_case: M3 uses explanation.pregradeTactic / .whyTempting / .whyWrong / .solution (M3.tsx:1066,1201,1204,1143). LIVE is pregrade_tactic / why_tempting / why_wrong / solution_path (explanations.ts:73,101; PedagogyPanel.tsx:309,611,622). Copy M3's px/color, never its field access.
- explanation can be null (DTK): fixturesSections.ts:505 explanation:null is deliberate. LIVE loadExplanation returns null for un-authored qids. Must render .hpc-m3-missing ('Förklaring saknas ännu för den här frågan.'), never crash on null.steps / null.distractors.
- passage white-space drift: live .hpc-m3-passage p has white-space:pre-wrap (index.css:689) NOT present in M3 (.m3-passage p M3.tsx:573-583). Remove to match M3 wrapping.
- promptMeta override drift: DrillQuestion.tsx:105-112 swaps the static rail label for <strong>{section}</strong> on short prompts — leaks 'XYZ'/'DTK' onto section prompt rails. M3 section rails always show static 'Frågan'/'Uppgiften'. Restrict the strong-section form to ORD.
- options-rail label: live RAIL_CHOOSE='Välj svar' applies to ALL sections incl ORD (sectionRailLabel.ts:25). M3 ORD uses 'Välj synonym' (M3.tsx:895), sections use 'Välj svar' (M3.tsx:1085). ORD is wrong — add per-section chooseLabel.

**Test contracts.**
- data-testid='drill-context' must stay on the LÄS/ELF passage BODY div, not the rail section (DrillQuestion.tsx:129 comment) — e2e reads its textContent and the mono rail label would pollute it. Dropping m3-passage-h must not move or remove this testid. (Referenced by drill-variants StyleA/B/C and any passage e2e.)
- data-testid='drill-prompt' must remain on the prompt element (.hpc-m3-display for ORD, .hpc-m3-q for sections) carrying ONLY the headword/stem (DrillQuestion.tsx:152,156,160). mistakes.spec.ts:63,110 and drill.spec.ts:65 read drill-prompt.textContent to resolve the answer — for NOG it must be the STEM (parseNogPrompt.question), never the full flat prompt with statements. ShareDebugButton.tsx:120,288 also queries drill-prompt.
- data-testid='question-figure' on the DTK figure button (QuestionFigure.tsx:83,144) is targeted by the index.css .hpc-m3-fig [data-testid='question-figure'] frame-strip rule (index.css:846) — keep the testid stable when editing QuestionFigure.
- data-testid='pedagogy-distractor-{letter}' (PedagogyPanel.tsx:577) must survive the struck-option-text rewrite; any distractor-block e2e asserting these IDs must still pass after M2's reprint lands.
- explanations.test.ts:13 constructs a DistractorExplanation with NO text field ({letter,why_tempting,why_wrong}) — the struck-text join must source text from question.options, NOT expect a distractor.text field, or this unit test (and the live type) breaks.
- Run pnpm test (vitest) — covers nogPrompt parsing, sectionRailLabel mapping, explanations type. Add/extend a unit test asserting railMeta('ORD').chooseLabel==='Välj synonym' and section chooseLabel==='Välj svar' if the label is added to SectionRailMeta.
- Run pnpm test:e2e (playwright) at all three viewports — drill.spec.ts + mistakes.spec.ts exercise drill-prompt/drill-context; confirm NOG/LÄS/DTK/XYZ still answerable and graded after the rail/label changes.

**Decisions applied.**
- Desktop drill = SINGLE COLUMN .m3-frame (880px), NOT StudyDesk 2-col — section drills must route to the single-column DrillQuestion chassis; reverses tasks 80-87.
- Phone drill = linear + bare (no running head) + floating bottom-center 'Nästa fråga' pill + VISIBLE touch exit — M4 verifies LÄS/NOG/DTK/XYZ content does not break these M1-owned phone affordances.
- M2 reprints each distractor's struck OPTION TEXT (M3.tsx:1196) joined from question.options — load-bearing for single-column safety; M4 verifies it across XYZ (LaTeX) and LÄS (prose) distractors and replaces the live letter-only block (PedagogyPanel.tsx:600).
- Progress = textual eyebrow ('SECTIONLABEL · FRÅGA n AV total'); NO progress bar — section drills inherit M1's eyebrow.
- camelCase-vs-snake_case is systemic: copy M3's CSS/px values, never its field accesses (figureMeta→figure, pregradeTactic→pregrade_tactic, whyTempting/whyWrong→why_tempting/why_wrong, solution→solution_path; no live sectionLabel/total/contextTitle).
- Anti-drift: every element diffed against M3 SectionDrillScreen at /redesign-l12?dev=1&v=3 per q-toggle; copy exact px/clamp/letter-spacing/tokens; no grafting accents onto EDITION leftovers.
- DROP m3-passage-h: no live title source (contextTitle has none) — do not invent or derive a passage title.
- Fix ORD options-rail label to 'Välj synonym' (M3.tsx:895); sections keep 'Välj svar' (M3.tsx:1085).

**Definition of done.**
- ANTI-DRIFT GATE: M3 reference URL+state cited (/redesign-l12?dev=1&v=3 for each of q=las|nog|xyz|dtk, SectionDrillScreen M3.tsx:997) and a live+M3 screenshot captured at 1440px AND 402px in BOTH light AND dark for each of the four sections in BOTH pre-answer and graded states.
- Every elementChecklist row resolved to MATCH against M3 with exact px/clamp/letter-spacing/color-token/Swedish-copy values copied verbatim — no approximations.
- M0-landed section CSS verified MATCH; the M0 gaps filled in index.css: .hpc-m3-missing (M3:671-680), .hpc-m3-step-t.is-pre / pre-line (M3:669), .hpc-m3-opts.is-prose .hpc-m3-opt-t (16px/1.55/65ch, M3:661-665) and .is-prose .hpc-m3-opt-k (padding-top:17px, M3:667).
- DRIFT reconciled: .hpc-m3-passage p white-space:pre-wrap removed (index.css:689); promptMeta strong-section override restricted to ORD (DrillQuestion.tsx:105-112); ORD options rail = 'Välj synonym', sections = 'Välj svar'; m3-coda margin reconciled to 10px.
- DTK missing-explanation renders the italic muted 'Förklaring saknas ännu för den här frågan.' (.hpc-m3-missing) and DOES NOT crash on null explanation/steps/distractors.
- Struck distractor option text (M2 dependency) verified reprinting via MathText for XYZ (LaTeX) and LÄS (full-sentence prose) with letter joined to question.options — no distractor.text field assumed.
- NOG: stem-only in drill-prompt, (1)/(2) statements in PÅSTÅENDEN rail with bottom hairlines, coda above options, multi-line step 9 renders on separate lines (pre-line), 5-option keys hint 'a–e'.
- All data-testids (drill-context, drill-prompt, question-figure, pedagogy-distractor-{letter}) stable and located per their comments.
- pnpm test green AND pnpm test:e2e green at all three viewports.
- Owner approves the visual diff before merge — NO auto-merge label on this PR.

**Dependencies.**
- M0 — Foundation (task 152): lands the .m3-frame single-column chassis + most .hpc-m3-* section CSS (passage/stmt/coda/fig/opts/verdict/solution). M4 verifies and fills its gaps (missing/is-pre/is-prose refinements). HARD dependency — M4 cannot diff section apparatus until M0's chassis exists.
- M1 — Drill pre-answer (task 153): owns the eyebrow (SECTIONLABEL · FRÅGA n AV total), pre-grade m3-tactic aside, keys hint, phone floating Nästa pill + visible touch exit. M4 confirms these survive per-section; does NOT re-implement them.
- M2 — Drill graded / pedagogy rewrite (task 154): owns the UTFALL verdict/solution/steps/distractor rewrite INCLUDING the load-bearing struck-option-text reprint joined from question.options. M4 depends on M2's distractor reprint and verifies it across sections. HARD dependency for the graded-distractor checklist row.
- sectionRailLabel.ts: source of rail labels (Texten/Underlaget/Frågan/Uppgiften/Påståenden) — extend with a per-section options-rail chooseLabel for the ORD 'Välj synonym' fix.
- nogPrompt.ts: parseNogPrompt is the NOG apparatus splitter (PUA markers + tail regex) — relied on for stem/statements/coda; keep \uXXXX PUA escapes intact.
- PedagogyPanel.tsx: owns the graded UTFALL content live; the struck-text reprint, is-pre step rendering, and missing-explanation state are reconciled here (jointly with M2).

## M5 — M5 — Carry-over: diagnostik / auth / repetition-idle / lektion adopt the muted chassis + minimal-mast chrome (no EDITION leftovers)

**Objective.** Make the four carry-over surfaces inherit the M1–M3 Boksidan vocabulary (muted/spalt chassis tokens, minimal-mast chrome) so NONE of them still wears the EDITION shell — the Page running-head+folio+status-line band, the doubled orientation (section echo + bottom status line), the AuthLayout exam-tag flourish, or the lektion 'esc tillbaka · ⌘k palett' hint rail. These surfaces have no dedicated M3 reference screen; each borrows its chrome from the drill (SessionPlayer phases / BoksidanDesk) or the home (HomeScreen). The charge is TARGETED FIXES ONLY: harmonize chrome and remove EDITION residue, do not redesign inner content (DiagnosticReport, Clerk card, LessonReader, idle masthead bodies stay as-is except where they carry EDITION chrome). Also close the Spalt-on-welcome gap (welcome.tsx omits the default palette from the picker) and document the picker→Settings migration as a deferred sub-item. Drift-proofing: every surface gets a side-by-side screenshot against the drill/home it borrows from, at 1440 and 402, light and dark.

**Reference.** No dedicated M3 screen exists for these surfaces. M3 truths to borrow: (1) M3 chrome decision = MINIMAL MAST (locked): one quiet hairline top band carrying ONLY brand (⌜ HP-Coach) + visible nav (Hem/Övning/Lektion/Framsteg) + a quiet picker corner; NO section echo, NO bottom status line, NO exam-tag flourish. M3.tsx itself ships NO masthead (HomeScreen M3.tsx:758-846, DrillScreen M3.tsx:848-995, SectionDrillScreen M3.tsx:997+ render only the .m3-frame body; the mast is the locked decision delivered live in M1/M3H). (2) M3 chassis = .m3-frame max-width 880px single column, .m3-row 128px|1px|1fr margin-rail grid col-gap 28px (M3.tsx:56-68), muted/spalt tokens. Live render to diff against: the M1/M2/M3H/M4 drill at /drill and home at / once those land. Diagnostik done-screen already uses the ported .hpc-m3-* chassis (DiagnosticReport.tsx:286-298); the gap is the idle + active drill phases routed through SessionPlayer's EDITION Page shell.

**Files to edit.**
- /home/loucmane/dev/hpfetcher/app/src/routes/diagnostik.tsx
- /home/loucmane/dev/hpfetcher/app/src/routes/sign-in.tsx
- /home/loucmane/dev/hpfetcher/app/src/routes/lektion.tsx
- /home/loucmane/dev/hpfetcher/app/src/routes/welcome.tsx (Spalt-gap sub-item)
- /home/loucmane/dev/hpfetcher/app/src/components/session/SessionPlayer.tsx (shared chrome for diagnostik + repetition idle/done — EDITION Page shell removal; coordinate with M1)
- /home/loucmane/dev/hpfetcher/app/src/components/AuthLayout.tsx (TopBand exam-tag + ↘ flourish — shared by sign-in + sign-up)
- /home/loucmane/dev/hpfetcher/app/src/components/Page.tsx (only if the minimal-mast lives here as the shared shell — coordinate with M1/M6; do NOT fork a second mast)

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| DIAGNOSTIK — idle screen chrome (desktop) | locked-decision minimal-mast; live offender SessionPlayer.tsx:551-564 (MobileFrame+Page runningHead=['HP · Coach', sectionLabel] status={mode statusMode 'Diagnostik', context:'redo', hints:['esc hem','⌘k palett']}) | Remove the EDITION Page shell (running head band + bottom status line). The idle body (IdleBody SessionPlayer.tsx:773-1003) keeps its chapter-opening composition but sits under the SAME minimal mast the M1 drill uses, NOT the runningHead+folio+status Page. Mast carries brand ⌜ HP-Coach + nav (Hem/Övning/Lektion/Framsteg) + quiet picker corner only. idleHeadline 'Var står du?' (diagnostik.tsx:60) and eyebrow 'Diagnos' (diagnostik.tsx:59) stay. | WRONG | At 1440 light+dark, /diagnostik idle shows NO bottom status-line testid and NO 'pp. n / total' folio; the top mast matches the M1 /drill mast pixel-for-pixel. No 'esc hem' / 'redo' / '-- DIAGNOSTIK --' string anywhere. |
| DIAGNOSTIK — active drill phase (desktop) | M3 single-column DrillScreen M3.tsx:848-995 via M1/M2 live drill; live offender SessionPlayer.tsx:622-639 (BoksidanDesk 2-col StudyDesk + EditionStrip header) | Diagnostik active questions route through the SAME single-column M3 drill body M1–M4 deliver (reverses the 2-col BoksidanDesk). .m3-frame max-width 880px (M3.tsx:57), .m3-row 128px\|1px\|1fr col-gap 28px (M3.tsx:65-68). No change in diagnostik.tsx itself — it inherits whatever SessionPlayer's active-phase renders once M1 reverses BoksidanDesk. | WRONG | At 1440 the /diagnostik active question is single-column (max-width 880px, centered), identical chassis to /drill; no StudyDesk 2-col grid, no EditionStrip in a sticky header. |
| DIAGNOSTIK — done screen (DiagnosticReport) | ported .hpc-m3-* chassis DiagnosticReport.tsx:286-298 (hpc-m3-row/meta/spine/content) | Already on the margin-rail chassis (the .hpc-m3-* mirror of M3.tsx:64-97). Only fix: ensure the done phase no longer wraps it in the EDITION Page shell (SessionPlayer.tsx:586-598 status mode:'Klar', context:'resultat', hints:['esc hem','⌘k palett']). Strip that Page band; the report sits under the minimal mast. | GAP | At 1440 the diagnostik 'Vad vi tror nu' report renders on its hpc-m3 rail with NO bottom '-- KLAR --' status line and NO folio; mast = minimal mast. |
| REPETITION (idle) — chrome | locked-decision minimal-mast; same SessionPlayer.tsx:551-564 path as diagnostik idle; route repetition.tsx:84-166 | Identical fix to diagnostik idle — repetition is a thin SessionPlayer route (kind='adaptive_review'). idleEyebrow 'Repetition' (repetition.tsx:106), idleHeadline 'Dina missar' (repetition.tsx:107) stay verbatim. Remove EDITION Page band (running head 'REPETITION' chromeLabelsFor SessionPlayer.tsx:1033, status mode 'Repetition'). idleSecondaryCta 'Öva istället / ORD →' (repetition.tsx:125-156) stays but loses the EDITION status-line context. | WRONG | At 1440 light+dark, /repetition idle shows minimal mast only (brand+nav+picker), no '-- REPETITION --' status line, no folio. Empty-queue state ('Inga missar att repetera…') still renders. Headline 'Dina missar' present. |
| REPETITION — resuming spinner chrome | live offender SessionPlayer.tsx:501-510 (Page runningHead+status 'Laddar …' with hints ['esc hem','⌘k palett']) | The 'Laddar …' loading line (SessionPlayer.tsx:476-488, mono 12px uppercase color var(--muted)) keeps its glyph but loses the EDITION Page wrapper; sits under minimal mast. | WRONG | Auto-resume (/repetition?qid=…) shows 'Laddar …' centered under the minimal mast, no status line, no folio. |
| LEKTION — picker chrome (PickerShell) | locked-decision minimal-mast; live offender lektion.tsx:85-97 (MobileFrame+Page runningHead=['HP · COACH','Lektion'] status={mode:'LEKTION', context:'välj sektion', hints:['esc tillbaka','⌘k palett']}) | Replace the Page EDITION shell with the minimal mast. Drop status line entirely (no 'LEKTION · välj sektion · esc tillbaka · ⌘k palett'). The picker BODY (PickerBody lektion.tsx:100-293) stays: header eyebrow ← Hem + LEKTION (lektion.tsx:154-172), h1 'Läs först.' clamp(48px,6vw+16px,88px) (lektion.tsx:176-184), section list with svagast + score badges (lektion.tsx:209-289). Already uses muted tokens. | WRONG | At 1440 light+dark /lektion picker shows minimal mast, NO bottom status line, NO 'esc tillbaka' hint. Section rows + 'svagast' (lektion-svagast) + score (lektion-score-*) badges still render. The redundant in-body '← Hem' kicker may stay but flag for M6 dedup. |
| LEKTION — reader chrome (ReaderShell) | locked-decision minimal-mast; live offender lektion.tsx:299-312 (Page runningHead=['HP · COACH', 'Lektion · NOG'] status mode:'LEKTION', context:section, hints:['esc tillbaka','⌘k palett']) | Same: replace Page shell with minimal mast (mast section token may read 'Lektion · NOG' nav-adjacent, but NO bottom status line). LessonReader + BackToPicker '← alla sektioner' (lektion.tsx:315-337) body unchanged. | WRONG | At 1440 /lektion?section=NOG reader shows minimal mast, no status line, no folio; LessonReader + '← alla sektioner' still present. |
| LEKTION — Esc-to-parent keyboard handler | lektion.tsx:65-75 (Escape: reader→picker, picker→home) | KEEP the Esc handler (desktop keyboard nav is fine; locked-decision only bans Esc as the SOLE phone exit). The status-line hint 'esc tillbaka' that ADVERTISED it (lektion.tsx:91,305) is removed with the status line. Esc stays functional, just no longer surfaced in a removed EDITION bar. | MATCH | Esc still navigates reader→picker→home; no 'esc tillbaka' string rendered. |
| AUTH (sign-in) — top band brand | locked-decision minimal-mast brand ⌜ HP-Coach; live AuthLayout.tsx:135-148 TopBand <BrandMark/> | BrandMark (⌜ HP-Coach) STAYS — it's the minimal-mast brand element and matches M3 chrome intent. AuthLayout.tsx:148. | MATCH | /sign-in top-left still shows ⌜ HP-Coach BrandMark at 1440 and 402. |
| AUTH (sign-in) — exam-tag flourish + ↘ glyph | locked-decision: NO exam-tag flourish in the mast; live offender AuthLayout.tsx:149-167 (currentExamPeriod() 'VÅR-2026' in accent mono + ↘ glyph color var(--muted)) | REMOVE the exam-period tag ('VÅR-2026'/'HÖST-2026', currentExamPeriod() AuthLayout.tsx:171-184) and the ↘ glyph from TopBand. The locked decision bans the exam-tag flourish from the masthead vocabulary. TopBand keeps brand + hairline borderBottom (AuthLayout.tsx:146) only; right side goes empty (the quiet picker corner is out of scope here — leave empty). | WRONG | At 1440 and 402 /sign-in shows NO 'VÅR-2026'/'HÖST-2026' tag and NO ↘ glyph. TopBand = brand + hairline only. currentExamPeriod() deleted or cleanly unused (no lint). |
| AUTH — Boksidan margin-rail form spine | AuthLayout.tsx:265-272 (borderLeft 1px var(--hairline), paddingLeft clamp(20px,2vw,32px)); CardSection cobalt rail label AuthLayout.tsx:363-375 | Already Boksidan per #143 (hairline spine + cobalt-accent rail label, mono 11px weight 500 letter-spacing 0.08em uppercase color var(--accent) AuthLayout.tsx:364-372 — matches M3 .m3-meta/.m3-dis-l accent register M3.tsx:75-83,538-545). No change. | MATCH | /sign-in form pane keeps the single hairline spine + cobalt 'LOGGA IN' rail label at 1440; phone keeps flush eyebrow. |
| AUTH — pull-quote (FÖRSTÅ / STRUKTUREN.) | AuthLayout.tsx:298-352 PullQuote (display 400, uppercase, letter-spacing -0.02em, 64px ink hairline) | Editorial pull-quote stays — it is the auth surface's typographic event and uses muted tokens (var(--ink)/var(--ink-2)). Not an EDITION-shell element. No change. | MATCH | /sign-in brand pane (auth-brand-pane testid) still shows the FÖRSTÅ / STRUKTUREN. pull-quote at 1440; hidden at phone. |
| WELCOME — Spalt palette option (the gap) | welcome.tsx:123-152 palette PickerRow renders sand/sage/ink/rose only; spalt exists in PALETTE_ACCENTS welcome.tsx:278 + is DEFAULT_THEME.palette tokens.ts:495 | ADD a fifth PaletteOption for spalt: p="spalt" label="spalt" sub="ivory · kobolt" (Swedish; matches the L12 'ivory + cobalt' colorway tokens.ts:263-266) active={palette === 'spalt'} onClick={() => setPalette('spalt')}. Accent already wired (welcome.tsx:278 oklch(0.49 0.21 265)). Insert after rose (welcome.tsx:145-151). | GAP | At /welcome the Palett row shows FIVE options incl. 'spalt' (cobalt-accented label); clicking sets palette to spalt and the row reflects active state. Since spalt is the DEFAULT (tokens.ts:495), a fresh visitor sees 'spalt' pre-selected. |
| PICKER → SETTINGS migration (deferred consideration) | locked-decision: palette/mode/edition picker destined for a real Settings surface, NOT ⌘K-only and NOT a permanent masthead fixture; live EditionStrip in Page.tsx:339 + BoksidanDesk header; ⌘K theme cmd CommandPalette.tsx:126 | DO NOT build Settings in M5. Document: the minimal-mast 'quiet picker corner' is a transitional home for palette/mode/edition until a dedicated /settings (or /installningar) route exists. Note EditionStrip currently couples palette+edition+mode into one masthead strip; a Settings surface would absorb it. Capture as an open question for the owner, not code. | GAP | Spec records the migration consideration; no Settings route created. Owner sign-off captured on whether the quiet picker corner is acceptable interim vs. pulling the picker out of the mast now. |
| PHONE — diagnostik / repetition idle + active (no EDITION leftovers) | Page is a passthrough at phone (Page.tsx:116 returns children); phone idle/active SessionPlayer.tsx:530-540, 574-583, 646-735 | Phone already bypasses the EDITION desktop chrome (Page.tsx:116). Confirm phone idle/active for diagnostik+repetition still renders the bare linear body + BottomTabs (MobileFrame.tsx:278) and — per M1 — the active drill gains the FLOATING bottom-center 'Nästa fråga' pill + visible touch exit. M5 verifies inheritance; the pill/exit are built in M1. | GAP | At 402 light+dark, /diagnostik and /repetition idle are linear + BottomTabs; active question (post-M1) has the floating 'Nästa fråga' pill + a visible back/close exit. No desktop running-head/status-line on phone. |
| PHONE — lektion picker + reader | Page passthrough Page.tsx:116; lektion bodies lektion.tsx:142-293, 308-309 | Phone lektion already bare (Page is no-op). Confirm no EDITION band leaks; picker rows + reader render under BottomTabs (activeTab='lektion'). | MATCH | At 402 /lektion picker + /lektion?section=NOG reader are linear, BottomTabs visible, no desktop chrome. |
| PHONE — auth (sign-in) | AuthLayout PhoneBody AuthLayout.tsx:188-221; TopBand always rendered AuthLayout.tsx:96 (no isPhone guard) | TopBand renders on phone too (AuthLayout.tsx:96) — so the exam-tag removal applies at 402 as well. Phone keeps the stacked PullQuote(compact) + CardSection + crossLink (AuthLayout.tsx:212-218). | WRONG | At 402 /sign-in: brand-only top band (no VÅR-2026 / ↘), then compact pull-quote, then Clerk card, then 'Inget konto? → börja här'. |

**Data traps.**
- camelCase vs snake_case is NOT a primary risk for M5 — these surfaces render chrome, not M3 fixture fields. But diagnostik/repetition active questions flow through SessionPlayer → the M1–M4 drill body, which DOES read explanation fields. Confirm the active body reads LIVE snake_case (why_tempting/why_wrong/pregrade_tactic, 'figure' not figureMeta) per the M0–M4 contract; do not let an M3-copied field access (whyTempting/whyWrong/pregradeTactic/sectionLabel/contextTitle/figureMeta) leak into the carry-over path.
- LIVE data has NO sectionLabel/total/contextTitle — M3.tsx:882 reads QUESTION.sectionLabel + QUESTION.total which DO NOT EXIST on live Question. The eyebrow ('FRÅGA n AV total') in the carry-over drill must be derived from SessionPlayer index+plan.length (SessionPlayer.tsx:743 folio current/total), NOT from a question field.
- chromeLabelsFor (SessionPlayer.tsx:1026-1047) hardcodes EDITION head labels ('DIAGNOSTIK'/'REPETITION'/'LEKTION') + status modes; removing the Page status line orphans these. Either delete chromeLabelsFor or repurpose its label for the minimal-mast section token — do not leave it feeding a deleted status line (dead code / lint).
- welcome.tsx PALETTE_ACCENTS already includes spalt (welcome.tsx:278) and EditionStrip's PALETTE_ORDER includes spalt — but the welcome PICKER ROW omits it (welcome.tsx:124-151). The gap is purely a missing <PaletteOption>, not a missing token. Do not re-add the accent (would duplicate).
- spalt is the DEFAULT palette (tokens.ts:495). An EXISTING dogfood user with hpc-ui state never sees /welcome (gated by hpc-welcomed, welcome.tsx:14-16), so they already start in spalt — the gap only bites a fresh user who nudges away from spalt and then can't pick it back from welcome. Frame the fix accordingly.
- AuthLayout TopBand has NO isPhone guard (AuthLayout.tsx:135-168) — the exam-tag + ↘ removal applies to BOTH phone and desktop in one edit. Don't fork phone vs desktop for this.
- DiagnosticReport already uses .hpc-m3-* (the ported chassis, DiagnosticReport.tsx:286-298) — these are the LIVE mirror classes in index.css, NOT M3.tsx's .m3-* (which only exist inside the devbake CSS const). Don't import M3.tsx's CSS into the report; the .hpc-m3-* set is the production chassis the whole product shares.

**Test contracts.**
- responsive.spec.ts:142-145 — 'Page shell renders at desktop, BottomTabs at phone' asserts page-shell + brand-mark + running-head + status-line on / (Home). If M5/M1 removes the EDITION Page shell on carry-over surfaces, this Home-scoped test still expects the OLD shell — coordinate: either Home keeps a status-line (unlikely under minimal mast) or this test is rewritten in M1/M3H/M6. M5 must NOT break it for Home; verify it still targets only /.
- responsive.spec.ts:177-201 — 'Auth brand pane visible at studio, hidden at phone' asserts auth-form-pane + auth-brand-pane at 1440, brand-pane absent at 390. Removing the exam-tag does NOT touch those testids, but RE-RUN to confirm the TopBand edit didn't disturb auth-top-band layout (auth-top-band testid AuthLayout.tsx:139).
- mistakes.spec.ts:95,153-172 — drives /repetition idle ('button only enables once /api/mistakes/due') + asserts the queue surfaces. Removing the EDITION Page shell must NOT change drill-start / drill-idle / drill-empty testids (SessionPlayer.tsx:798,947,989). Keep all data-testids stable.
- home.spec.ts:30-32 — '/' redirects to /sign-in (unauthenticated). Auth TopBand edit must keep /sign-in rendering (no crash from deleting currentExamPeriod).
- lektion-svagast (lektion.tsx:274) + lektion-score-{sec} (lektion.tsx:279) testids must survive the PickerShell chrome swap — they live in the BODY, not the removed Page shell.
- No existing welcome.test.* found (grep returned none) — adding the spalt option needs a NEW assertion (unit or e2e) that the Palett row renders 5 options incl. spalt and selecting it sets the store. Add it; don't rely on a non-existent test.
- Any new e2e that screenshots carry-over surfaces must use the deterministic seed from task 107 (/test-reset, in flight) so the repetition queue count is stable across the side-by-side captures.

**Decisions applied.**
- Desktop chrome = MINIMAL MAST: one quiet hairline top band with brand (⌜ HP-Coach) + nav (Hem/Övning/Lektion/Framsteg) + quiet picker corner. NO section echo, NO bottom status line, NO exam-tag flourish. Applied to diagnostik idle/active/done, repetition idle/resume, lektion picker/reader, auth top band.
- Desktop drill = SINGLE COLUMN (.m3-frame max-width 880px), reversing the BoksidanDesk 2-col StudyDesk. Diagnostik + repetition active questions inherit this via SessionPlayer (built in M1; M5 verifies inheritance).
- Phone drill = linear + bare, floating bottom-center 'Nästa fråga' pill + visible touch exit (built in M1). M5 verifies diagnostik/repetition phone paths inherit it.
- ⌘K is a power-user accelerator, NOT primary navigation — so removing the 'esc tillbaka · ⌘k palett' status-line hints from lektion + the diagnostik/repetition status lines is correct; nav stays visible in the mast.
- The palette/mode/edition picker is destined for a real Settings surface, not ⌘K-only and not a permanent masthead fixture — captured as a deferred consideration (no Settings built in M5).
- Progress = textual eyebrow ('FRÅGA n AV total'); NO visual progress bar — so the EDITION status-line ProgressGlyph (Page.tsx:452-479) is removed with the status line, not ported.
- Anti-drift: TARGETED FIXES ONLY — harmonize chrome + kill EDITION residue; do not redesign DiagnosticReport, the Clerk card, LessonReader, or the idle masthead bodies.

**Definition of done.**
- Anti-drift gate: M3 chrome decision cited (locked minimal-mast) + the live drill/home each surface borrows from is named; side-by-side screenshots captured of EACH carry-over surface vs its borrowed drill/home at 1440 AND 402, in light AND dark (diagnostik idle+active+done; repetition idle+resume; lektion picker+reader; sign-in desktop+phone). Every elementChecklist row resolved to MATCH.
- Diagnostik: idle + active + done render under the minimal mast — NO running-head+folio+status-line EDITION band, NO '-- DIAGNOSTIK --'/'redo'/'esc hem' strings, active question single-column 880px. DiagnosticReport hpc-m3 rail intact.
- Repetition: idle + resume render under minimal mast; no EDITION status line/folio; empty-queue copy + 'Öva istället' CTA intact; active question single-column.
- Lektion: picker + reader render under minimal mast; status line + 'esc tillbaka' / '⌘k palett' hints gone; Esc handler still works; svagast + score badges intact.
- Auth: TopBand shows brand + hairline ONLY — no 'VÅR-2026'/'HÖST-2026' exam tag, no ↘ glyph, on both 1440 and 402; pull-quote + cobalt rail label intact; currentExamPeriod removed or cleanly unused (no lint).
- Welcome: Palett row offers 5 options incl. 'spalt' (cobalt label, sub 'ivory · kobolt'); selecting it sets the store; default state pre-selects spalt. New test covers it.
- Picker→Settings migration documented as a deferred owner consideration; no Settings route shipped.
- All data-testids preserved (drill-start/idle/empty, lektion-svagast, lektion-score-*, auth-form-pane, auth-brand-pane, auth-top-band, page-shell where still applicable); chromeLabelsFor dead-code resolved.
- pnpm test green; pnpm test:e2e green (responsive.spec, mistakes.spec, home.spec, auth — re-run with task-107 deterministic seed).
- Owner approves the visual side-by-sides before merge. NO auto-merge label on this PR.

**Dependencies.**
- M1 (#153) — defines the single-column M3 drill body + minimal mast + phone floating 'Nästa fråga' pill + visible touch exit that diagnostik/repetition INHERIT via SessionPlayer. M5 must land AFTER M1 (or coordinate the shared SessionPlayer/Page mast edit) so it harmonizes to the final drill chrome, not the EDITION one.
- M2 (#154) — pedagogy/graded rewrite (distractor struck OPTION TEXT) flows into diagnostik/repetition graded view via the shared drill body. M5 verifies the carry-over graded view inherits M2, not the old letter-only block.
- M3H (#155) — Home rebuild defines the minimal-mast + the / surface lektion/auth nav should match. The mast M5 reuses should be the one M3H/M1 finalize, not a third fork.
- M4 (#156) — section apparatus; diagnostik samples ALL sections (pickDiagnosticQuestions) so its active questions exercise LÄS/NOG/XYZ/DTK apparatus built in M4. Verify diagnostik active inherits M4 correctly.
- M0 (#152) — foundation CSS gap fill + meta color; the .hpc-m3-* chassis tokens these surfaces depend on. M0 first.
- M6 (#158) — cross-surface fidelity sweep absorbs the redundant in-body '← Hem' kicker dedup (lektion) and the picker→Settings decision; M5 flags, M6 may resolve.
- Task 107 (#107, in flight) — /test-reset deterministic mistakes seed; needed for stable repetition-queue screenshots in the side-by-side gate.

## M6 — M6 — Cross-surface fidelity sweep

**Objective.** Produce docs/m3-fidelity-report.md: a per-element live-vs-M3 comparison grid covering EVERY surface x {1440,402} x {light,dark}, with a per-element PASS/FAIL verdict and side-by-side live+M3 screenshots. This is the verification step skipped in tasks 80-87 (which grafted M3 accents onto EDITION and never diffed). No feature edits — M6 is a read-only audit/gate. Ship only when every row PASSes; any FAIL routes a fix back to M0-M5 (M6 does not fix in place).

**Reference.** M3 reference is /redesign-l12?dev=1&v=3 (M3.tsx routed at redesign-l12.tsx:106). State via search params: s=home|drill (redesign-l12.tsx:67), q=ord|las|xyz|nog|dtk (redesign-l12.tsx:68, section-aware only for v=2/3 per SECTION_AWARE l.53). Graded state is NOT a URL param — it is component useState (M3.tsx:849 picked); to capture graded, click the correct option then screenshot. Light/dark via the switcher mode toggle (redesign-l12.tsx:182, applyThemeToDocument l.85) — palette MUST be spalt (DEFAULT_THEME.palette (tokens.ts:495), the shipped default per redesign memory). Canonical capture widths: 1440 (matches responsive studio, responsive.spec.ts:46) and 402 (spec-mandated phone width — NOTE: differs from the e2e mobile project's iPhone-13 390x664, playwright.config.ts:52). Per-surface reference URLs: HOME = ?dev=1&v=3&s=home; DRILL ORD idle = &s=drill&q=ord; DRILL ORD graded = same + click correct option D; sections = &s=drill&q={las|nog|xyz|dtk} idle and graded (dtk has NO explanation so graded shows m3-missing). Live counterparts: / (HomeMobile), /drill?section=ORD (DrillQuestion phone / BoksidanDesk desktop / PedagogyPanel graded), /drill?section={LAS|NOG|XYZ|DTK}, /diagnostik, /repetition, /lektion, /sign-in, /sign-up, /progress.

**Files to edit.**
- /home/loucmane/dev/hpfetcher/docs/m3-fidelity-report.md (CREATE — the deliverable; the ONLY file M6 writes)
- NO feature/source edits in M6 — any FAIL is logged and routed back to the owning task (M0 css, M1 drill idle, M2 graded, M3H home, M4 sections, M5 carry-over); M6 re-runs the grid after the fix lands

**Element checklist.**

| Element | M3 source | Exact values | Status | Acceptance |
|---|---|---|---|---|
| GRID SCHEMA — column definitions for every row in the report | docs/m3-faithful-rebuild-spec.md:131-135 (M6 charge) | Columns: Surface \| M3 ref URL+state \| Width(1440/402) \| Mode(light/dark) \| Element \| M3 source (file:line) \| Expected value (px/clamp/token/copy) \| Live value observed \| Verdict(PASS/FAIL) \| Note/owning-task. Pass bar: PASS only when live matches M3 on font-family, font-size(px/clamp), font-weight, letter-spacing, color token, spacing(margin/padding/gap), label copy verbatim, and presence+position; otherwise FAIL with the delta named. | GAP | Report opens with this column legend + the explicit pass-bar definition before any grid row. |
| CAPTURE MATRIX — surfaces x widths x modes enumeration (the row index) | m3-faithful-rebuild-spec.md:30 (1440 AND 402, light AND dark) | 9 surface-states x 2 widths x 2 modes = 36 screenshot pairs (live+M3 each = 72 images). Surface-states: (1) Home; (2) Drill ORD idle; (3) Drill ORD graded; (4) Drill LÄS idle; (5) Drill NOG idle; (6) Drill XYZ idle; (7) Drill DTK idle; (8) Drill section graded (XYZ, has explanation); (9) Drill DTK graded (missing-explanation state). Carry-over surfaces (diagnostik/auth/repetition/lektion/progress) have NO M3 ref — graded vs the M1-M3 pattern they borrow (M5 rule, spec:122-127), captured at same 2x2. | GAP | Every cell of the 36-cell matrix has a captured live+M3 pair filename referenced in the report; none missing. |
| m3-frame chassis — single-column 880px | M3.tsx:56-60 (.m3-frame max-width:880px; padding 56px 24px 96px; @media max-width:900px -> 40px 18px 72px, l.721) | max-width:880px; desktop padding 56px 24px 96px; phone padding 40px 18px 72px. Desktop drill MUST be single column (LOCKED) — verify NO 2-col StudyDesk at 1440. | WRONG | Row asserts live desktop drill renders one 880px column (not BoksidanDesk 2-col); FAIL routes to M1/M2. |
| m3-row margin rail — 128px \| 1px \| 1fr, gap 28px | M3.tsx:64-68 (.m3-row grid 128px 1px 1fr; column-gap 28px); l.720 phone 96px 1px 1fr gap 20px | desktop 128px 1px 1fr / column-gap 28px; phone 96px 1px 1fr / column-gap 20px | GAP | Rail columns measured at both widths match; spine is var(--hairline) 1px (M3.tsx:71). |
| m3-meta rail label | M3.tsx:75-91 | font-mono 11px; letter-spacing 0.08em; uppercase; color var(--muted); text-align right; padding-top 5px; tabular-nums; meta strong = var(--ink-2) weight 500 block | GAP | Rail meta typeset + colored exactly; no section echo beyond M3's own (decision: minimal mast). |
| m3-eyebrow — FRÅGA n AV total | M3.tsx:127-134; usage 882 (DrillScreen) / 1035 (Section) | font-mono 11px; letter-spacing 0.14em; uppercase; color var(--muted); tabular-nums. Copy: '{sectionLabel.toUpperCase()} · FRÅGA {number} AV {total}' e.g. 'ORDFÖRSTÅELSE · FRÅGA 3 AV 10'. NO progress bar (decision: textual eyebrow only). | WRONG | Live eyebrow = full section label + FRÅGA n AV total; FAIL if it says 'ORD · ÖVNING' (old). Routes to M1. |
| m3-display headword | M3.tsx:136-146; usage 884 | font-display italic weight 400; font-size clamp(44px,6vw,64px); line-height 1.05; letter-spacing -0.01em; margin 10px 0 0; color var(--ink); copy 'eftertrakta' | GAP | Headword serif-italic at clamp(44,6vw,64) at both widths; routes to M1. |
| m3-tactic pre-answer aside | M3.tsx:300-322; usage 886-889 (only when !graded) | margin-top 24px; padding 14px 18px; border-left 1px var(--hairline). tactic-h: mono 11px ls 0.1em uppercase var(--accent), copy 'Taktik · {handle}' (handle 'Stamordskartan', fixtures.ts:29). tactic-t: display italic 16px lh1.5 var(--ink-2) max 60ch. | GAP | Tactic block present+positioned under headword before answering, hidden after grading; routes to M1. |
| m3-keys hint (pre-answer) | M3.tsx:406-412; usage 925 | mono 11px var(--muted-2) ls 0.06em; copy 'Tangenter a–e väljer · klick fungerar också' (a–{lastKey} for sections, M3.tsx:1118) | GAP | Keys hint present @1440 light/dark; on PHONE the keys hint is moot — phone needs a VISIBLE touch exit + floating 'Nästa fråga' pill instead (decisions); routes to M1. |
| m3-opts / m3-opt option rows | M3.tsx:324-404; usage 896-922 | opt grid 3px 36px 1fr auto, gap 14px, border-bottom 1px var(--hairline-2). opt-k mono 12px var(--muted-2) pad 15px 0. opt-t display 18px weight 450 pad 15px 0. is-ok bg var(--ok-soft) text var(--ok); is-bad bg var(--bad-soft) text var(--bad) + line-through 1.5px; is-dim var(--muted-2). LÄS is-prose: opt-t 16px lh1.55 max 65ch (M3.tsx:659-667). | GAP | Rows match grid+type+graded colors at both widths; correct row green (--ok) not cobalt (convention-2). |
| m3-verdict UTFALL | M3.tsx:414-447; usage 930-941 | verdict-word display italic weight 600 clamp(34px,5vw,46px) lh1.05 ls -0.01em; is-ok var(--ok) / is-bad var(--bad); copy 'Rätt.'/'Fel.'. verdict-sub 15px var(--ink-2) max 60ch; correct copy 'Snyggt — taktiken höll hela vägen.', wrong 'Rätt svar är d) vilja ha. Häng med i varför.' | GAP | Verdict semantic-colored italic word + sub sentence verbatim; routes to M2. |
| m3-solution lede | M3.tsx:451-463; usage 942 | margin-top 22px; padding 18px 0; border-top 1px var(--ink); border-bottom 1px var(--hairline); display weight 600 19px lh1.5 max 75ch | GAP | Bold serif solution line between rules at 75ch; routes to M2. |
| m3-step numbered steps + tier badge | M3.tsx:465-511; usage 945-962 | step grid 36px 1fr gap 16px pad 18px 0 border-bottom 1px hairline-2. step-n display 600 21px var(--accent). step-h display 600 18px. step-tier mono 10px ls0.12em uppercase var(--muted-2) ml10px, copy 'kärna'(essential)/'detalj'(detail). step-t 15px lh1.65 var(--ink-2) max 75ch. Section meta '{n} steg' title 'Så löser du den'. | GAP | Numbered steps with kärna/detalj tier badges, 75ch body; routes to M2. |
| m3-dis distractor block — STRUCK OPTION TEXT reprinted | M3.tsx:513-553; usage 964-983 (esp. <s>{d.text}</s> l.974) | dis pad 16px 0 border-bottom 1px hairline-2. dis-h display 600 17px; dis-k mono 12px var(--muted-2) mr8px copy '{letter})'; <s>{text}</s> line-through 1.5px (the struck OPTION TEXT, e.g. 'ta reda på'). dis-l mono 11px ls0.12em uppercase var(--accent) copies 'Varför det lockar' / 'Varför det är fel'. dis-p 14.5px lh1.6 var(--ink-2) max 75ch. Section meta '{n} fällor' title 'Varför de andra lockar'. | WRONG | M2 reprints each distractor's struck-through option text (LOCKED decision) — FAIL if live shows letter-only block (old PedagogyPanel.tsx ~600); routes to M2. |
| m3-next-row Nästa fråga | M3.tsx:555-560 + 985-990 (.m3-cta 202-218) | next-row margin-top 36px flex gap 16px. cta: ui 13px weight 600 var(--accent-ink) bg var(--accent) pad 10px 20px; copy 'Nästa fråga'; hint mono 'Enter går vidare'. PHONE: this becomes a FLOATING bottom-center pill reachable without scroll (decision). | GAP | Desktop next-row matches; phone shows floating bottom-center 'Nästa fråga' pill; routes to M2. |
| HOME m3-display greeting + m3-stats row | M3.tsx:758-786; CSS 136-176 | rail meta strong = HOME.dateLabel 'Torsdag 11 juni'. display 'God eftermiddag'. stats flex gap 48px mt28 pad-top20 border-top 1px hairline. stat-n mono 30px lh1.1; stat-l 12px var(--muted); stat-d 12px var(--ok). Values: '1,4' (svDecimal) 'prognos av 2,0' '+0.1 sedan förra veckan'; '12' 'dagar i rad'; '16' 'min idag'. | GAP | Greeting + 3-stat row with localized 1,4 present; routes to M3H. |
| HOME m3-resume card | M3.tsx:788-803; CSS 178-218 | resume flex space-between bg var(--accent-soft) pad 16px 20px mt4. resume-t 15px weight 500 'Övning · ORD'. resume-s mono 12px var(--muted) 'fråga 4 av 10 · telefon · 11:07'. cta 'Fortsätt här'. Rail meta 'Påbörjad'. | GAP | Resume card present with copy verbatim; routes to M3H. |
| HOME m3-plan numbered items | M3.tsx:805-829; CSS 220-272 | plan-item grid 32px 1fr auto gap16 pad14 border-bottom hairline-2. plan-n display 600 19px var(--accent) '1.' '2.' '3.'. plan-t display 500 17px (with m3-tag section). plan-r 13px var(--muted) max56ch. plan-min mono 12px var(--muted-2) 'N min'. Rail meta '<strong>16 min</strong>3 moment' title 'Dagens plan'. tag mono 11px ls0.08em border 1px hairline pad 1px 6px. | WRONG | Numbered 1./2./3. plan items (not cobalt rail kickers); routes to M3H. |
| HOME m3-trap rows | M3.tsx:831-843; CSS 274-296 | trap flex baseline space-between gap24 pad13 border-bottom hairline-2. trap-t 14.5px (with tag). trap-n mono 12px var(--muted-2) 'N ggr'. Rail meta 'Mönster' title 'Dina fällor just nu'. | GAP | Trap rows with section tag + N ggr; routes to M3H. |
| SECTION LÄS — TEXTEN passage | M3.tsx:1041-1050; CSS 564-583 | Section meta 'Texten'. passage-h display 600 21px lh1.3 (contextTitle 'Kopplingen till Medelhavet'). passage p display 16.5px weight400 lh1.7 var(--ink-2) max70ch, split on \n\n. | GAP | LÄS TEXTEN passage block matches; routes to M4. |
| SECTION NOG — UPPGIFTEN + PÅSTÅENDEN | M3.tsx:1060-1083; CSS 585-622 | meta drill==nog ? 'Uppgiften':'Frågan'. m3-q display 500 clamp(22,3vw,27) lh1.35. statements Section meta 'Påståenden': stmt grid 44px 1fr gap14 pad15 border-bottom; stmt-n mono 12px '(n)'; stmt-t display 17.5px lh1.55 max65ch. coda (Välj svar) display italic 16px. | GAP | NOG apparatus (prompt + (1)/(2) statements + coda) matches; routes to M4. |
| SECTION XYZ — FRÅGAN math prompt | M3.tsx:1060-1063 (MathText prompt) | meta 'Frågan'. m3-q with MathText for LaTeX (e.g. (x + 3)(x - 5)). Options carry MathText (M3.tsx:1107-1109). | GAP | XYZ prompt+options render math (U+E000/E001 fences via MathText); routes to M4. |
| SECTION DTK — UNDERLAGET figure + missing-explanation graded | M3.tsx:1052-1058 (figure) + 1145-1147 (m3-missing) | Section meta 'Underlaget'. m3-fig strips QuestionFigure card frame (border/radius/bg/pad none !important, M3.tsx:645-650). DTK graded: explanation null -> m3-missing display italic 16px var(--muted) copy 'Förklaring saknas ännu för den här frågan.' | GAP | DTK figure borderless + graded shows graceful missing-explanation copy; routes to M4. |
| DESKTOP minimal mast — brand + nav + picker corner | Decision (LOCKED); M3 itself has NO chrome (M3.tsx:1233-1244 renders only m3-root) | One quiet hairline top band: brand '⌜ HP-Coach' + visible nav (Hem/Övning/Lektion/Framsteg) + quiet picker corner. NO section echo, NO bottom status line, NO exam-tag. ⌘K is accelerator only, NOT primary nav. | WRONG | Sweep confirms desktop chrome is the minimal mast (not the full EDITION shell, not bare); FAIL routes to M0/M5. |
| PHONE focused drill — visible touch exit | Decision (LOCKED; belongs in M1) | Focused phone drill MUST have a VISIBLE touch back/close control. Esc/⌘K do NOT count (no keyboard on phone). Phone is linear + bare (no running head) + floating bottom-center 'Nästa fråga' pill. | GAP | 402px drill screenshots show a tappable exit + floating Nästa pill reachable without scroll; routes to M1. |
| CARRY-OVER surfaces — diagnostik/auth/repetition/lektion/progress | m3-faithful-rebuild-spec.md:122-127 (M5) | No M3 ref screen; verify each inherits the SAME m3-frame 880px chassis + rail + type now that drill/home are faithful — no EDITION leftovers (page-shell/running-head/status-line removed). Auth: auth-form-pane + auth-brand-pane testids preserved. | GAP | Each carry-over surface captured 2x2 and judged vs the drill/home pattern it borrows; FAIL routes to M5. |
| prefers-reduced-motion parity | M3.tsx:709-717 (M3) + playwright reducedMotion:'reduce' (playwright.config.ts:37) | Under reduced-motion all m3 animations collapse to ~0.01ms. Screenshots taken with motion settled (or reduced) so entrance choreography doesn't pollute the diff. | GAP | Each pair captured after motion settles (or with reduced-motion) so the diff is steady-state, not mid-animation. |

**Data traps.**
- camelCase (M3 fixtures) vs snake_case (live corpus): M3.tsx reads whyTempting/whyWrong/pregradeTactic/sectionLabel/contextTitle/figureMeta/total (fixtures.ts:28,58,60 + fixturesSections.ts:36,42,48); LIVE corpus is why_tempting/why_wrong/pregrade_tactic, 'figure' (not figureMeta), and has NO sectionLabel/total/contextTitle. M6 must compare VISUAL OUTPUT, not field names — a PASS means the rendered px/copy match even though the live component maps different field names. Do NOT flag the live snake_case access as a FAIL.
- M3 fixtures are hardcoded ideal data (eftertrakta/var-2026, 3 steps, 4 distractors, 1,4 prognos); LIVE pulls real corpus which varies per qid. To make the diff fair, drive the live drill to the SAME qid as the fixture where possible (var-2026-verb1-ORD-003 via ?qid=, drill route supports qid param per task 146) OR explicitly note 'content differs, comparing CHASSIS/TYPE not copy' in the row.
- Graded state is component useState, not a URL param (M3.tsx:849, DrillScreen). Capturing 'graded' requires clicking the correct option (D for ORD, M3.tsx:899) before screenshot. The report must record the interaction step, not just a URL.
- 402px is the SPEC-MANDATED phone width and differs from the e2e mobile project's iPhone-13 viewport (390x664, playwright.config.ts:52). Do not assume the e2e mobile project width equals the sweep width — explicitly set 402 when capturing.
- M3 reference is full-bleed (no Frame) and applies theme directly (redesign-l12.tsx:85); palette must be forced to spalt (DEFAULT_THEME.palette (tokens.ts:495), redesign-l12.tsx:42) to match the shipped default. A diff captured under a non-spalt palette is invalid.
- DTK fixture has explanation:null on PURPOSE (fixturesSections.ts:505) — graded DTK exercises the m3-missing state. Do NOT treat the absent steps/distractors as a FAIL; the correct M3 behavior is the 'Förklaring saknas' line (M3.tsx:1146).
- M3 desktop is SINGLE column 880px; the live BoksidanDesk 2-col StudyDesk shipped by tasks 80-87 is the WRONG state being reversed (LOCKED). If the sweep still sees 2-col at 1440, that is a FAIL, not the target.

**Test contracts.**
- M6 writes no source, so it cannot itself break tests — but the sweep MUST confirm M0-M5 left these e2e testids intact (tests-e2e/*.spec.ts): drill-idle, drill-start, drill-prompt, drill-next, drill-result, drill-result-headline, drill-result-detaljer, option-A/option-${letter}, home-greeting, daily-plan-card, daily-plan-skeleton, session-active, session-none, session-resume, auth-form-pane, auth-brand-pane, cmdk, cmdk-item-drill-ord, api-ok, mut-ok.
- AT-RISK testids: page-shell, running-head, status-line, brand-mark (home.spec.ts:142-145) assert EDITION chrome (running head + folio + status line, task 94). The minimal-mast decision REMOVES the section echo + bottom status line — if M0-M5 dropped status-line/running-head, those assertions break. The report must flag whether these testids were repurposed/removed and whether home.spec.ts was updated accordingly; this is a row in the grid, not a silent pass.
- pnpm test (vitest) and pnpm test:e2e (playwright) MUST be green at sweep time — the report records the run output (pass counts) as evidence, since 'tests green' is part of the per-task DoD M6 is auditing.
- e2e mobile project is iPhone-13 390x664 (playwright.config.ts:52) — if any M0-M5 layout assumes exactly 402, confirm it still passes at 390 (the test width) so the suite doesn't go red at a width the sweep didn't capture.

**Decisions applied.**
- Desktop drill = SINGLE COLUMN m3-frame 880px (M3.tsx:57) — sweep FAILS any surface still showing the 2-col BoksidanDesk StudyDesk; this reverses tasks 80-87.
- Phone drill = linear + bare (no running head) + floating bottom-center 'Nästa fråga' pill reachable without scroll — verified at 402.
- Desktop chrome = minimal mast (brand ⌜ HP-Coach + visible nav Hem/Övning/Lektion/Framsteg + quiet picker corner); NO section echo, NO bottom status line, NO exam-tag — sweep checks both 'not bare' and 'not full EDITION shell'.
- M2 reprints each distractor's struck-through OPTION TEXT (M3.tsx:973-974) — sweep FAILS the old letter-only block (PedagogyPanel.tsx ~600).
- Phone focused drill has a VISIBLE touch exit (back/close); Esc/⌘K do not count — verified in the 402 captures.
- ⌘K is a power-user accelerator, NOT primary navigation; nav+settings visible without it; palette/mode/edition picker belongs in a real Settings surface, not ⌘K-only nor a permanent masthead fixture.
- Progress = textual eyebrow 'FRÅGA n AV total' (M3.tsx:882); NO visual progress bar — sweep FAILS any progress bar.
- Copy M3's CSS/px VALUES, not its camelCase field accesses (systemic trap) — the sweep judges rendered output, not field names.
- Anti-drift: build/verify TOWARD M3 pinned on screen; 'looks good' is not the bar, 'matches M3 per-element' is. NO auto-merge on this design gate (owner approves the visual).

**Definition of done.**
- docs/m3-fidelity-report.md exists with the column legend + explicit pass-bar definition at the top.
- All 36 capture cells (9 surface-states x 2 widths x 2 modes) have referenced live+M3 screenshot pairs (72 images), palette forced to spalt, motion settled/reduced.
- Every elementChecklist element appears as one-or-more grid rows per applicable surface/width/mode with a PASS/FAIL verdict; no element silently omitted.
- Every row PASSes. Any FAIL is logged with the named delta + owning task (M0/M1/M2/M3H/M4/M5), the fix lands in that task, and M6 re-runs the affected rows until green. M6 itself makes NO in-place feature fixes.
- Anti-drift gate satisfied: M3 URL+state cited per row (incl. screen/drill/graded interaction); live+M3 side-by-side at 1440 AND 402 in light AND dark; every element matched; exact px/clamp/letter-spacing/token/Swedish-copy values copied from M3.tsx and recorded.
- All data-testids intact + pnpm test green + pnpm test:e2e green; the report records pass counts and flags any at-risk EDITION testid (page-shell/running-head/status-line) resolution.
- Owner approves the visual before merge — NO auto-merge label on this task.

**Dependencies.**
- M0 — Foundation CSS gap fill + meta color (#152): chassis CSS must be ported before any surface can PASS.
- M1 — Drill pre-answer eyebrow + tactic + keys (#153): drill-idle rows depend on it.
- M2 — Drill graded / pedagogy rewrite (#154): graded + distractor-struck-text rows depend on it.
- M3H — Home rebuild (#155): home rows depend on it.
- M4 — Section apparatus LÄS/NOG/XYZ/DTK (#156): section rows depend on it.
- M5 — Carry-over diagnostik/auth/repetition/lektion (#157): carry-over rows depend on it.
- M6 is the LAST task (spec sequencing m3-faithful-rebuild-spec.md:139-143) — it cannot pass until M0-M5 are merged and owner-approved.
- M3 reference must remain live at /redesign-l12?dev=1&v=3 (PR #146 restored it); if the dev route is removed the sweep loses its truth source.


## Site-vs-plan sweep corrections — MUST HONOR (applied 2026-06-18)

A 6-critic comparison of the CURRENT LIVE SITE (source-of-truth) vs this plan found four classes of holes the per-screen specs missed. Dispositions below are folded into the named tasks.

### BLOCKERS
- **Nav reconciliation pointed at DEAD code.** Spec said "reconcile DesktopNav.tsx:32-35" — but DesktopNav is mounted on NO live route. The actually-rendered nav is **Page.tsx NavLinks** (NAV_LINKS, Page.tsx:182-191 — 5 links incl Feedback) and **MobileFrame.tsx TABS + TabKey** (MobileFrame.tsx:106,113-119 — 5 phone tabs incl Feedback->/coach). MC (#159) must edit THOSE to the locked 4-link set (Hem/Övning/Lektion/Framsteg, drop Feedback + the 'coach' TabKey), NOT DesktopNav. Rewrite home.spec.ts:50-59,61-77 (hard-asserts the Feedback tab) in the SAME PR. Until this lands the 4-link decision is FALSE live.
- **KVA is entirely uncovered.** KVA (540 q, 518 drillable, drill.tsx:21) has a BESPOKE KvaPrompt.tsx "Kvantitet I / II" 3-row apparatus (DrillQuestion.tsx:151-153) + 80 figure-bearing items, and is absent from M3 fixtures (fixturesSections.ts has no kva), M4, and M6. -> M4 (#156) adds KVA (idle+graded, reconcile KvaPrompt inline styles to tokens, figure rail + KvaPrompt for the 80); M6 (#158) captures it. WARNING: M4's "restrict promptMeta to ORD" edit touches DrillQuestion.tsx:105-112/151-163 — the SAME lines as the KVA branch and the empty-ELF path; the refactor must not regress KvaPrompt or the ELF empty-hero.
- **/avancerat is a live EDITION orphan** (avancerat.tsx:45-54 — full Page shell; the de-facto settings page: diagnostic reset + Cmd+K hint + 'Öppna Feedback' link). DISPOSITION: **fold into Settings #160, which RETIRES /avancerat.** M3H drops its Home entry (onAvancerat); until #160 it's reachable via ⌘K only. #160 is no longer purely deferred — it owns avancerat's retirement.

### MAJORS
- **Empty-prompt ELF cloze (135/540)**: promptIsShort (DrillQuestion.tsx:91) renders an empty .hpc-m3-display hero + a stray rail label. M4 must suppress the empty hero + rail for these and lean on the TEXTEN passage / a "Lucka N" eyebrow. ELF treated as "inherits LÄS chassis" is wrong — a LÄS prompt is never empty.
- **MEK has no owner**: the promptMeta strong-override fires for any prompt <=18 chars (DrillQuestion.tsx:105-112). M4 owns MEK + the promptMeta restriction (was implicitly only las/nog/xyz/dtk).
- **PedagogyPanel ERROR state** (PedagogyPanel.tsx:83-88 returns null -> blanks the whole post-grade column): M2 must render the verdict word + a quiet "kunde inte ladda" line (parallel to m3-missing); M6 captures it.
- **Post-grade LOADING skeleton** (PedagogyPanel.tsx:769-778 "Tänker igenom uppgiften", EDITION mono): a guaranteed cold-cache window. M2 styles it to hpc-m3 (or renders nothing-until-ready); M6 row.
- **Stale-resume recovery idle** (SessionPlayer.tsx:219-237: "Övningen är inte längre tillgänglig", CTA flips to Starta) + **resuming "Laddar…" spinner** (SessionPlayer.tsx:470-511, route-agnostic) — M5 names the spinner for repetition only; generalize to drill+diagnostik+repetition x both viewports in M6.
- **Lektion phone tabs are inert** (lektion.tsx:85,299 mount MobileFrame with NO onTabChange -> tapping any tab does nothing). M5 wires onTabChange={navigate(TAB_ROUTE)} on both mounts + an e2e tapping a tab from /lektion.
- **sectionLongLabel.ts — lock all 8 Swedish strings with the owner before M4 ships** (only ORD fixed; resolve ELF Swedish-label-vs-English). An unresolved label blanks every non-ORD eyebrow (the only progress affordance). M1 owns the map; gate M4 on it.
- **/coach ratify** (dev feedback tool, EDITION shell): DISPOSITION: **dev-only — leave EDITION, demote from primary nav** (drop from MobileFrame TABS + the 4-link sets; keep a ⌘K entry for reach); M6 records it as an intentional non-rebuilt dev surface. Same treatment as dev.tsx.

### MINORS
- **CommandPalette** (EDITION card, 0 hpc-m3, CommandPalette.tsx:259-353; COMMANDS still lists Coach + tweaks->avancerat): keep a Feedback/⌘K reach, retarget tweaks to the future Settings; give the card a flush-hairline pass or state it intentionally un-restyled. -> MC / #160.
- **parsing_status type is wrong** (questions.ts:71 declares 'complete'|'answer_only' but corpus has 'pdf-recovered' — 77 XYZ; the complete-filter silently drops ~116 q). Fix the union + decide if pdf-recovered is drillable. -> M4 or a small data-hygiene task.
- **Micro-surfaces to record/ratify in M6**: SplashLoading + ClerkLoadFailed (__root.tsx:88-121,179-193, ad-hoc inline), the MISSING 404 (main.tsx:32-33 has no notFoundComponent), the welcome body (welcome.tsx:52-185, bare EDITION — only the spalt swatch is added), and ExplanationPanel.tsx dead code + the unreachable SessionPlayer branch (739-753).
- **Home PlanSkeleton + allComplete** (HomeMobile.tsx:299): daily-plan-skeleton is in the preserved-testid list but has no styled M3 target — M3H/M6 sub-rows. DiagnosticReport NoSignal variant (DiagnosticReport.tsx:165) — separate diagnostik done-state or scope out.


---

## Decisions ratified post-red-team (2026-06-18)

- **Orphan surfaces — REBUILD BOTH.** DrillResult ("Klart." end-of-session, app/src/components/drill/DrillResult.tsx) and /progress (ProgressMobile.tsx) are rebuilt to M3, not left EDITION. DrillResult -> new task #161; /progress -> M5 (#157) upgraded from chrome-only to a FULL rebuild. Neither may pass through M6 as an EDITION orphan.
- **Mast nav set — Hem / Övning / Lektion / Framsteg (4 links).** Feedback/Coach (/coach, a dogfood feedback-export DEV tool, MobileFrame.tsx:116) is OUT of primary nav — reachable from a profile/settings area later, not the mast. Reconcile DesktopNav.tsx:32-35 (ships Coach, no Lektion) to this set; strip the "STRECK 5d ↘" streak flourish (DesktopNav.tsx:11).
- **Progress = textual eyebrow** (no bar); **picker -> future Settings (#160)**; ⌘K is a power accelerator not primary nav (memory feedback_cmdk_not_primary_nav).

## Red-team corrections — MUST HONOR (applied 2026-06-18)

Adversarial dissection returned GO-WITH-FIXES. 80–87-form drift is structurally hard now; two NEW vectors remained: ORPHAN SURFACES (resolved above) and FATIGUE drift. Per-task fixes below are verified against source.

**Global**
- Add ONE mechanical gate in M0: a Playwright getComputedStyle spec asserting load-bearing M3 values (.hpc-m3-frame max-width 880px; .hpc-m3-meta color === --muted NOT --accent; .hpc-m3-eyebrow letter-spacing 0.14em; .hpc-m3-step-n color === --accent; NO 2-col at 1440). Green required on every M-series PR.
- HARD GATE: M0 merges before any screen PR adds/modifies .hpc-m3-* DEFINITIONS (index.css:553-929). After M0, screen PRs CONSUME classes only. DROP the "add the classes yourself if M0 hasn't landed" escape hatch from M1/M2/M3H/M4.
- Treat the doc's MATCH labels as HYPOTHESES to falsify — confirm each by reading the LIVE computed value beside M3's, not by echoing the doc.
- Close the M6 "content differs, comparing chassis/type not copy" escape hatch: drive every live drill to the SAME qid as the M3 ref via ?qid= (task 146); chassis-only fallback needs per-row owner sign-off.
- Capture palette = DEFAULT_THEME.palette ('spalt', tokens.ts:495). NB PALETTES[0] is actually 'sand' — do not force that.

**M0 (#152)** — land the mechanical-gate Playwright harness; enforce the index.css hard gate; tokens.ts stays byte-identical (tokens.test snapshot); independently re-derive every MATCH row.
**M1 (#153)** — DELETE the BoksidanDesk active-drill branch (SessionPlayer.tsx:622-664), don't bypass; delete BoksidanDesk.tsx/StudyDesk.tsx from the live tree (only SessionPlayer->BoksidanDesk->StudyDesk; no e2e pins them) or keep dead with a removal ticket + grep-clean acceptance. Derive eyebrow position/total from SessionPlayer internal index+1/plan.length (SessionPlayer.tsx:177/215) — live routes pass NEITHER prop, so prop-threading alone blanks it; keep the undefined-guard. M1 is a HARD predecessor of M4. Capture every SessionPlayer branch (idle+active x phone+desktop). SPLIT: ship plumbing (sectionLongLabel.ts + usePregradeTactic.ts + threading + guard) as a unit-tested prerequisite PR, then the visual rebuild.
**M2 (#154)** — thread question.options + question.answer into DrillQuestion (DrillQuestion.tsx:200 gets only {qid,graded,correct,flush} today); remove the dead StudyDesk call site after M1. NULL-GUARD the struck-text join: 39 answer_only questions have options=null -> fall back to letter-only heading, never an empty strikethrough element (reads as a clean strikethrough in a screenshot). Unit test: each distractor's struck text === its option row, and no empty <s>. Tier badge: corpus carries deep/verification/supporting/support/optional beyond essential/detail — map essential->kärna, detail->detalj, collapse stragglers->detalj.
**M3H (#155)** — OWN the responsive.spec.ts rewrite (132-154: page-shell/brand-mark/running-head/status-line on / at 1440, page-shell count 0 at phone) in the SAME PR as the contract change. computeProjectedDelta returns null when EITHER week is below threshold (prior week often empty for the dogfood user -> blank is the honest common case); add a sign-aware color branch (.m3-stat-d hardcodes --ok green). SPLIT: M3H-a (scoring: formatScoreComma + computeProjectedDelta + scoring.test, NO UI, gated on unit tests) first, then M3H-b (visual + HomeMobile.test + responsive.spec rewrite, gated on clean diff).
**M4 (#156)** — contextTitle premise is WRONG: 506/540 live LÄS contexts embed the title as context[0] (double-newline-split). Derive contextTitle from context[0] AND strip that line from the rendered body, or drop m3-passage-h cleanly — do not leave the title as an unstyled first paragraph.
**M5 (#157)** — /progress is a FULL rebuild now (orphan decision), not chrome-only. Replace 'auth-top-band' in the preserved-testid list with the actually-asserted auth-form-pane + auth-brand-pane (responsive.spec.ts:192-193,198). Replace 'adopt/inherit the chassis' with concrete per-surface element lists (frame 880, rail 128/1/1fr, meta --muted, MC mast); verify by overlaying the M5 capture on the M1/M3H capture. Lektion bodies + IdleBody hero are body-EDITION under chrome-only fixes — capture the BODY in M6, get owner ratification.
**M6 (#158)** — add ELF + MEK + /progress + DrillResult to the matrix; make it incremental (scripted Playwright capture forced to spalt, contact sheet, grid stood up after M0, rows confirmed as tasks merge — not a 72-image marathon at peak fatigue).

## MC (#159) — Build the shared Minimal-mast chrome component

**Objective.** Build the ONE shared minimal-mast top band (the locked chrome decision) so M1/M3H/M5 CONSUME it — closing the doubled-orientation fork. Reference: the approved bake-off mock app/src/components/devbake/LayoutBakeoff.tsx MinimalMasthead.
**Sequencing.** After M0; blocks M1, M3H, M5.
**Home decision (owner-ratified).** Refactor Page.tsx's running head DOWN to the minimal mast (preferred — kills the EDITION shell at the source) vs a new MinimalMast.tsx. Own the SINGLE mount point; "no second mast fork" is an acceptance — M1/M3H/M5 only DELETE local runningHead/status and slot the MC mast.
**Element checklist.**
- Band: one hairline (border-bottom 1px var(--hairline)), transparent bg, vertical padding ~14px/11px, horizontal clamp(24px,5vw,64px). NO frosted backdrop, NO bottom status line.
- Brand: ⌜ HP-Coach — display italic, weight 600, ~17px ink; corner glyph in muted-2.
- Nav: Hem · Övning · Lektion · Framsteg — mono 11px, letter-spacing 0.12em, uppercase, muted (active = ink). 4 links ONLY. NO Feedback/Coach. NO streak flourish.
- Picker corner (interim): LJUS / SPALT in muted-2 mono — wired to uiStore setMode/setPalette; migrates to Settings (#160).
- NO section echo (the rail owns "ORD · 3/10"), NO folio, NO exam-tag.
- Responsive: full mast at 1440; degrade sanely at reader widths; phone uses bottom tabs / bare drill, NOT this mast.
**DoD.** Owner approves a pinned mast spec artifact (band height, hairline token, brand glyph, nav copy/spacing, picker-corner) BEFORE build (M3 renders no chrome — the mast is prose-defined). live diff @1440 light+dark; brand+4nav+picker present, NO echo/status/flourish; nav testids intact; pnpm test + e2e green; owner approves; NO auto-merge.

## DrillResult (#161) — Rebuild the "Klart." end-of-session screen to M3

**Objective.** Rebuild DrillResult.tsx (the payoff screen after EVERY session — currently 0 .hpc-m3-* classes, EDITION Page shell at SessionPlayer.tsx:586-596) to the M3 chassis. No M3 reference screen exists for it (M3 has only Home/Drill/SectionDrill), so the owner first ratifies the target composition: likely .m3-frame + m3-display "Klart." + m3-stat / m3-trap rows for the miss-list + "Att repetera imorgon" payoff. Keep the MathText miss-list fix (task 124). After MC (chrome) + M2 (so the miss-list distractor styling matches). Captured in M6.
